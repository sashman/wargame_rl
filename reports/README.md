# Experiment reports

> **Paths in these reports predate 2026-08-09.** Configs moved from
> `examples/env_config/` to `configs/{golden,dev,experiments}/`, and most of the
> configs named here were deleted once their question was answered. The old
> paths are left as written — a report records what was true when it was run.
> `git log -- examples/ configs/` finds any of them.
>
> **`.planning/` was retired on 2026-09-06** for GitHub Issues. Reports naming a
> path under it are likewise left as written — the same rule. Its one surviving
> file is [`2026-08-11-handover-state-of-play.md`](2026-08-11-handover-state-of-play.md),
> moved here unedited; everything else is in `git show <sha>^:.planning/`.

Findings from training experiments, kept for retrospection. Each report records what
was asked, what was measured, and what the measurement does and does not support.

## Index

| Report | Question | Outcome |
|---|---|---|
| **[2026-09-24 — Curriculum, the join on A3's shape (CM4 / FH1): NULL ×3 on both arms, the members are the larger half, and a frozen planner is not a fixed plan](2026-09-23-curriculum-commitment-plan-preregistration.md#amendment-6)** | Step 4 of the plan question (#384): the head that plans at 1.000 with scripted members, trained jointly with learning members from scratch (CM4, #405), and the members alone under that head's finished weights frozen (FH1, #408). Which half fails? | **NULL / NULL / NULL on both. CM4 as trained 0.00 / 0.37 / 0.32 at the cap with the same heads' plans walked by the bar at 0.72 / 0.68 / 0.72 — the head plans late and not fully, the members lag it by 0.35–0.72 and leave 0.68–0.77. FH1 split 0.37 / 0.61 / 0.63, BEHIND A3 with no plan (0.70 / 0.80 / 0.77) and the greedy environment writer (0.82 / 0.85 / 0.93); the executor with a random head reads 0.40 / 0.48 / 0.72, ahead on two seeds: the walk is the geometry, not the plan. And the frozen planner's plan degraded under its executor (last-turn coverage 0.59–0.71 against 0.93–0.97 with the bar): an adaptive head's plan is a function of its members, only the writer fixes a plan.** Proposed: no half-step under the joint head, no FH1 extension; hold the head's commitment still (decide only on an empty slot) as the next arm; climb to the half-step by the staged route (plan-only head, then members under it), where the executor-alone row is the legibility readout. |
| **[2026-09-23 — Curriculum, can the commitment head make a good plan? The plan-only readout, the desk check, and the plan-only rung (PL1 / PL2)](2026-09-23-curriculum-commitment-plan-preregistration.md)** | Stage 1's head jointly trained read 0.03 / 0.00 / 0.00 and stacked (#384). Is the plan the wall, or the execution? A plan-only readout (the head commits, the bar's soldiers walk), a desk check of what the head is paid and reads, and the head trained plan-only under the broadcast credit (PL1) and a per-unit counterfactual (PL2, B6). | **THE HEAD CAN PLAN: plan-only success 1.000 on 6/6 seeds at the cap under either credit, faster than the bar; AHEAD ×6 on the letter, PLANS on none, and the PLANS clause (a fixed covering assignment from turn one) was the defect — the heads plan adaptively, covering ≥ 0.93 by the last turn.** The counterfactual credit is a paired null (its planning critic at EV 0.05–0.07). Step 1: CM3's heads on the plan-only row 0.530 / 0.000 / 0.130 — one plans half-well, two stack. Step 2: the planning stream is broadcast with no pots; the head read nothing per unit. First launch void: KEEP on an empty slot let the head opt out and the script's fallback walked (fixed: KEEP illegal on an empty slot). Proposed: retire the distinct/claim clauses as criteria; run CM4 with PL1's recipe reading the plan-only row beside the as-trained row; a frozen-head members rung if CM4 reads low. |
| **[2026-09-22 — Curriculum, the execution phase: the soldiers paid by the plan alone — EX1 NULL, the A3 check COSTS; keeping improves, arrival collapses](2026-09-22-curriculum-commitment-execution-preregistration.md)** | Pay each soldier only by its squad's plan — approach the committed objective until the squad arrives, then hold, leaving in hold mode paying exactly zero, the end bonus kept on the planning stream — do the soldiers keep their objectives (#384)? | **EX1 0.100 / 0.030 / 0.000 against CM1-R1's 0.340 / 0.100 / 0.060, leave share 0.38 / 0.60 / 0.52 (bar 0.14): NULL. The A3 check 0.420 / 0.270 / 0.660 against `a3_cm`'s 0.820 / 0.850 / 0.930, turns 7.9 against 5.6–6.0: COSTS.** The reward is as designed (a mode-aware probe reads leaving in hold mode at +0.000 on every seed, staying at +0.024–0.044) and keeping improves where the plan is held (leave in hold mode 0.07–0.12 on A3, from 0.40–0.49), but with the outcome terms on a planning stream nobody receives, nothing says WHEN to arrive: the travel potential is path-independent and the success bonus was the members' only speed signal. Proposed next: an arrival pot on the member stream when the squad's mode flips. |
| **[2026-09-22 — Curriculum, the commitment head on A3's shape (CM3): NULL on every seed — the head commits, keeps and stacks](2026-09-22-curriculum-commitment-stage1-preregistration.md)** | Stage 1 of #384: can the set network CHOOSE each squad's objective at the start of its turn (a sampled commitment head, paid the close's outcome on a planning stream with a semi-Markov return and its own value head) and have its squads carry it out? | **0.030 / 0.000 / 0.000 at 122,880 against A3's own 0.700 / 0.800 / 0.770 and the environment-assigned 0.820 / 0.850 / 0.930.** The head learned to commit and keep (persist 0.85–0.91 on two seeds), the members follow (0.74–0.87) and complete its plan (s2 0.95), and the plan stacks: 1.6–2.6 squads per claimed objective, one objective empty in 90–100% of episodes; planning explained variance 0.23–0.33. Every squad's decision is paid the same broadcast outcome, so stacking and spreading earn identical credit — the shared-credit failure the design held B6 for. Proposed next: a per-unit counterfactual (coverage without the squad's bodies) on the planning stream. |
| **[2026-09-21 — Curriculum, the commitment layer's revision: the legibility rung is READ by two seeds of three, CM1 under the arrived-keeps rule is NULL](2026-09-21-curriculum-commitment-revision-preregistration.md)** | After Stage 0 found the members never read the marked target (#384), can the set network learn to read a commitment at all when reading it is the only way to win — and does closing the retirement hole make the half-step's squads stay? | **LR1 NULL on the letter, and the first per-model policies that condition on a commitment**: 0.710 / 0.950 / 0.080 at 122,880 with the flag blanked taking s2 to 0.00 and s1 to 0.00 (and the flag pointed at the nearest objective the same), s3 solving the geometry and never the flag; bar 1.000, plain `take` 0.000. **CM1-R1 NULL as pre-registered**: leave share 0.56 / 0.60 / 0.60 (Stage 0 0.58 / 0.56 / 0.50), success 0.34 / 0.10 / 0.06 against 0.15 / 0.28 / 0.19, the leaving step still paid ~0: the retirement hole was not the price of leaving. Two defects fixed on the way: the env's settle context carried no commitment (a commitment-keyed criterion was always false) and scripted names were scored on the phase facade, which has no commitment state. |
| **[2026-09-21 — Curriculum, the commitment layer's Stage 0 on the ladder: plumbing PASS, A3 NULL (ahead on two seeds), CM1 EXECUTION — the members never read the pointer](2026-09-21-curriculum-commitment-stage0-preregistration.md)** | Does a per-unit commitment (#384) — a sticky objective assignment written by the environment, shown to every member as a marked-target relation, with travel and staying keyed to it, no head yet — lift the five-objective half-step (#387)? | **Plumbing PASS** on A0 / A1 / A2 (1.000 ×9 at 40,960; the leave clause unclearable against a zero bar, recorded as a clause defect). **A3 NULL on the letter** (0.820 / 0.850 / 0.930 at 122,880 against A3's own 0.700 / 0.800 / 0.770: ahead on two seeds, none behind, held 3.81–3.92, the empty column nearly gone). **CM1 EXECUTION as pre-registered**: 0.150 / 0.280 / 0.190 against the original's 0.030 / 0.060 / 0.330 (ahead on two seeds, behind on the third — the staying arms' shape), leave share on the assigned objective 0.50–0.58, follow-through 0.76–0.79, a body on its objective still never stands still. A play-time ablation that blanks or misdirects the flag moves success by hundredths on both rungs: **the policy never reads the commitment; what moved was the reward keying alone**. The D9 diagnosis names the retirement hole (a squad walking off a shared objective is re-assigned for free) and a legibility rung before any head. |
| **[2026-09-20 — Curriculum A5-points, three arms in one night: the wall is the per-model trainer's, a warm start carries four objectives of five, and a start with four objectives given is walked off](2026-09-20-curriculum-a5-points-whose-wall-it-is.md)** | The whole-army control on the five-objective half-step (#340) reads **0.980 / 0.620 / 0.870 at 60 epochs and 0.910 / 0.810 / 0.950 at 120**, 4.8–4.95 objectives held — FAIL on the letter by two seeds and two to five times ahead of any per-model setting at equal rounds: the wall is the per-model trainer's. The per-model trainer warm-started from A4x reads **0.460 / 0.260 / 0.350 at 122,880 and 0.340 / 0.170 / 0.600 at 245,760** — FAIL on the letter, ahead of the original (0.030 / 0.060 / 0.330) on 3/3 at twice the cap, no walk-off, one seed still climbing. The backward start curriculum (episodes begin with four squads on four objectives; built as `--backward-start`) reads **0.000 / 0.030 / 0.030** and its level never stepped down: with four objectives given the policy keeps 2.0–3.1 of them to the end where the bar keeps 3.9 — given an objective, this policy walks off it. **Addendum (same day): the update regime is ruled out** — at the whole-army trainer's 2,048 rounds per update the per-model trainer reads 0.000 / 0.010 / 0.000, and the walk-off probe finds a body on an objective paid the same for staying as for leaving and standing still on 0–2% of its decisions. **Addendum 2 (same night): the staying payment is closed too** — a per-decision pot for ending inside an objective at two weights reads 0.21 / 0.19 / 0.12 and 0.23 / 0.11 / 0.11, FAIL on the letter, the leave share 0.48–0.67 and the stationary action never learned; six reward-side levers closed, the commitment layer (#384) is next. |
| **[2026-09-19 — Curriculum A5-points, the reward-shape arms: paying the criterion as a lump breaks the critic, and paying per point buys the near column](2026-09-19-curriculum-a5-points-reward-shape-the-lump-breaks-the-critic.md)** | Two one-change arms on the half-step (#340), three seeds each from scratch: R1 pays the success bonus in full however late (the original pays 1.0 of 5.0 for a first success in round nine); R2 adds 1.0 per point held at the clock. **FAIL both, behind the original at every read**: 0.000 / 0.030 / 0.010 and 0.020 / 0.030 / 0.000 at 122,880 against 0.030 / 0.060 / 0.330 (the original 0.22 / 0.22 / 0.11 at 81,920 where the arms read 0.00–0.02). Explained variance −0.5 to +0.2 on five of six seeds for the whole run (the original 0.63–0.73): a terminal lump is a value target this critic cannot fit, and the remaining-rounds scale was what kept it small. R2 at two thirds of the budget had the far column empty in 90–100% of episodes on every seed — partial credit bought the partial behaviour. Three arms on this half-step (discount, terminal shape, credit) now read the same policy; the spread wall is not the reward's timing, size, shape or attribution. First arms recorded in-run; videos on every run. |
| **[2026-09-19 — Curriculum A5-points at gamma 0.99: the horizon is not the wall — the critic sees the longer game and the policy does not move](2026-09-19-curriculum-a5-points-discount-the-horizon-is-not-the-wall.md)** | The ladder's first optimiser-side arm (#340): A5-points from scratch with the discount per turn close raised 0.9 → 0.99, so the terminal bonus reaches the first decisions at 4.19 of the bar's 4.38 instead of 2.79. **FAIL as pre-registered: 0.070 / 0.180 / 0.100** at 122,880 against the original's 0.030 / 0.060 / 0.330 (MOVES needs two SE ahead on 3/3: one of three), behind the original at matched rounds at 40k and 80k, the same census on every column (five or six of eighteen bodies on points, a point empty on every seed, the walk-off, the clock run out), sampled 6–7 vp worse than greedy. The one thing that moved is the critic: explained variance 0.63–0.73 → 0.78–0.85 on the same in-run curve. The original's own 80k read (0.22 / 0.22 / 0.11) shows two seeds peaked mid-run and collapsed. Next on the half-step: the reward-shape arm (flat terminal bonus; coverage paid per flag to its unique holder), not another discount. |
| **[2026-09-19 — Curriculum E1: two armed armies on a flat board — the control learns the guns and loses the formation; the per-model arm learns neither, and A5i's walk does not survive contact](2026-09-19-curriculum-e1-the-walk-does-not-survive-contact.md)** | The join of the bodies and guns branches (#340, #381): A5's army with rifles on both sides against a mirror `squad_march_take`; the whole-army control, the per-model arm from scratch, and a companion warm-started from A5i, three seeds each, read paired at n=180 on each facade. | **FAIL as pre-registered, all nine runs** — the control beats the script by 45–73 vp on two seeds by killing and fails the three-clause letter on every seed (formation 0.77–0.78, one seed's points); the per-model arm from scratch is **150–160 vp behind** with a fifth of the bar's points; the companion from A5i had its walk overwritten within 10k rounds and ends 88–180 behind — a warm start does not survive a change of game, and the per-model trainer has not learned to shoot from reward on any rung |
| **[2026-09-19 — Curriculum D3: the anchor is a brake with no engine — anchored PPO holds a clone with two thirds of the rung to gain and takes no step](2026-09-19-curriculum-d3-the-anchor-is-a-brake-with-no-engine.md)** | Plain `take` cloned on C3b (0.361 against the escort's 0.978, the gap one ordered plan), anchored PPO from it at two coefficients (#340), three seeds each, 122,880 rounds. | **HOLDS on both, IMPROVES on neither** — 0.33–0.36 on every seed, the census the clone's on every column, twenty-four reads without one moving, 17–22 episodes in 180 differing from the clone at the end; **the D-route on the per-model facade is imitation only**, and the anchor's coefficient is not the lever (10 and 1 hold, 0.1 loses, 0 destroys) |
| **[2026-09-19 — Curriculum A5, the second pass: PASS on the start axis — the credit is not the wall, the clone is the ceiling, the anchor holds it over the mark](2026-09-19-curriculum-a5-passes-on-the-start-axis.md)** | Ten per-model arms and two half-steps on the allocation rung (#340): the actor credit built and run three ways, the bar cloned from 1,200 then 2,000 games, anchored PPO from each, and A4's two axes stepped one at a time. | **A5 PASSES on the letter (A5i 0.960 / 0.980 / 0.960 at 122,880, no dip, sampled = greedy) by imitation of the bar held under the KL anchor; nothing learned from reward** — the credit moves nothing (A5d 0.05 / 0.07 / 0.01), the clone is the ceiling (0.930 → 0.960 with more games, one failure event: two squads on one point), anchored PPO holds every clone and lifts none (coef 10 and 1 hold, 0.1 loses), and the wall is the point count (A5-bodies 0.96 / 0.93 / 0.86, A5-points **0.03 / 0.06 / 0.33**) |
| **[2026-09-17 — Curriculum D2: the anchor, not the critic](2026-09-17-curriculum-d2-the-anchor-not-the-critic.md)** | Four arms on the rung that asks whether per-model PPO improves a policy that starts with the plan (#340; #376, #379, #378, #380): PPO from D1b's escort clone on C3b's final-board scenario, plain, with the clone's critic fitted first, with a KL anchor to the clone, and with both — three seeds each, 122,880 rounds, read at n=180 paired against the clone. | **REWARD DESTROYS A COPIED PLAN UNLESS THE WEIGHTS ARE ANCHORED TO IT; THE CRITIC IS NOT THE LEVER; UNDER THE ANCHOR REWARD IMPROVES NOTHING.** Plain PPO **0.028 / 0.183 / 0.028** from a 0.95 start, gone by 2,560 rounds (the drift prediction held exactly); the critic fitted first (explained variance 0.74, policy bit-identical) **0.056 / 0.006 / 0.028**, the same collapse at the same speed — the whole-army record's diagnosis is not the mechanism here; the KL anchor (#332, coef 10 / target 0.03) **0.944 / 0.956 / 0.956**, paired vp −1.4 to +0.7 (t −1.5 to 0.9), half the episodes identical, the board the clone's at every turn, explained variance 0.66 from cold; both changes 0.939 / 0.906 / 0.911, the anchor's twin a hair worse. The destroyed policies keep the order (fire first in 87–97%) and lose the walk. Rules: warm-start the per-model PPO from a clone only under a KL anchor to it; the critic is not the lever; the IMPROVES question is now the anchor's coefficient. |
| **[2026-09-17 — Curriculum D1b: the escort cloned from four times the games](2026-09-17-curriculum-d1b-four-times-the-games.md)** | D1's clone with one change (#340, #375): 1,200 demonstration games instead of 300, two fit seeds. Does more data close the success gap the over-fitting displacement head left? | **PASS ON THE RUNG'S CRITERION, FAIL ON THE FIDELITY BOUND — WHICH IS RETIRED.** Success **0.960 / 0.960** (bound 0.96; the escort 0.990), held 3.93, alive 0.953 / 0.958, kill before arrival **1.00 / 1.00**, blockers wiped 98% — from D1's 0.83 / 0.85. The displacement head rose 0.61 → **0.72** held-out (0.94 on the training games) against a 0.90 bound the teacher's own quantisation forbids; the opening order stays at chance held-out. The two clones agree to the third decimal. The best policy on the per-model facade on any rung with guns, and D2's start. Rule: score a clone by the rung's criterion and the ordering; report per-head match, never bound it. |
| **[2026-09-17 — Curriculum D1: the clone holds the plan and misses the mark](2026-09-17-curriculum-d1-the-clone-holds-the-plan.md)** | The supervised rung (#340, #373): the escort's decisions copied into the set network by maximum likelihood from 300 demonstration games (two clones, fit seeds 0 and 1), scored on C3b's final-board scenario. Can the network hold an ordered plan that reward could not teach? | **FAIL ON THE LETTER, AND THE PLAN IS HELD.** Success **0.830 / 0.850** against the escort's 0.990 (bound 0.96), kill before arrival **0.99 / 1.00**, blockers wiped 98%, alive 0.90 — where reward alone reached 0.20 on the same scenario. The pre-registered joint match reads 0.44 because it counts a factor no clone can learn: which unit the escort opens next is not in the observation (chance held-out, 0.60 on the training set). Per head, declaration 0.93 and unit-pointer 1.00; the displacement head over-fits (0.95 on the training episodes, 0.61 held-out), which is where the success gap lives — a data-size question, answered by D1b at four times the demonstrations. Rule: score a clone per head given the teacher's model and by the rung's criterion, never by a joint match that includes whose turn it is. The two clones agree to a thousandth. |
| **[2026-09-17 — Curriculum C3b: with success read at the end of the game, nobody holds the point](2026-09-17-curriculum-c3b-nobody-holds-the-point.md)** | C3 with one flag (#340, #372): `terminate_on_success: false`, so the four points have to be held on the final board after twelve rounds — the dash that won C3 has to survive on the blockers' point. Does either trainer learn the escort's plan, shoot first then walk in and stay? | **FAIL AS PRE-REGISTERED, EVERY SEED OF EVERY TRAINER.** Per-model from C2 **0.200 / 0.130 / 0.230** at 245,760 (ordering 0.86 / 0.77 / 0.85), from scratch **0.020 / 0.020 / 0.060**, the control **0.280 / 0.000 / 0.450** at 120 epochs; the escort 0.990, plain `take` 0.280. Removing the instant win moved the arm halfway to the plan — it now wipes the blockers in 75–79% of episodes (C3: 16–30%) and fires first in 77–86% — and it stops there: two and a half points held from round five to the end, 47% of its openings stationary, the cleared point and the far point empty in half the episodes, explained variance 0.20–0.25. The companion's board decays. Neither trainer learns an ordered plan from reward at this budget; the missing piece is the continuation after the half it was paid for, which is the D rungs' question (clone the escort, then improve it). Rule: read a multi-phase rung's census by phase. |
| **[2026-09-17 — Curriculum C3: the arm found the dash, not the plan](2026-09-17-curriculum-c3-the-dash-not-the-plan.md)** | The first rung that needs a plan with an order to it (#340, #370): one of our squads armed and outranging three tough, lethal blockers on a point; the escort shoots them off from outside their reach while the others wait. Does the per-model arm learn the order — warm-started from C2, from scratch, and the whole-army control beside both? | **FAIL AS PRE-REGISTERED ON BOTH CLAUSES — THE WARM START CARRIED THE WRONG HABIT.** The arm from C2 reads **0.660 / 0.920 / 0.830** at 245,760 with the kill before the unarmed arrival in **0.67 / 0.71 / 0.62** of episodes: it dashes a last body onto the blockers' point the instant the other three are held and `terminate_on_success` ends the game before the blockers fire again — faster than the escort on two seeds, the blockers alive in 70–84% of episodes, a third of its bodies dead, drifting between that and something else. The from-scratch companion learned the **order** (0.95–1.00) and not the walk (0.740 / 0.740 / 0.090). The control fails 3/3 at 120 epochs (0.87 / 0.83 / 0.84). The escort reads 0.990 and plain `take` 0.590 (both facades identical, after the #317 fix). The scenario's defect is measured and answered by C3b (#372): success read at the end, where the escort still reads 0.990 and `take` 0.280. Rule: judge success on the final board wherever reaching a state and keeping it are different skills. |
| **[2026-09-17 — Curriculum C2: the enemy squad on the point fires back](2026-09-17-curriculum-c2-one-seed-one-hundredth-short.md)** | The first guns rung (#340, #368): C1 with the enemy unit armed and firing at whoever approaches (`hold_and_shoot`, the shooting phase stepped), our squads unarmed. Does the per-model arm, warm-started from C1, still put a body on every point — and does the whole-army control? | **FAIL AS PRE-REGISTERED BY ONE SEED AT ONE HUNDREDTH UNDER THE MARK — AND THE FIRST RUNG WHERE THE PER-MODEL ARM IS AHEAD OF THE CONTROL.** Per-model **0.890 / 0.920 / 0.950** at 245,760 (n=100), turns 9.8–10.2 phase-clock against the script's 9.60, `alive` 0.826 / 0.842 / 0.856 against the bar's 0.842, `held` 2.87–3.00; 80% in-run by 2,560 rounds on every seed. The whole-army control **fails its own criterion on all three seeds at 120 epochs (0.790 / 0.690 / 0.780)**, arriving in round seven of eight (turns 14.2–14.8 of 16) with its bodies on the points it reaches — speed under fire, on a rung the script passes at 0.900. The arm's misses are spread across the points (C1's allocation residual); the script's are at the enemy's disc. Redesigned before launch: with both sides armed the phase facade ends the battle on the wipe (#317) and the bar reads 0.38 there against 1.00 on the per-model facade, so no whole-army control is readable on a rung where the opponent can be wiped. |
| **[2026-09-17 — Curriculum T1: warm-starting A5 from A4x does not rescue it](2026-09-17-curriculum-t1-transfer-does-not-rescue-a5.md)** | The transfer rung (#340, #363): A5 (eight squads, six objectives) started from A4x's checkpoints seed for seed, read against A5b from scratch. Does what a smaller army learned carry to a bigger one, and by how much? | **FAIL AS PRE-REGISTERED, 0 OF 3 — AND THE TRANSFER IS REAL.** No seed reaches a rolling 95% in-run (peaks 64 / 51 / 64%), so rounds-to-pass is the budget on every seed and the halving bound is missed; at n=100 **0.450 / 0.290 / 0.200** against A5b's 0.260 / 0.180 / 0.160, `held` 4.9 / 4.3 / 4.5 of 6. The warm start is ahead at every matched round and at 20,480 rounds already sits where A5b ends at 245,760 (eight to eleven bodies on points, four held) — then 225k more rounds buy almost nothing. A head start on a curve that never reaches the mark cannot halve an infinite rounds-to-pass: the bound presupposed that scratch passes. Health panel normal (clip 0.22–0.32) where A5b's is red. Consequence applied: later rungs train from scratch unless the start is the rung's own axis; the C rungs' warm-start rule stands on S3 and C1. |
| **[2026-09-16 — Curriculum C1: with an enemy squad standing on a point, two seeds of three](2026-09-16-curriculum-c1-two-of-three.md)** | The enemy rung of the per-model curriculum (#340): A3 plus one enemy unit of three holding one of the four points, nobody shooting. Does the per-model arm, warm-started from A3x, still get onto every point? | **FAIL AS PRE-REGISTERED, BY ONE SEED ON A PLATEAU.** Greedy success **0.990 / 0.930 / 0.990** at 245,760 rounds, turns 5.0–5.4 against the script's 4.98 and the control's 6.8–7.1; the control passes 3 of 3 at 120 epochs (0.98 / 0.99 / 0.99). Every seed was at 80% in-run by 2,560 rounds — the warm start carries the skill — and seed 2 sat at 85–95 for 240k rounds without a dip. The census says the short point is never the enemy's: the engagement rule costs nothing the policy cannot learn; what remains is A3's residual allocation error. Coherency 0.50–0.63 greedy, 0.34–0.47 sampled. |
| **[2026-09-15 — A3 speed screen S3: warm-starting the spread rung from the one-point rung](2026-09-15-curriculum-a3-speed-s3-warm.md)** | Why does A3 take ~200k rounds? Third of three arms: `a3.yaml` unchanged, each seed started from its A2b checkpoint. Does the one-point skill carry over? | **AHEAD ON 3 OF 3** (not FASTER — two seeds under 0.95). Success **0.910 / 0.940 / 0.960** at 122,880 against scratch's 0.700 / 0.800 / 0.770, +0.14 to +0.21 every seed; in-run 80% at **11k–22k rounds** where scratch took 91k or never; seed 3 arrives faster than the script with 3.96 of 4 held. The prediction (NULL: it must unlearn stacking) was wrong. The transfer rung T1 now has a prior one rung early. Unpaired on init. |
| **[2026-09-15 — A3 speed screen S2: one objective per squad in the travel assignment](2026-09-15-curriculum-a3-speed-s2-match.md)** | Second arm: the travel term's per-objective assignment becomes a matching (`one_objective_per_group`), so no squad owns two points and none is paid toward a claimed one. | **HARMFUL** as the clause names it. Success **0.830 / 0.690 / 0.610** against scratch's 0.700 / 0.800 / 0.770 — two seeds 0.11–0.16 behind, `held` 3.25 on the worst, in-run curves falling back to 30–70% at the end; explained variance 0.26–0.38, below scratch. The gate moved exactly as measured before launch (0% multi-owning, 1% split) and the policy learned slower under it — a matching re-solves every step and a target switch is unpaid. Fifth empty-or-worse result on this term; the flag stays, default off. |
| **[2026-09-15 — A3 speed screen S1: paying for standing on a point, pot-split](2026-09-15-curriculum-a3-speed-s1-hold.md)** | First arm: `objective_hold` at `crowding_exponent` 1.0 added to `a3.yaml`, aimed at a measured walk-off (at 20k rounds the scratch policy reaches points and leaves them). | **NULL on the letter, two seeds moving.** Success **0.940 / 0.810 / 0.900** against scratch's 0.700 / 0.800 / 0.770 — +0.24 and +0.13 on two seeds, +0.01 on the third, no seed at 0.95. Explained variance 0.59–0.68 against 0.34–0.45. **The walk-off is still there at 20k on two seeds**, and the retimer says why: on the per-model facade a per-model STATE term is paid at the turn close as the mean over alive models, one scalar, so the pot never reaches the body that moved — every per-model state term is a global lever on this facade. Sampled coherency 0.16–0.23, the ladder's lowest. |
| **[2026-09-15 — Curriculum A4x: given twice the cap, the per-model arm places the spare squad](2026-09-15-curriculum-a4x-passes.md)** | A4's per-model arm read one SE short at the cap while the control had passed inside it at 92k–121k rounds, so 6× the control was far above the cap. Read at 2× the cap, does it get there? | **PASS 3 OF 3, AT THE SCRIPT'S SPEED.** Greedy success **0.980 / 1.000 / 1.000** at 245,760 rounds, `held` 2.96 / 3.00 / 3.00 of 3, turns 4.72–4.75 against the script's 4.70 and the control's 6.12–6.23. The rule the cap implied is confirmed: when 6× the control's slowest pass exceeds the cap, the per-model budget is 2× the cap, read once. The spare squad ends on a held point (`on_obj` 0.65–0.73 v 0.724); coherency rose to 0.52–0.70 greedy unpaid; explained variance 0.56–0.59. |
| **[2026-09-15 — Curriculum A4: one standard error short at the cap on two seeds](2026-09-15-curriculum-a4-one-se-short-at-the-cap.md)** | The spare-squad rung of the per-model curriculum (#340): four squads, three objectives, every point occupied. Does the per-model trainer place a surplus squad within the cap? | **FAIL AS PRE-REGISTERED, ONE SE SHORT AND RISING.** Greedy success **0.930 / 0.930 / 0.970** at 122,880 rounds, `held` 2.91–2.97 of 3, against a control at 0.98 / 0.98 / 0.99 that itself passed only at 92k–121k of its 123k rounds. The same shape as A3 at its cap, one rung closer; the spare squad ends on a held point (`on_obj` 0.60–0.72 v the script's 0.724). **A4x** (#358) resumes the same runs to 245,760 and writes the rule the cap implies: when 6× the control exceeds the cap, the per-model budget is 2× the cap. |
| **[2026-09-15 — Curriculum A3x: given the control's rounds, the per-model arm spreads over four points](2026-09-15-curriculum-a3x-passes.md)** | A3's per-model arm trailed the control at the cap while still rising, and the control had needed twice the cap. Given the same 245,760 rounds, does it get there? | **PASS 3 OF 3.** Greedy success **0.960 / 0.960 / 0.970**, `held` 3.94–3.96 of 4, turns 5.19–5.33 against the script's 5.28 and the control's 6.24–6.33 — matching the control's 0.95 / 0.95 / 0.96 at the same rounds. Both trainers take ~200k rounds to learn a conjunction over four points; the per-model one arrives a round faster. Explained variance rose from 0.34–0.45 to 0.48–0.57 as the policy converged. The cap rule is now symmetric: when the control needs its extension, the arm under test gets the same rounds before the rung is called. Coherency 0.38–0.52 greedy, the ladder's lowest, unpaid. |
| **[2026-09-15 — Curriculum A3: behind the control on the spread rung at the cap](2026-09-15-curriculum-a3-behind-the-control-at-the-cap.md)** | The spread rung of the per-model curriculum (#340): four squads, four objectives, every point occupied. Does the per-model trainer learn to spread within the cap? | **FAIL AS PRE-REGISTERED, STILL RISING AT THE CAP.** Greedy success **0.700 / 0.800 / 0.770** at 122,880 rounds, `held` 3.5–3.7 of 4 — the bodies arrive and one point stays short — against a control at 0.85 / 0.98 / 0.93 at the same rounds and **0.95 / 0.95 / 0.96 after its once-only extension to 245k**. The first rung on which the per-model arm trails the control at equal rounds, and the first on which the control needed twice the cap. Explained variance 0.34–0.45, the ladder's lowest: a conjunction over four points is what per-step credit has not yet learned to value. **A3x** (#357) resumes the same runs to the control's 245,760 rounds, the symmetric extension. |
| **[2026-09-15 — Curriculum A2b: at `ent_coef` 0.003 the per-model arm holds the rung](2026-09-15-curriculum-a2b-passes-with-drift.md)** | A2's per-model arm solved the rung by 10k rounds and unlearned it by 123k at the default entropy coefficient. Same seeds, same everything, `ent_coef` 0.003: does it hold? | **PASS WITH DRIFT — THE ENTROPY BONUS WAS THE CAUSE.** Greedy success **1.000 / 1.000 / 0.990** at the end of 122,880 rounds, turns 4.11–4.48 against the script's 4.55; displacement-head entropy **0.3 nats against A2's 3.3**, clip fraction 0.12–0.15 against 0.26–0.37. The strict clause (no in-run dip below 0.80 after the first pass) is missed on every seed — 7 to 10 of ~230 evaluations, two transient collapses to zero at n=30, all recovered, none after 63k rounds — where A2's dips were terminal. Every per-model arm from here runs at 0.003. Coherency 0.74–0.79 greedy against the bar's 0.889 and A2's early 1.000: faster than the script and less coherent than its own 10k self. |
| **[2026-09-15 — Curriculum A2: solved by 10k rounds, then unlearned](2026-09-15-curriculum-a2-solved-then-unlearned.md)** | The third rung of the per-model curriculum (#340): four squads onto one point. Does the per-model trainer learn a crowd, and hold what it learned to the end of its budget? | **FAIL AS PRE-REGISTERED — IT PASSED AT 10K ROUNDS AND DRIFTED.** At the end of 122,880 rounds greedy success reads **0.370 / 0.880 / 0.980** against a control at 1.000 ×3; scored from the periodic checkpoints, **every seed read 1.000 at 10,240 rounds, half a round faster than the script (4.02 v 4.55) with coherency 1.000**, held it through 20k, then lost formation (to 0.08–0.36) and success. The critic is fine (explained variance 0.88–0.90); the displacement head's entropy plateaued at ~3.3 of 4.57 nats from 12k on and the clip fraction climbed to 0.26–0.37, the ladder's highest — the entropy bonus at 0.03 is what the update is left optimising once every episode succeeds. **A2b** (#356) reruns the same seeds with `ent_coef` 0.003, pre-registered with "pass at the end and no dip after the first pass". Reading at the end of the budget is what made the drift visible. |
| **[2026-09-16 — Curriculum A5b: the per-model arm fails the allocation rung, and not the way the control did](2026-09-16-curriculum-a5b-fails-differently.md)** | The per-model arm on A5 (#340, #355): eight squads of three, six objectives, from scratch, read at the control's 245,760 rounds beside it rather than gated by it. Does per-step credit buy the allocation the whole-army trainer could not? | **FAIL AS PRE-REGISTERED, 0 OF 3, AND A DIFFERENT FAILURE.** Success **0.260 / 0.180 / 0.160** at n=100, never a rolling 50% in-run (peaks 48 / 43 / 30%), turns 9.5–9.7 of ten against the script's 6.77. The control (0.80 / 0.94 / 0.98) puts 22 of 24 bodies on points and leaves one empty; the arm puts **8–10 bodies on points and holds 4.1–4.6 of 6**, the empty point a different one each episode, max stack 3.0–3.5 against the script's 5.8, coherency **0.10** — two thirds of the army never arrives, the A3 speed screen's walk-off never outgrown. The first rung where the per-model arm is worse than the control at equal rounds. Health panel red (clip 0.37–0.42, ratio p99 2.25–2.40, critic fine). Hypothesis on file: the per-model state terms are paid as the mean over alive models on this facade, so a body's credit for its own inch is 1/24 here against 1/12 on A3–A4. |
| **[2026-09-15 — Curriculum A5: the whole-army control cannot put a squad on every point](2026-09-15-curriculum-a5-control-cannot-allocate.md)** | The last movement-only rung of the per-model curriculum (#340): eight squads, six objectives, success only when every point has a body on it. Can the whole-army pipeline control learn it, and what does its answer gate? | **NULL (SCENARIO) AS PRE-REGISTERED, AND THE CLAUSE IS WRONG.** The control reads success **0.890 / 0.820 / 0.600** at its 60-epoch cap and **0.800 / 0.940 / 0.980** extended to 120 (245k rounds) — two of three miss the bound — while the script scores **1.000**. The failure has the record's own shape: `held` 5.76–5.98 of 6 with `on_obj` 0.93–0.96, **twenty-two of twenty-four bodies on points and one point empty**, the allocation failure attributed to the whole-army trainer at exactly this shape. A NULL clause that fires when the control cannot do what the script can measures the control; the ladder was built to measure the per-model arm against it. The per-model arm did not run under this arm; **A5b** runs it with the clause corrected (the script gates the scenario, the control calibrates the budget and is read beside). Coherency collapsed to 0.47–0.62 with the extension, unpaid. |
| **[2026-09-15 — Curriculum A1x: the same runs at twice the rounds pass 3 of 3](2026-09-15-curriculum-a1x-passes.md)** | A1's failing seed was still rising at a budget of 3× the whole-army control's rounds-to-pass. Given 6×, does it pass, and do the passing seeds hold? | **PASS 3 OF 3 — THE BUDGET RULE WAS THE DEFECT.** Resumed in place to 122,880 rounds: success **1.000 / 1.000 / 1.000**, turns **4.93 / 4.88 / 4.93** against the script's 4.96 and the control's 6.1–7.4; the seed at 0.790 passed on the first evaluation after the resume and never dipped, the other two hold with one n=30 dip each in 240. Per-model rounds-to-pass 32k–62k against the control's 14k–20k, the same 2–4× as A0, so the budget floor becomes **6× the control** from A2. Coherency, unpaid, rose on every seed with the extra rounds (greedy 0.97–0.98, sampled 0.70–0.81). A1's FAIL stands as written; this is its answer. ⚠ The resume dropped the eval and checkpoint cadences and overwrote the provenance (#346). |
| **[2026-09-15 — Curriculum A1 fails on one seed](2026-09-15-curriculum-a1-fails-on-one-seed.md)** | The second rung of the per-model curriculum (#340): A0's three models bound into one squad. Does binding bodies into a unit change what the per-model trainer learns? | **FAIL AS PRE-REGISTERED, 2 OF 3 SEEDS PASS.** Greedy success **1.000 / 0.790 / 1.000** at n=100 against a control that passes 3/3; the two passing seeds land at the script's speed (5.01 / 4.90 v 4.96) where every control seed is 1.1–2.5 rounds slower. The failing seed plateaued at 0.80–0.93 in its last quarter and was still moving, and the passing seeds needed 32k–53k rounds against the control's 14k–20k — so **the 3×-the-control budget rule is the live suspect**, and a pre-registered follow-up (A1x, the same runs resumed to 122,880 rounds) decides. Two readouts to keep: the ratio tail is **1.68–1.83 on pass and fail alike** at 128 rounds per update, so the panel's ~1.5 line is recalibrated for this regime rather than read as a fault; and the sampled policy's coherency is **0.23–0.62 against the greedy policy's 0.84–0.94** — the policy trained walks the squad apart far more than the policy scored, with nothing on this rung paying for formation. |
| **[2026-09-15 — Curriculum A0 passes](2026-09-15-curriculum-a0-passes.md)** | The first rung of the per-model curriculum (#340): can the per-model trainer learn to walk three lone models onto one objective, with the whole-army trainer as the pipeline control? | **PASS WITH A DEFECT, 3 of 3 seeds.** Greedy success **1.000 / 1.000 / 0.950** at n=100, turns **4.93 / 4.96 / 5.11** against the script's 4.93 — the per-model agent arrives at the script's own speed where the whole-army control (success 1.000 ×3) is a round slower at 6.21 / 6.67 / 6.07. It takes **2–4× the control's rounds** to pass (29k / 52k / 41k v 16k / 12k / 18k). No do-nothing fingerprint (stationary 0.00–0.04, greedy ≈ sampled). The defect: the importance ratio's p99 is 1.65–1.79 on every seed over the last quarter, past the panel's ~1.5 line — whether that is the update over-stepping or a threshold that does not transfer from 1024 to 128 rounds per update is carried to A1. ⚠ The pre-registered speed bound ("script's slowest + 1") was missed 3/3 by the control and recorded as a defect in the bound before the per-model numbers existed. |
| **[2026-09-14 — One episode per update](2026-09-14-one-episode-per-update.md)** | The first per-model training runs (the calibration sweep and the observe runs) sat at the random floor. Did the set network fail to learn, or did the pipeline never give it a chance? | **VOID FOR REGIME, NOT A VERDICT ON THE ARCHITECTURE.** Every run trained on **one rollout env**, updating every 8–32 rounds against the phase facade's 1024 — under one episode per update, re-read four times, at 32–128× the per-round policy drift. Eval sat at −147 to −241 (random floor −222.5) with the greedy stationary share swinging **0.00 ↔ 0.94** between consecutive evaluations and the critic's explained variance spanning −6.6 to 0.98: a diffuse declaration head whose argmax flips, not a policy that learned to do nothing. ⚠ **Also void for code** — the checkpoints predate the audited network and cannot load on `main`, so the planned greedy-vs-sampled rescoring is not done. Ships Stage 0 of the curriculum: rounds per update on every row, the passive fingerprint on `EvalResult`, sampled play as a diagnostic, resume and warm start, and the pipeline-health panel with its overfit-one-rollout test. |
| **[2026-09-06 — House-fidelity distillation carries the decoded policy](2026-09-06-the-clone-carries-the-decoded-policy.md)** | Can imitation learning carry a policy whose strength comes from play-time decoding, or does a per-model fit lose the joint behaviour? | **IT CARRIES IT — the pre-registered prediction is REFUTED.** 1200 demos × 60 epochs, teacher = the agent played with K=3 + reallocation + charge decode: fit reaches **action-match 0.973, unit-match 0.933**, and at n=180 the clone is **not worse than its teacher on any cell** (gains +6.87 / +2.67 / +3.93 / +3.27, 4/4 positive but **t < 1** and seed 3 negative throughout — direction, not effect). ⚠ **§46's "the clone falls below the plain teacher by 3.9" is a POWER ARTEFACT**: it used 120 demos × 8 epochs, 10× fewer demonstrations and 7.5× fewer epochs than house fidelity. **The policy-improvement loop — decode to improve, distil to project back — is viable here**; the step believed blocked is not. ⚠ Also records a comparison avoided: against the bar the clone reads +12.78 / +12.48 / +12.83 / +7.71 against a **six-seed** agent row, which is not like-for-like — **always difference against the same seed set.** Fixed a defect first: the clone was distilling teachers that declare charges they cannot execute. |
| **[2026-09-05 — Pre-registration: 1000 epochs](2026-09-05-thousand-epoch-preregistration.md)** | Does training the anchored arm three times longer win the mirror matchup? | ⚠ **CELL VERDICTS RETRACTED 2026-09-06 (n=45, void).** As measured it read "the mirror is WON, `vs_shoot` given back" — a trade-off between two matchups. Re-measured at n=180 with the comparator measured too, **neither resolves**. The registered FAIL bound (`vs_shoot` below +56.6) did fire. |
| **[2026-09-05 — Pre-registration: weight averaging](2026-09-05-weight-average-preregistration.md)** | Does averaging two arms' weights land between them on both matchups at once? | **FAILED — averaging is confined to the straight line between the two arms, and "between" won neither.** ⚠ Cell verdicts retracted 2026-09-06 (n=45). |
| **[2026-09-05 — Pre-registration: extrapolation](2026-09-05-extrapolation-preregistration.md)** | If a small step in some direction helped, does a bigger step help more? | **FAILED — the direction is only locally good.** ⚠ Cell verdicts retracted 2026-09-06 (n=45). |
| **[2026-09-05 — Pre-registration: a 2:1 opponent-pool floor](2026-09-05-weighted-floor-preregistration.md)** | Would a differently-weighted fixed-opponent mix win both traded matchups at once? | **NOT RUN.** Conditional on the 1000-epoch arm failing the mirror; superseded when the measurement correction showed no cell resolves and the goal was cleared. Its own stated prior was "PARTIAL at best". |
| **[2026-09-04 — Pre-registration: the KL anchor](2026-09-04-kl-anchor-preregistration.md)** | Can holding a policy near its starting weights stop training from spending skill it already has? | **Mechanism confirmed** (drift held at target; see *The anchor holds*). ⚠ Its cell verdicts are n=45 and retracted 2026-09-06. |
| **[2026-09-04 — Pre-registration: a two-anchor pool](2026-09-04-two-anchor-preregistration.md)** | Does adding a second fixed opponent to the self-play mix change which matchups the agent wins? | **Yes, as a trade** — it won one matchup and gave back another. ⚠ Cell verdicts n=45, retracted 2026-09-06. |
| **[2026-09-06 — Stock-take: the melee-ladder goal, before the re-work](2026-09-06-stock-take-the-melee-ladder-goal.md)** | What did the melee-ladder goal achieve, what survives the measurement correction, and what should a re-work carry? | **GOAL NOT MET — the agent is slightly ahead on three matchups and level on the fourth, none resolved** (gaps +4.21 / +6.73 / +7.45 / **−0.19**, t = −0.03 to 1.12 at n=180). ⚠ **Every agent-versus-bar number produced before 2026-09-06 was measured with a broken instrument**: the comparator was treated as a constant (its `vs_deny` value moved **+11.8 → +36.8** on remeasurement) and the across-seed SE omits scenario noise. Per-scenario sd is 81–89, so **every arm difference ever measured on this ladder is inside the n=45 error bar** — a +81 effect survives, a +5–15 effect does not, and that is the size of nearly every lever this project has tuned. **Survives:** the decode stack (~+81), the reallocation decode (**+9.57 ± 2.55, 6/6** at n=180), the decode-headroom account of why training gains do not show up where they are scored, and that **self-play teaches charging from scratch** (cold run: 7.6–9.4 charges/ep against the bar's 5.8–6.3). **Shipped:** the KL anchor, the multi-anchor pool, `measure-melee-ladder`, and three defect fixes. **Retracted: five, three of them the author's own pre-registered predictions.** Twelve carried-forward lessons; four things the re-work should not carry. |
| **[2026-09-06 — `min_stack` is not confirmed: the tuning band picked the wrong cells](2026-09-06-the-tuning-band-picked-the-wrong-cells.md)** | A held-out sweep said lowering the reallocation decode's `min_stack` 4 → 2 is worth +5.2 on `vs_shoot`. Does it confirm on the evaluation band? | **NOT CONFIRMED — the registered primary is a null.** n=180, six seeds, paired per scenario: `vs_shoot` **+0.51 (t=0.45)** against the criterion's t>2. ⚠ **The tuning transferred in neither magnitude nor location**: the band predicted **+5.2, 3/3, t≈2.9** on `vs_shoot` and **−0.93** on `refereed`; the evaluation band gives **+0.51** and **+1.51 (t=2.40)** respectively. Tuned-and-adopted in one step, a false positive would have entered the record as a win on the goal's blocking cell. **The held-out-tune-then-confirm protocol is the only reason it was caught** — but the tuning band was itself under-powered (n=90, three seeds, SE ≈ 9.4 per seed): ⚠ **a tuning sweep needs the same n discipline as the arm it feeds.** Even with `min_stack=2` applied anyway the ladder resolves nothing (gaps +5.72 / +7.19 / +9.98 / **+0.32**, t = 0.05–1.48). |
| **[2026-09-06 — The ladder was measurement noise](2026-09-06-the-ladder-was-measurement-noise.md)** | Does the melee-ladder agent beat `squad_march_take_charge` on all four cells at six seeds? | ⚠ **RETRACTED SAME DAY — the n=45 result does not survive n=180, and NO cell is resolved.** At n=45 all four cells read WON (2.82–8.11 SE); at n=180 with both sides measured on the same scenarios the gaps are `refereed` **+4.21** (t=0.65), `vs_take` +6.73 (t=1.08), `vs_deny` +7.45 (t=1.12), `vs_shoot` **−0.19** (t=−0.03). **`vs_shoot`, reported as a +15.6 win, is dead level.** Two independent errors, both inflating: (i) **the comparator was a single noisy estimate treated as a constant** — the bar's `vs_deny` moved **+11.8 → +36.8** on remeasurement, so every ladder row ever published here carried an unpropagated **±12.7**; (ii) the **across-seed SE (n=6) omits scenario noise**, which is common-mode because every seed is scored on the *same* scenarios. ⚠ The pre-registered prediction ("`vs_shoot` clears at t≈2.6") was **wrong**: raising n moved the *mean* by −15.9, not just the interval. ⚠ Sign counts do **not** discriminate at this noise level (22/45 and 25/45 against 24.4 and 25.9 *expected under a true effect of the measured size*). **What survives:** the reallocation decode, **+9.57 ± 2.55, 6/6 seeds** paired at n=180 — the only claim of the day to survive its own remeasurement — and that **seed variance was never the binding constraint; episodes were.** |
| **[2026-09-06 — Three decode knobs, and none of them pays](2026-09-06-three-decode-knobs-and-none-of-them-pays.md)** | The ladder needs ~+10 vp on two cells. Can the decode stack's unmeasured knobs supply it? | **NO — the decode stack is at its ceiling.** Pre-registered grid, n=180, six seeds, paired per scenario, adopt-rule committed in advance (t>2 and ≥5/6 averaged over four cells): `decode_stay` **+0.52 ± 0.31, t=1.69** (not adopted); **iterated reallocation REJECTED at −1.28 ± 0.76**, and significantly harmful on `vs_deny` (**−4.33, t=−3.17, 1/6**; −3.99 and **0/6** with both knobs). ⚠ **The author's own pre-registered prediction for iteration ("+3 to +8, the larger of the two") was wrong in sign** — the code was written, registered, and lost; a held-out tuning band agreed independently. The only mover is `min_stack` 4 → 2, worth **+5.2 on `vs_shoot`, 3/3** on the held-out band and **~0 on `refereed`** — against the +13 and +8 needed. Defaults verified **bit-identical** (+79.4, coherency 0.952, decl 11.13/tried 9.40/stood 6.82 either side). Closing this gap needs a better policy, not a better decode. |
| **[2026-09-05 — Where the opponent turn goes](2026-09-05-where-the-opponent-turn-goes.md)** | Where does the 42.9% of `env.step` that the scripted opponent's turn costs actually go? | **Profile only — no verdict, nothing optimised.** `step_toward_objective` is **70.8% of `select_movement`** (14.7 scalar polygon queries per movement phase at ~101 µs each) against every member of a squad testing the *same* objective. ⚠ **`best_action_toward` was predicted to be the hot spot and is NOT — 2.1%**, despite allocating a constant `np.linspace` per call and repeating 75.6% of its argument tuples. ⚠ The opponent pays **0.777 ms** to the shared shooting resolver against the player's **0.248** on the same call count — a 3.1× asymmetry that is unexplained and probably a workload artefact of profiling against random play. ⚠ **Ceiling is ~12–15% of epoch wall-clock, not 2×.** CPU-only box. Work tracked in issue #270. |
| **[2026-08-11 — Handover: the state of play](2026-08-11-handover-state-of-play.md)** | *Not an experiment report* — a session handover, moved unedited from `.planning/` on 2026-09-06 when that directory was retired. | **Read the corrections first.** Superseded three times in place (`SUPERSEDED`, `KILLED`, `RETRACTED`) plus a `CORRECTION` after the seven-pass audit and a third correction below it; several sections retract the section above them. Its git facts are stale — branches and open-PR triage from 2026-08-11. |
| **[2026-09-04 — The anchor holds](2026-09-04-the-anchor-holds.md)** | Does holding a clone near its start let self-play improve it instead of destroying it? | ⚠ **CELL VERDICTS RETRACTED 2026-09-06 — n=45, void; the mechanism stands.** **Screen PASSES; goal NOT met.** Unanchored self-play from the clone ends at **−27.17** against those clones' −9.07 — **self-play alone is not the fix, it is the control.** With a KL anchor: **+29.63 ± 1.74, t=17.0, 3/3** over that control, and the pre-registered mechanism confirms — headroom **+73.47** against the clone's +74.87 (control +49.9, plain PPO +40.23), drift 0.039 v 1.770. **SIX-SEED VERDICT: 2 of 4 cells WON, 0 LOST — goal NOT MET** (`vs_shoot` ties at −0.15 SE). Best policy on file: it doubles the clone's won cells and loses none, moving the head-to-head from LOST to ahead. Headroom **+78.43**, above the clone's. The projection committed before seeds 4-6 landed ("2 WON / 1 ahead / 1 tie") was exactly right. ⚠ Arm 2 (`require_coherent: false`) **REJECTED**, and it corrects the morning's corollary: freecoh's DECODED coherency is only 0.906–0.921, so **a decode substitutes for a skill's EXECUTION, not for the training pressure that makes it representable**. |
| **[2026-09-04 — A tenth of the step](2026-09-04-a-tenth-of-the-step.md)** | If PPO travels too far from a decodable policy, does travelling a tenth as far keep the gain? | ⚠ **CELL VERDICTS RETRACTED 2026-09-06 — n=45, void; the mechanism stands.** **Yes, and the goal is still NOT MET.** Interpolating the §46 clone toward its own §47 PPO endpoint at **α=0.1** gains **+8.65 vp paired, t=3.15, 6/6 seeds** on the refereed cell, for no GPU training. Dose-response over a grid fixed in advance: 0.1 helps, 0.25 helps less, 0.5 **hurts**. Four cells: `vs_deny` **WON** (+16.22, 3.3 SE), the other three **ties** — but the clone's own reading is 1 won / 1 ahead / 1 tie / **1 LOST**, so this is the first policy here with **no losing cell**, and the cell it repaired is the head-to-head. It paid for that with `vs_take`. ⚠ A random displacement of the same size gives **+2.91** (ns); the difference is +5.74 at t=2.10 — resolvable only at 18 draws, not 6. Mechanism confirmed: headroom **+74.87 → +65.40** (full PPO +40.23) at 0.0155 nats of drift. |
| **[2026-09-04 — PPO did not fail; it spent the decode's headroom](2026-09-04-ppo-spends-the-decodes-headroom.md)** | Why does PPO destroy a behaviour clone it was placed in, with a warm critic? melee-teaching-goal §48 records this as "the whole question". | **Answered, no GPU.** PPO rolls out at `K=1` undecoded and is scored at `K=3` + charge decode. In its OWN regime it improved the clone by **+19.72 vp, 6/6 seeds, t=8.2**; in the scored regime it lost **14.92**. **Decode headroom fell +74.87 → +40.23** — it bought 19.7 vp of unaided skill and spent 34.6 of headroom. Both policies reach 0.96 coherency once decoded, so what it learned the decode already supplied. **Headroom decays over training on 6/6 seeds** (30 points; corr with the scored value **+0.638**), two thirds of it gone by epoch 100. ⚠ A pre-registered prediction that the CHARGE decode was the lever was **falsified** (+18.22 against a ≤+5.0 bound); the joint coherent decode is, and that one cannot be moved into training. ⚠ Also: **training is NOT bit-reproducible** — identical code/seed/config gave 0/222 identical tensors on the melee config — so the record's "never retrain a control" is unsafe and a pre-registered no-op digest was impossible. |
| **[2026-09-01 — Melee self-play runs end to end, and the control refused to charge](2026-09-01-melee-self-play-runs-end-to-end.md)** | Does the whole stack — melee, the command-phase declaration, self-play, the in-run ratings — work together? | **Mechanism PASS, no verdict — one seed.** Pool filled to 8 and ended spanning epochs 0-275; snapshots landed in the run's own directory. **The two ratings disagreed as designed**: the arm's ladder read **+146.3** while its rating against the script read **−185.8** — read `self_play/learner_elo` as "the agent's Elo" and you conclude the opposite of the truth. ⚠ **The control essentially never charged** (0.20 decl/ep against the arm's 14.50) despite facing the same charging opponent, **and scored better for it** (−29.5 v −57.0) — one seed at n=10, so a hypothesis with two readings, the repo's existing evidence favouring "charging does not pay at this horizon". Also records a **trajectory that does not exist**: two mid-run censuses were on the training config and the final one on the refereed eval config, so −127 → −5.0 → −57.0 is three numbers on two scales. |
| **[2026-08-24 — The advance lever: 300 epochs said free, 1000 says −16.3](2026-08-24-carrying-the-lever-is-free-using-it-is-not.md)** | Does the agent pay for merely *having* the advance lever? | ⚠ **UNRESOLVED, and the report's own headline is RETRACTED inside it.** At 300 epochs, paired **+2.2 ± 6.5** with flipping signs — read as "free". Resumed to 1000 and rescored: **−16.3 ± 8.9, t=−1.84, all three seeds negative**, with **s1 and s3 both flipping sign**. Verdict against a criterion committed to git *before* the scores existed: **UNDERPOWERED** (lower bound −42.2) — reported as such, not as a pass. ⚠ **"Two of three seeds learned to decline it" is retracted**: at 1000 only s1 is near zero (0.3%), s2 and s3 near 5% — **more training made them worse at ignoring the option**, and the prediction that s2 would converge FAILED (a second mode, not under-training). What survives: the usage↔score link (0.3% → −6.9; ~5% → −8.0/−34.0) and the forbid-at-play falsifier (**+26.3** recovered). ⚠ **A three-seed screen was read as a result twice and reversed both times** — six seeds needed, and the 300-epoch accept criterion was separately shown unpassable (a free lever fails it 56% of the time). |
| **[2026-08-23 — Three prices for the Advance move, and none of them is low enough](2026-08-23-three-prices-for-the-advance-move.md)** | The advance arm lost 26.7 vp. Three encoding defects were nominated — is any of them what binds? | **None of them, and the move itself is the problem at this horizon.** A new census (`just measure-advance-use`) eliminated all four candidates at convergence: dominated advances **0.4–5.9%**, unanimous unit triggers **64–81%**, waste **1.8–4.0%**, and within-unit distance spread identical on *walking* turns of the same policy. Three scripted rules were then priced against plain `squad_march_take`, each one narrower: −78 (2×2), **−18.4 (0/3)**, **−11.9 (0/3)**. The proposed mechanism was refuted — advancing moves end inside enemy reach on 4.1% of model-moves against walking's 22.4% — and the measured cost is whole-episode: exposure +10.8%, firepower 1.091 → 1.004. **Pre-registered and confirmed: the value is monotone in the round count** (rounds 5 → −1.7, 3/3 to advancing; 10 → +1.3; 20 → +11.9, 0/3), and the five-round game is not degenerate. **Advance is a short-game move.** |
| **[2026-08-22 — The agent is never paid to attack — and paying it does not help](2026-08-22-the-agent-is-never-paid-to-attack.md)** | `closest_objective_v2`'s candidate gate imagines exactly ONE model arriving, so ground the opponent holds by 2+ can never be a travel target. Is that the offence deficit? | **Mechanically yes, causally no — REJECTED.** Measured before writing code: the gate excluded **43.4%** of non-candidate objectives, left **68% of units** unassigned and dropped **63.1% of model-steps** through `fallback_to_nearest`. `contest_deficit: 5` fixed that exactly (exclusion 43.4% → **3.9%**, units with their own objective 32.0% → **48.1%**) and bought nothing: three seeds, 300 epochs, refereed at K=3, **paired** — **−2.7 ± 4.8, t=−0.55, 1/3 seeds**; across tables −2.7 ± 3.7, **ahead on 2 of 9**. ⚠ **It failed on the ACCEPT criterion, which was OFFENCE**: −61.2 → **−71.5**, backwards on 2/3 seeds. ⚠ **`alive` fell 3/3 with `held` flat — the REVERSE of hoarding, which the original reject rule would have missed**; the symmetric clause was added at epoch 290 before any score existed, after adversarial review. The mechanism is the 2026-08-11 **teleport audit reproduced by gradient** (−1.69 of 5 models, −29.41 of its own income against 4.91 defenders): paying a policy to walk at defended ruins gets it shot crossing open ground, so **the one-model gate was load-bearing**, exactly as the overstack penalty was. **Third consecutive reward term to leave offence flat or worse (−50.5, −42, −71.5)** — the supported diagnosis is a **difference-reward** problem: `vp_gain` is net but **global**, so no model can prefer taking theirs to standing on ours. |
| **[2026-08-22 — The horde is the agent's best matchup ever measured](2026-08-22-the-horde-is-the-agents-best-matchup.md)** | Does a *trained* agent exploit the side-dependence the scripted screen found in the asymmetric scenario? | **Yes, by the largest margin recorded here — and for the reason already on file, not a new one.** Three seeds, 300 epochs, scored refereed at K=3 on the held-out nine: agent **+16.2** against the best script's **−32.3**, a gap of **+48.5**. Two independent estimators agree — across seeds +48.5 ± 6.7 (t=7.26, df=2) and across tables +48.6 ± 9.2 (t=5.30, df=8), **ahead on 9 of 9 tables** (9/9, 8/9, 8/9 per seed). ⚠ **UNPAIRED** — 30 models and `max_groups` 6 share no initialisation with any other lineage. ⚠ **This is not the agent getting better.** Offence is **negative on 3/3 seeds** (−11.4 / −43.9 / −31.1) and defence carries the whole gap (+73.3 / +84.4 / +74.4) — the identical decomposition as the mirror, where the gap tracked *what the best script concedes* at r=+0.991. The elite opponent concedes **187.4** to a script and **103–114** to the agent, so ~80 vp of denial is on the table and the agent collects it; against `advance_and_shoot`, where both sides concede ~130, the same trait was worth **−75.9**. The one new observation: **`held` inverted** — the agent holds **more** ground than the scripts here (1.38–1.72 v 0.60–1.00) while keeping 2.9x as much army alive (0.287–0.374 v 0.077–0.125). The hoarding did not stop; it stopped costing ground, because control is a **headcount** and 30 survivors outnumber 15 elites wherever they arrive. ⚠ So the horde side **masks** the offence deficit and is a poor place to study it. ⚠ **The referee tax here is enormous and REORDERS the bar**: `take` −6.9 → **−46.4**, `contest_and_spread` −47.8 → −90.7; unrefereed `take` leads, refereed `deny` leads. Every screen number is void as a bar — "measure what ships" applied to a scenario, not a config field. **Untested: the elite side**, which the denial-price account predicts should win by less |
| **[2026-08-22 — Reach beats bodies, and closing time is the lever](2026-08-22-reach-beats-bodies.md)** | Can an asymmetric elite-v-horde scenario be balanced, and is it a real scenario or a foregone conclusion? | See the report — the balancing lever turned out to be **closing time, not firepower** |
| **[2026-08-22 — The overstack penalty was paying for itself](2026-08-22-the-overstack-penalty-was-paying-for-itself.md)** | `overstack_penalty_per_extra` has negative net income in the ledger — is it a broken term? | **No: removing it costs −12.2 ± 5.5 paired, t=−2.23, 3/3 seeds negative**, `held` 2.19 → 2.05. The term *is* the whole of `closest_objective_v2`'s negative net income (progress +0.08, penalty −0.90) and removing it flips that term to +0.29 with every other calculator bit-identical — the mechanism was exactly as diagnosed, and it **still lost**. Offence +2.9, defence **−15.1**: the travel term did pay more for movement, and fifteen VP were conceded for it. ⚠ **A term with negative net income is not thereby a broken term.** What a term costs shows up in `measure-income-share`; **what it prevents does not** |
| **[2026-08-22 — Holding pays, and the agent stacks rather than hides](2026-08-22-holding-pays-and-the-agent-stacks.md)** | Is the agent playing badly, or is the reward telling it to hide? | **Neither — it allocates badly.** ⚠ **"Hiding is correct play" is REFUTED**: `just measure-hold-hazard` prices a hold at **+0.37 to +0.44** more per model-step with **negative** excess death hazard in **5 of 5 policies**, against a break-even of +3.4–6.0%. The agent spends **54.4% of model-steps on objectives against the scripts' 75.5%** and stacks **4.90 models on its top point where `squad_march_take` puts 2.73**, leaving 55.3% of points empty at a **+2.20 redistribution ceiling** — the largest recorded. **53.7% of its income is global** against the script's 25.8%. ⚠ **Do not reach for anti-stacking shaping** — `crowding_exponent: 1.0` is already on and the agent ignores it |
| **[2026-08-22 — Spare squads pose the allocation question; the agent still cannot answer it](2026-08-22-spare-squads-pose-the-question-the-agent-still-cannot-answer.md)** | Is the agent's offensive deficit caused by the scenario never posing an allocation problem? | **The scenario genuinely was not asking — and fixing it did not fix the agent.** Five squads against five or six objectives make `take` and `deny` differ by **+5.7 / −9.2 / +9.8** across three layout sets: it **changes sign**, mean +2.1, with 25–31 of 100 episodes identical. Eight squads of three do ask: **+16.0, positive on 3/3 sets**, 2–5 identical. ⚠ **Mixed weapon profiles are a measured null, and the first two arms measured their own lethality** — 45 shots a round against the control's 25 collapsed `alive` to 0.135; held at **exactly 25 shots** roles reproduce the control to one decimal. Trained on the config that does ask, three seeds: gap **+9.1, t≈1.44, UNPAIRED**, offence **−50.5**, defence +59.6 — still entirely denial. ⚠ **THE AGENT HOARDS**: **52.9% of its army alive against the scripts' 27.4–30.9% while holding fewer objectives.** **The VP cap taxes the SCRIPTS, not the agent** — `take` loses 10.1% of its VP to it, the agent 1.1%, so the shortfall is **fully payable** |
| **[2026-08-22 — Asymmetric armies: preregistration](2026-08-22-asymmetric-armies-preregistration.md)** | (Criteria written before any numbers) Is an elite-v-horde scenario worth GPU? | Accept/reject rules for degeneracy, one-sidedness and "no new question", fixed in advance because picking the worst-looking case and then measuring it has manufactured a retracted claim here before |
| **[2026-08-21 — The agent cannot tell which game it is in](2026-08-21-the-agent-cannot-tell-which-game-it-is-in.md)** | Is the `contest_and_spread` loss real, and does `25v25_maps_coherency` need retraining? | **The loss is real and is the mild end of a much larger one.** Six seeds replicated the four-opponent table within 2 vp (`deny` firmed to **9/9**, `shoot` still unsettled at t=1.64) and left `contest_and_spread` at **−9.5, t=−1.18, 4/9** — not settled, but no longer carried by one seed (five of six behind, seed sd 9.1). A **fifth opponent** exposed the real result: against `advance_and_shoot`, the weakest, the agent is **−75.9 behind the best script on 0 of 9 tables**, t=−7.12, the largest deficit ever measured here. **One trait explains all five rows.** Splitting each gap into offence and defence, offence is FLAT at −42 to −71 everywhere while defence falls +96.1 → +82.3 → +58.2 → +38.5 → **−4.5**, and the gap tracks what the best script concedes at **r = +0.991**. The agent holds **1.89–2.11** objectives against every opponent alike where scripts reach **2.93–3.86** against weak ones: its lead is worth whatever denial is worth, and against an opponent everyone holds to ~130 conceded, that is nothing. Underneath is the documented allocation gap — **0.00 models on the bottom three objectives in every seed and every matchup**, abandonment 0.645 v 0.653 across opponents (invariant), redistribution ceiling +1.5 to +2.5 objectives — with `objective_hold.crowding_exponent` **already at 1.0** and already recorded as not fixing it here. **`25v25_maps_coherency` retired as a training config**: comments stripped it is `two_mode` but for the opponent, so it is the same scenario against the matchup the agent is worst at; its agent column now comes from scoring the `two_mode` lineage on a new refereed eval config. Tooling: `measure-objective-split` gained `decode_topk` (it had been decoding argmax, describing a player that does not ship), `train-coherency-baseline` gained a seed range so a lineage extends rather than retrains, and `tests/test_map_config_pairs.py` pins the single-variable property the whole table rests on |
| **[2026-08-21 — The short game is sharper, and the agent's edge does not survive it](2026-08-21-the-short-game-is-sharper-and-the-agent-cannot-play-it.md)** | Does the tabletop's five battle rounds make results harder to read, and does longer weapon range open the game up? | **No and no, and the interesting result is neither.** Five rounds *improves* measurement: the do-nothing floor still sits **30 error bars** below the best script (33 at twenty), best-to-worst spread over its own error bar goes **~11 → ~16**, the ranking is identical, and paired sensitivity rises (t −0.56 → **−1.89**). The noise composition **inverts** — dice 75.3 v layout 43.5 at twenty rounds, **6.6 v 15.2** at five — because a long game lets one unlucky exchange compound, and the term that now dominates is the one every measurement already controls. Uniform weapon range is a **null**: 12/18/24 moves every policy under 1.5 vp and *narrows* the spread, changing only the casualty rate (`alive` 0.708 → 0.562) — raising a number on both sides leaves the mirror intact. What the work actually found: **the agent's entire edge is time-dependent.** Six seeds, K=3, held-out nine — **+27.8 clear of the best script at twenty rounds, −5.7 behind at five.** Split into offence and defence the decomposition closes to 0.1 vp at both horizons, and while the VP scale shrinks 5.3×, **offence shrinks 7.3× and defence shrinks 35×**: denial is banked per round, ground taken is a fixed fraction of what is on offer. ⚠ **Transfer result** — all six seeds trained at twenty rounds, so nothing here says how a five-round-trained policy would play. **Refuted: the offensive deficit is NOT a clock artefact** — `squad_march_take` holds **2.63 at both horizons** while the agent's `held` *falls* 2.05 → 1.83, and the premise that scripts "reach 4 objectives by walking the table" came from a weaker matchup (they reach 2.1–2.6 here). **Two of the pre-registered criteria were themselves bad** and are recorded as such: the tie-count test flunks the known-good scenario too and measures policy *similarity*, not degeneracy (`take` v `deny` ties 47/100 at five rounds, `take` v `shoot` 10/100) |
| **[2026-08-20 — The eval tables are generated now, and the tracing they replaced was right](2026-08-20-the-tables-are-generated-now.md)** | Should the public layout API be the source of truth for the evaluation tables? | **Partly: terrain and deployments yes, objectives no.** `just fetch-maps` generates all 45. They are the *same* 45 layouts — matched 45/45 by piece bounds, so the numbering and the held-out nine survive. Three undocumented conventions settled by measurement: the local frame is **centred** (720/720 pieces on-board with zero overhang, against 70 off-board by up to 3.75" under the corner reading); simplification is **mandatory** (source outlines are **167–348 vertices** against a budget of 8, which is *why* the traced tables were quads); and **Douglas-Peucker** wins on numbers (worst-case area 1.078 v the convex hull's 1.160 and the footprint rectangle's **1.592** tail). ⚠ **RETRACTED: this row used to say the traced objectives "were not the layout's".** They were — checked against the published layout cards they are right on **45 of 45, worst error 1.5"**, better than anything derived from the API, whose own markers are wrong on **6 of 45 by 12–18"**. The error was assuming the API was authoritative. Objectives now come from the validated positions and are pinned in `tests/test_map_objective_counts.py`. **An objective is a RUIN, not a terrain piece** — the layouts build one structure from several kit pieces, so resolving a marker to the nearest *piece* left **30 of 225 objectives covering half a ruin**, caught by eye on a rendered table after every count, split and resolution check had passed. Pieces are grouped by **shared boundary ≥ 1.0"**, a threshold sitting in empty space (nothing falls between 0.33" and 1.45"); a position takes the **largest ruin within 4.5"** and **a tie designates both**, which is what makes a table carry six. Counts land at **24 fives and 21 sixes** as published, zone split exactly **82/82/82**. **The tables also bring their own DEPLOYMENT ZONES** — only one of the six real deployments is an axis-aligned band; the rest are triangles, staircases and arcs, and here the API **is** trustworthy (tinted region ≥98% inside its polygon on 45/45). ⚠ **`long_edges` puts the armies 20" apart across the short axis** against 24–40 elsewhere, at a 12" weapon range — a fifth of the pool. Observation width unchanged (budgets stay 6/16), so checkpoints still load and arms still pair. **Measured on all 45 tables, paired, against the tables AS SHIPPED: they are NOT harder.** `held` does not fall for any script (+0.03, +0.01, +0.09), `deny` gains **+12.8** (t=3.87, 31/45) and `take` **+7.5** (t=2.06, 27/45), the bar `squad_march_shoot` is a flat null, and only `random` loses (−3.7). ⚠ **An earlier read of this same comparison concluded the opposite** ("harder to HOLD", `random` −18.9) — it ran the new terrain under the OLD rectangular deployment, a board nobody plays; and its `on_obj` row was actually the `alive` column. **The new bar, all 45 at n=30:** `random` −222.5 · `take` **+5.9** · `shoot` −5.9 · `deny` **+5.4**. **The pool's resolution floor is ~6 vp** (per-table sd 18.5–20.6, SE 2.75–3.07), better than the traced tables' ~8 because the zones remove across-table spread; more episodes cannot help, since the variance is *across tables* and only 45 exist. Quote a t **and** a sign count here — the per-table differences are heavy-tailed and the two disagree |
| **[2026-08-20 — Joint constrained decoding does not belong in training](2026-08-20-decoding-does-not-belong-in-training.md)** | The decode is worth +40.5 vp at play time — should it be folded into training? | **No, and the implementation was correct — the idea is wrong.** From scratch it costs **−51.8 vp** paired (control +33.8, joint −18.0) and buys *less* coherency; warm-started onto a trained policy it costs **−43.7** and **destroys its own initialisation by 37.9 vp**, best checkpoint at epoch 0. The fresh-optimiser confound is ruled out — the control got the same cold optimiser and *improved* by 5.8. **Annealing K is impossible by arithmetic**: `K**k` against a 4096 cap means **K ≤ 5** on a five-model unit, where an untrained policy has only **8.9%** of its mass in the candidate set (K=20 would need 3.2M combos, 780x over). Top-K starvation is real from scratch (top-3 mass **0.071** untrained v **0.895** trained) but is NOT the main cause, since the warm start fails nearly as badly at 89.5%. What fits both arms is the **speed tax** — constraining every sampled move to a legal combination forces slow clumped play, which earns less, and the policy learns that. Same destination as *enforcement is a referee*, different route: **the constraint substitutes for the skill**. PPO correctness is NOT at fault — the ratio gate passes and is verified sensitive (a wrong stored density gives ratio **78,143**), and the unit-level clip has 5x margin (1.037 v the recorded 25-model failure at 1.2002). Three test defects were found by sensitivity-checking rather than running: a padding bug that would double a combination's probability mass, an insensitive padding test, and tests passing vacuously because randomly-placed models start incoherent. **Nothing was shipped** — the flag and wiring were reverted rather than left as config surface inviting a re-run |
| **[2026-08-19 — The two seats are not the same game](2026-08-19-the-two-seats-are-not-the-same-game.md)** | Are the two seats equivalent, so that a rating measures the policy rather than the seat? | **No, by 24.6 vp on the reference scenario.** One policy played from both seats over the balanced four legs (zone x first mover) loses from the **player** seat by **-24.6 +/- 9.4** on `25v25_shooting_opponent.yaml` -- larger than most effects measured here, and every number in this repo is quoted from that seat. Ruled out by measurement: movement and placement (a non-shooting policy is **fair** on the same config, +7.2 +/- 9.1), terrain/sight/cover (deleting terrain makes it **worse**, ~-40 on all four legs), the known LOS-asymmetry bug (real but **unbiased** -- 29 v 23 of 18 750 pairs), and policy identity (`contest_and_spread` loses on all four legs too). **Part of it is a genuine second-mover advantage**: the clock is IGOUGO, so the first mover shoots at a gap one enemy move wider, worth 1.69 v 4.25 shots in round 3 and reversing to 6.19 v 5.25 when turn order is flipped. Notably the first-mover advantage **changes sign** with shooting -- movement-only it is +59.2 Elo. **After averaging turn order out, the residual is not combat**: the player fires more (+3.07), kills more (+2.15) and survives more (+0.09 alive) while scoring **-9.38 VP** -- it out-fights and under-scores, holding 1.00 objectives against 1.67. **The residual is positional, and the scoring cadence was proposed and then REFUTED.** VP is awarded when a side leaves its own command phase, and the two scoring instants sit half a turn apart in the interleaving -- which looked like the cause. The control at n=30: sampled at a **neutral** cadence identical for both sides, the player holds **1.154** objectives and the opponent **1.486** (gap **-0.331**); sampled at each side's own scoring instant, 1.072 v 1.343 (gap -0.272). **The neutral gap is the LARGER**, so the scoring rule makes the seat gap smaller, not bigger -- the board favours the opponent at every instant and the cadence is innocent. The earlier 'the arithmetic closes' was an accounting identity that could not have failed. ⚠ the 'neutral' cadence is **not neutral** -- sampled at step boundaries it always lands just after the opponent's turn, so it is biased the same way as the effect and -0.331 is an upper bound. **What stands is sharper: the player wins the race and loses the ground.** Tracking both armies per round, the player is ahead on objective occupancy every round to 8 (0.899 v 0.858, arriving first and with more models on), then the curves cross and diverge -- player decays to **0.587** while the opponent climbs to **0.975**. By round 10 both armies are within 0.05 of an objective, so it is not approach or travel: it is which side's SURVIVORS stay inside the radius once the armies are in contact. Also ruled out: objective placement is exactly symmetric (mean x = 30.00 between zones [0,20] and [40,60]), and the shoot-vs-advance idea is wrong by inspection since `select_action` dispatches by phase and shooting costs no movement. Incidental: `advanced_this_turn` is **never set true anywhere**, so the rule gating a moved model's fire is inert. n=8 on one config, so confirm at higher n before changing anything. **Replicated across the sight fix #211**: -24.6 -> -24.7, unchanged to a tenth of a vp on two code versions, which also confirms independently that the (real, now fixed) sight asymmetry was unbiased between the seats. Rating that scenario is blocked; `just measure-seat-parity` is the gate |
| **[2026-08-18 — The chain tail and the frozen army](2026-08-18-the-chain-tail-and-the-frozen-army.md)** | Why does coherency-enforced play *look* wrong on screen, and what makes a coherency-legal game possible? | **The complaint was correct, the cause is aggregation not policy, and the fix is the DECODE.** Under `revert_unit` **33% of unit-moves are cancelled and 49% of all intended movement inches destroyed**, 91.5% of unit-episodes freeze at least once, and freezing is an **absorbing state** (P(frozen next|frozen now)=0.62; `squad_march_take` deadlocks for all 20 rounds of one seed). The mechanism: the **2" chain binds, not the 9" spread** — 7.8% of models sit beyond it, and `1−0.922⁵ = 0.32` against a measured 0.331, so an all-or-nothing revert turns a 7.8% per-model tail into a **33% unit veto**. Legality is a property of the *combination*, so a per-model policy is punished by `p^k` arithmetic rather than by judgement. **Joint constrained decoding** — take each model's top-3 moves, enumerate the 243 combinations, pick the most probable LEGAL one — is worth **+40.5 vp on 45 of 45 tables and lifts coherency 0.639 → 0.936**, on three seeds, **changing no weights at all** (positive on every table measured, coherency +0.309 on every table). ⚠ **§21's claim that the benefit was unevenly spread across maps is RETRACTED** — it was n=10 on a table selected for looking bad, and its median was read against a mean. `enforce_move: repair` (pull strays onto the body instead of cancelling) puts the agent **ahead of every script** but is a scenario change that moves everyone and a documented divergence from the rules. **Baseline reached, three seeds, held out, under the spec's own rule: coherency 0.847 (scripted band 0.772–0.891) at −8.0 vp against the best script's −4.4** — level, from thirty behind. **Every training lever failed by contrast**: a 2×2 over `observe_unit_centroid` × `ent_coef` left the tax unmoved, and **replicated to three seeds the single-seed ranking INVERTS** (`ctl` 0.790 → **0.674 ± 0.104**, `ctlE` 0.703 → **0.771 ± 0.060**), with `observe_unit_centroid` the null at −62.1. **⚠ The seed spread — 26.0 vp, 0.202 coherency — dwarfs every lever**, and **six of my own claims were retracted**, from n=4–6 interims and from quoting seed 1 as a baseline. Durable descriptive finding: the agent **stands still on 0.4% of unit-moves against the scripts' 38–57%**, so 98.8% of its cancelled moves were moves it wanted where a script's are mostly stays it was making anyway — which is why coherency *rate* does not predict the tax. **The entropy explanation for that is refuted**: `ent_coef` 0.003 concentrated the policy exactly as predicted (entropy 3.545→1.893) and made STAY **30× rarer**. Also refuted without GPU: **unit-level action spaces** (`squad_march_take` is one and reaches only 0.915) and **smaller units** (p³ gain cancelled by a worse per-model tail; the apparent win was the casualty confound). Landed: **`attrition: true` in the play config** (+15 vp), the **attrition kill-credit leak fixed**, `measure-objective-split` fixed for map-pool configs, and a nine-table held-out map directory |
| **[2026-08-18 — The lead was rule-breaking](2026-08-18-the-lead-was-rule-breaking.md)** | Was the *opponent* the ceiling, and can PPO beat the strongest scripted policy on unseen tables? | **The opponent was most of the ceiling, and the agent's win does not survive the rules.** `scripted_baseline` (new) plays any baseline on the opponent side; swapping `scripted_advance_and_shoot` for `squad_march_take` drops the bar from **+104.9 to −5.6** and beats every one of the seven scripted policies, where six of seven beat the old opponent — so headroom exists again. Trained two seeds to 1000 epochs: unrefereed, **s2 beats the strongest script by +17.0 ± 6.1** (t=2.79, ahead 8/9 held-out tables), which would be a first here. **⚠ It is rule-breaking.** `coherent` 0.79 means one unit-movement phase in five is illegal; re-scored under `enforce_move: revert_unit` the ranking **reverses** — s2 **−10.0 ± 2.6** (t=−3.83, ahead 1/9) and the rule costs the agent **22–31 vp against the script's 4.3**, because `squad_march_take` moves whole units on one shared vector while a per-model policy loses a whole unit's move to one stray model. The agent's route *is* the taxed behaviour: it stands on far less ground (`on_obj` 0.75–0.80 v 0.98) and wins anyway on **firepower 1.9–2.0 v 1.1** and survivors — which needs spreading out, which the rule forbids. What survives: both seeds still beat the old bar, s2 on 9/9. **Every score now carries `coherent`/`adrift` unconditionally** — `random` is 0.008 coherent with 22.5 adrift and nothing else in its row said so. Also: `vp_gain` **is** net, so denial *is* paid — but only by a **global** term, while every per-model term pays **1.0** to hold our ground v **0.25** to contest theirs. **Two seeds only** (22.6 vp apart), so neither direction is settled |
| **[2026-08-16 — The VP cap makes this a denial game](2026-08-16-the-cap-makes-it-a-denial-game.md)** | Why does the agent trail `squad_march_shoot` on the real tables, and can a reward change close it? | **The mission caps own VP at three objectives** (`min(15, held * 5)`, and the cap is the rule), so own score saturates — 272–278 of a 285 ceiling for anything competent — and **all remaining margin is denial**. Four independently trained seeds concede **181.6 ± 0.6** opponent VP against the bar's **160.4**: 57% of the bar's denial, and the most reproducible number measured here. `squad_march_deny` (new baseline) **ties the bar while holding 3.00 objectives against its 4.00**, which is the mechanism proof — and shows **`held` stops ranking policies once the cap binds**, qualifying `docs/metrics.md`. **Raising the pay for contested ground is a null** (188.0 v 188.5 opp VP, two lineages, two controls each, measured at the plateau) — and the reason was already in the repo: a teleport audit found the advancing squad **loses 29.4 of its own income and 1.7 of 5 models**, dying against 4.91 opponents, so no rate pays a corpse. Steering is already control-aware, so all three config-level levers are closed: **the obstacle is coordination, not pricing**, and the agent sits in a good local optimum a gradient cannot leave. **`map_pool.mirror` measured and null** (+6.3 ± 10.4 and +1.8 ± 3.5, ahead on 4/9 maps, sign flips without one outlier). **The held-out split is confounded** — enriched 3x in the map type the agent is worst on (−51.5 v −10.6 vp), so the "generalisation gap" is partly a mission-mix difference. Full floor-to-bar ladder at n=100 with error bars and the player/opponent VP split |
| **[2026-08-16 — Enforcement is a referee, not a teacher](2026-08-16-enforcement-is-a-referee.md)** | Should coherency be taught, forced, or both — and which configuration ships? | **Train with the reward gate; never train under enforcement; switch enforcement on for play.** Measured with the referee **OFF** on the rules' own 2"/9", so the numbers describe the policy rather than the wrapper: trained under enforcement **0.569** coherent / **70.3** held-out vp; under enforcement plus the `coherency_intervention` penalty **0.783 / 68.0**; **`objective_hold.require_coherent` alone, never enforced, 0.756–0.886 / 81.5** (six seeds, nine held-out tables). Enforcement supplies **no gradient at all** — every reverted action produces the identical outcome, so they share an advantage and the policy gradient inside that set is exactly zero. **Supersedes and voids all three reports below**, for two independent reasons: every compliance figure in them was sampled *after* the enforcement fixed point (`coherency_rate` is 1.000 by construction there), and a geometry fix (#193) moved LOS on these maps by **22.6%**, voiding every eval-map number measured before it. **`coherency_intervention` refuted and removed**: it raises formation reliably and finishes **last** on unseen ground. Config surface for this rule cut from **ten knobs to five**; `configs/golden/25v25_maps_coherency.yaml` is the shipped configuration. **Open and unexplained:** gate-only outcomes are bimodal and the split tracks the **warm start**, not the training seed — eight runs, no overlap (0.657–0.704 v 0.853–0.903, ~3% by chance) — while both warm starts measure ~0.51 on the predicate themselves. The coherent lineage also scores **+11 vp** held-out, so formation and winning go together |
| **[2026-08-15 — Coherency: the verdict](2026-08-15-coherency-the-verdict.md)** ⚠ SUPERSEDED | Can an agent play in full unit coherency, and what does it cost? | **⚠ CORRECTED after adversarial review — the first verdict was wrong.** The reward gate `objective_hold.require_coherent` genuinely teaches formation: measured with the referee OFF, the policy *intends* **0.847** coherency (2.20 of 25 adrift) against the unconstrained control's ~0.55, with `held` 3.41 v 3.45 and `vp_margin` inside noise — free. But **hard enforcement is a shield, not learning**: the published "1.000 compliance" was measured *after* the enforcement fixed point, and the same weights with `enforce_move: off` intend **0.630 with 5.37 adrift** — inside the unconstrained range. Worse, **training under the constraint erodes what the gate taught** (0.847 → 0.630 intent over 300 epochs): every reverted action yields an identical outcome, so the policy gradient is zero inside that equivalence class and only entropy acts there — and stage 2's config drops the gate, removing the only term paying for legality. **Cost:** enforcement applies to BOTH sides (one `ActionHandler` path), so the −8.7 margin nets two handicaps — the agent loses **17.8** player VP, the scripted opponent **9.1**; `held` falls 3.45 → 3.01 on frozen weights, so most of the "policy" deficit is the rule. **Retracts** "sometimes free" (a max-of-7 at +1.47 sd where E[max]=1.354, and the very `save_top_k` training-reward checkpoint the report retracts elsewhere), the warm-start-basin claim (6 of 7 scores descend from ONE checkpoint), "two stages required" (both basins reach 1.000), and `alive`-monotonicity. **Defects found and fixed:** `require_coherent` was blind to the 9" spread clause (a chained 11.78" line paid 5 of 5); `eval/models_out_of_coherency` reported 0 for every spread-only breach; `clamp`'s fallback revert skipped the overlap cascade — invisible because every clamp test ran at `base_radius` 0.0 |
| **[2026-08-14 — Coherency: what it costs, and the one lever that moved it](2026-08-14-coherency-what-it-costs.md)** ⚠ SUPERSEDED | What does rules-accurate coherency actually cost, and can a reward buy it? | **Obeying the rule is cheap; training under it is not.** n=100 on identical layouts: imposing enforcement on trained weights costs **−8.7** (+98.6 → +89.9/+86.5), then training under it for ~100 epochs costs a further **−14.3/−15.3 on both seeds** (→ +75.6/+71.2) — so **~62% of the price is caused by training under the rule, not by obeying it**, via a reproducible trade of ground for survivors (`alive` up 0.963→0.984, `held` down 3.01→2.79). **`objective_hold.require_coherent` is the lever that worked**: a detached model earns *no* objective income (the user's argument — an illegal state should not be paid), lifting `coherency_rate` 0.55 → **0.708/0.849** with `held` **3.41 v 3.45** and `vp_margin` 2.6 down, inside noise — the first lever to move the metric through *learning* rather than construction, and it does not suppress objective play. Failures: the probability sweep has **no knee** (p=0.75 dominated by p=1.0); `clamp` (new mode — shortens a move along its own segment instead of cancelling it) plateaus at **−25** from scratch against a control at +90 by epoch 40; and **`gated_clamp` was ill-conceived** — under full enforcement the gate is *definitionally inert* (nothing is ever detached), and the claimed mechanism was refuted directly, reverts/step **6.79 v 6.34** ungated, because the gate reduces breach *severity* not *frequency*. **Retracts** today's "~28-30 vp" (n=6, SE ~19), "a coherent policy can win here" (used the bar's *unenforced* score), and "the difference reward tests the defection incentive" (it collapsed `objective_hold` income 0.176 → 0.014, deleting the term). Method: seed-set choice moved **three** conclusions — one deterministic policy scores +67.5 at seeds 10000+ and +93.5 at 700000+ |
| **[2026-08-14 — Coherency is a symptom: the adrift models are defecting](2026-08-14-coherency-is-a-symptom.md)** ⚠ SUPERSEDED | Why will the agent not hold unit coherency, and which mechanism fixes it? | **The formation problem is not a formation problem.** 1399 adrift-model observations: **82.4% have a different nearest objective from their unit's coherent body**, a median **13.6** board units away against a **9.0** spread cap — they are not drifting, they are *defecting*. `objective_hold`'s `crowding_exponent` splits a point's pot among occupants, so the marginal model's gradient points at the emptiest objective and nothing makes that spread respect unit boundaries. A per-model coherency reward is a counterweight to it: monotone in gradient strength (0.562 → 0.621 → 0.659 at matched epoch 175) with **`vp_margin` unchanged** — free, and not the lever; diminishing increments as the gradient doubles are the signature of a bonus fighting a stronger term. An observation-only arm is **null** (0.608 v 0.585), so the movement is the reward and not the inputs. **Enforcement dominates and the earlier refutation was wrong**: `revert_unit` from scratch scored −75.3 because a near-random policy has *every* move cancelled and correctly learns that moving does nothing — a credit-assignment pathology of training *into* the constraint. Warm-started from a competent policy, `revert_model` reaches `coherency_rate` **1.000** with **zero** models adrift inside **ten epochs**. Also: the defection does not even work (control is a strict count, so a lone model flips nothing) — the agent holds **fewer** objectives than the bar, 3.44 v 3.76, while keeping **96%** of its army alive against 72%. Next lever is **unit-atomic objective income**, not formation. Two method mistakes recorded: an arm comparison across *different epochs* inverted the first conclusion, and `coherency_rate` is confounded with squad size so the scripted 0.837 is measured at a different `alive` |
| **[2026-08-12 — Coherency was never deployed](2026-08-12-coherency-was-never-deployed.md)** | Implement unit coherency (`03-moving.md`) accurately — learned or hard-constrained. | **The mechanism debate was downstream of a placement bug.** Built the predicate (both conditions **plus connectivity**, base to base — two pairs 5" apart satisfy every *stated* condition and are still not one unit) and `just measure-coherency`, then measured before choosing: **nothing in this repo has ever been in coherency.** The bar is legal on **3.3%** of steps, the trained agent **2.2%**, and **deployment is coherent in 0 of 20 episodes for every policy** — placement anchors each model on one *random* squadmate, bounding the nearest neighbour and leaving unit span unbounded. `hold_deployment` never moves and scores **0.006**, isolating the cause completely. Deploying coherently (`03-moving.md` § Setting up, off by default) takes STAY to **0.992**, the marching baselines **0.25 → 0.80** and out-of-coherency models **8.13 → 1.29**, *with no enforcement during play* — three quarters of the breach was inherited from set-up. Measuring base to base rather than centre to centre is what keeps the constraint controllable: it widens the legal band from 0.74" to 2.0", against a 1.0" minimum move. Also found: **the cohesion observation counts corpses** while the cohesion reward masks them. End-of-move revert and attrition deliberately not built — and per the spec, attrition is a *backstop*: if it fires often, the revert is wrong. **Re-measured: legality is not a tax.** Deploying legally is worth **+20.9 vp to the bar** (+38.0 → **+58.9**, n=100 identical layouts) and ~+21 to every squad-manoeuvring policy, ~+6 to those that scatter; `alive` goes 0.450 → 0.592, so a squad that starts concentrated concentrates its fire. The ordering does *not* flip, unlike the one-distance precedent |
| **[2026-08-12 — The agent beats the opponent on the real tables](2026-08-12-the-agent-beats-the-opponent-on-the-real-tables.md)** | Train on the 45 real table layouts and beat the opponent there. | **Achieved on both seeds, on tables never trained on** — 0.99 / 0.93 win, **+84.5 / +79.3 vp_margin**, against `random` at 0.28 / −39.2 and a new `hold_deployment` (STAY) floor at **0.00 / −70.2**. The real tables are a *different mission*, not a harder board: the generator can only place objectives in the contested middle, while the real layouts split them **82 / 82 / 82** across player zone, middle and opponent zone, so a player **starts holding 1.98** — hence the STAY baseline, which loses every episode and settles the "is it won by deploying" question. It does **not** reach the bar (+105.7) on held-out ground, but on **training** tables it is level with it (+89.5 / +90.3 v +91.2) and holds *more* objectives (3.64 v 3.57): **the deficit is a generalisation gap, not a capability ceiling**, and 36 layouts is a small distribution. Plateaus at **epoch 140** — 850 further epochs moved ±3 vp. Needed two new pieces of machinery (`objective_budget`/`terrain_budget`, `map_pool`) and caught a bug that returned **nine byte-identical table rows** because `config_for_map` cleared `random_terrain` but not `map_pool` |
| **[2026-08-12 — The physics were off, and then the agent beat the bar](2026-08-12-the-physics-were-off-and-the-agent-beat-the-bar.md)** | Beat `scripted_advance_and_shoot` on the 25v25 shooting scenario. | **Achieved** — but first: `configs/golden/25v25_shooting_opponent.yaml` never set `base_radius`, so it took the old default of **0.0**, at which the whole of phase 03 is a documented no-op — no occlusion, **cover cannot occur at all**, no collision, centre-to-centre range. It was the pre-geometry scenario on the post-geometry engine. With 32mm bases, objectives as the largest unclustered middle-section ruins and terrain refitted to the 45 real layouts, the bar went +6.7 → +27.8 → **+38.0**. `objective_hold` 1.25→2.5 with `objective_coverage` 0.3→0.1 — same budget, global term to per-model — ⚠️ **carries a correction** — its second seed came back at **−0.9**, fifteen points from the first, so the headline +14.2 does not replicate. Pooled over four 1000-epoch runs the agent beats the bar by **+6.08 ± 3.09**, sign test **219/347 = 63.1%, z = 4.89**, and the income shift (+6.65) is not separable from the control (+5.50). Also **+8.8 ± 5.8 zero-shot** on the goal's own config with terrain it never trained on (59/90, p ≈ 0.003). **γ = 0.97 was the lead hypothesis and came last** (−8.8, retired at the 300 screen). Every baseline was marching its squads onto the objective *centroid*, because `radius_size` is 0.0 for an area by design. And **four claims died to proper pairing, all of them mine** — +8.0→+1.7, +10.2→+7.5, +7.6→+0.8, and a 300-epoch arm ranking that reversed |
| **[2026-08-11 — What a seven-pass audit found](2026-08-11-what-a-seven-pass-audit-found.md)** | Do the day's conclusions survive review? | **Four of them do not** — and the pattern matters more than any one: **every error was in how data was bucketed or paired, none was in the code under test.** The environment, reward and network were sound; the probes ran correctly and measured something other than what their author believed. The "transit trough" was a battle-phase artefact (walking pays **41% more** than loitering at fixed phase, not half); the local-optimum verdict read the **team scalar** while PPO trains on the per-model vector, where the same deviation *costs* the movers 29.4; +21.1 vp came from a `zip` that diverged after the first skipped episode; and a global reward term cannot create a preference between two things a model might do. Cost: one hour, no GPU — against ~7 GPU-hours per arm it would have prevented. Carries the outstanding items and the audit composition that worked |
| **[2026-08-11 — Spreading objectives lowers the bar, not the abandonment](2026-08-11-spreading-objectives-lowers-the-bar-not-the-abandonment.md)** | The agent abandons 38% of objectives. Is that reward weighting, or objectives packed into a 16" circle? | **Neither.** Abandonment across five weight configurations and two scenarios: 0.380, 0.370, 0.357, against the bar's 0.147. It is pushed off an objective **1.7%** of the time against the bar's 24.7% — it is not losing fights, it is not turning up. What *did* replicate: moving income from the **global** `objective_coverage` to the **per-model** `objective_hold` is worth **+9.8 vp** on both seeds, up from a weak +5.2 on the other scenario. Refuted: `overstack_penalty_per_extra` (zeroing it is null), and the prediction that spreading objectives would fix allocation — measured at 3.6 sigma as a natural experiment, it did not transfer to training. The deficit narrowed **-17.1 -> -9.9**, but by the **bar falling 8.8** while the agent stayed flat. Remaining hypothesis is representational: nothing encodes "an uncontested objective is worth a 20-inch walk", and the reward pays zero during the walk |
| **[2026-08-10 — Real terrain, and the abandonment gap](2026-08-10-real-terrain-and-the-abandonment-gap.md)** | First training on the continuous board with polygon terrain, model bases and cover. Does the agent still beat the shooting bar? | **No — +50.2 / +51.3 against the bar's +67.7**, two seeds within 1.1 vp, converged by epoch 680. The gap is entirely *conceded* VP: player VP is a dead heat, opponent VP is 20 higher. `measure-objective-split` says why — the agent **abandons 38% of objectives** against the bar's 16% while being pushed off only 0.7% of the time, and parks **8.6 surplus models** on the points it holds against the bar's 3.4. It is not losing objectives, it never goes. That is exactly what `objective_hold.crowding_exponent` prices, and it is too weak here. It is meanwhile *better at fighting*: firepower 1.76 v 0.88, alive 0.65 v 0.51, **exposure 0.245 v 0.438**. Getting here first required scrapping a run at epoch 470: the terrain profile had been tuned against `measure-terrain`'s own hidden-fraction metric, which rewards piece count, and `_coverage` billed bounding boxes — together giving **0.194 hidden against the real game's 0.088** and objectives that held 5 models instead of 18. Refitted to the 45 real layouts |
| **[2026-08-09 — TF32 costs 8.5 vp_margin](2026-08-09-tf32-costs-eight-vp.md)** | A fresh run of the golden config scored +21.2/+19.9 where the published figure was +28.4. What changed? | **TF32**, which shipped on by default on 2026-08-08 and had never been measured against a trained result. At matched epoch 1000, n=100 identical layouts: **s1 +30.8 → +21.2, s2 +27.4 → +19.9** — beating the bar by 12.1 becomes beating it by 3.6. The `--no-tf32` control reproduced the pre-TF32 run **bit-identically** (222/222 tensors, max abs diff 0.0), which proves TF32 is the whole effect *and* re-confirms determinism across every other change in the window. It buys **17.8%** of an epoch, not the 1.34x the update-only benchmark implies. **Now off by default.** The refuted claim — "below every effect size this project can resolve" — was inferred from the mantissa drop and a throughput benchmark, never measured. **Win rate would have missed it** (0.705 → 0.65, inside the ~7pp limit); `vp_margin` caught it. Lesson: a precision setting is a reward-affecting change |
| **[2026-08-08 — paying the pot beats the bar](2026-08-08-paying-the-pot-beats-the-bar.md)** | Beat `scripted_advance_and_shoot`. | **Achieved, on both seeds.** Pricing objective *crowding* — paying a point a fixed pot split between its occupants rather than every occupant the same wage — takes the agent from +2.5 to **+28.4 vp_margin**, past the `squad_march_shoot` bar at +17.0 (n=100, identical layouts, +30.1 and +26.6 on the two seeds). The lever is `objective_hold`'s `crowding_exponent`. **Confound-controlled:** the same weight with a *flat* exponent scores **−40.4**, worse than the control, piling 20 of 21 survivors onto one objective — so the exponent is worth 68 vp at fixed weight and "objectives just pay more" is refuted. Why it worked where two earlier anti-stacking levers failed: **it redistributes reward instead of destroying it** — a penalty and a discount both lower total objective income, so the policy reads either as "objectives pay less". Also: `last.ckpt` was **not the last epoch** (it held epochs 970/692/948/998), so every prior "at N epochs" score was really "at whatever epoch that run last improved" |
| **[2026-08-06 — beat the shooting opponent](2026-08-06-beat-the-shooting-opponent.md)** | Why does the agent lose to `scripted_advance_and_shoot`, and what actually closes the gap? | **The agent was never ahead of the bar** — batch 3 scored agent and baseline on different layout sets; on matched seeds it is 0.67 against 0.77. Two binding defects found, neither about cover: the reward makes objective **stacking provably profitable** (hold pays +0.25/step, the anti-stack knob charges at most 0.24), and **36–40% of shots are discarded** as overkill because the policy is factorized over models. Decoding targets autoregressively is worth **+10.5 vp_margin on the existing checkpoint with no retraining**. **Closed 2026-08-08 — every lever here measured null or negative**; the goal was met by [the follow-up](2026-08-08-paying-the-pot-beats-the-bar.md). Two conclusions carry inline corrections: the lowered prior on anti-stacking, and the diagnosis that the levers failed because occupancy was unobservable (adding the observation was worth +1.55 ± 4.5). All its numbers were scored through a `last.ckpt` that was not the last epoch |
| [2026-08-06 — cover: signal, reason, geometry](2026-08-06-cover-signal-reason-geometry.md) ⚠️ **corrected** | With all three blockers removed — 19.8% of the board hidden, a per-model LOS input, and losses priced — does the agent use cover? | **Still no**, and this time the null is clean. Exposure 0.092–0.110 across a 2x2, two seeds each. Side findings are stronger: the `models_lost` penalty is worth **+7 vp_margin** (non-overlapping seeds), the `observe_threat_count` input is **null**, and the penalty made the agent lose *more* models, not fewer — the opposite of the mechanism it was added for. `firepower_advantage` is broken as specified: `random` outscores every trained arm |
| [2026-08-05 — stochastic terrain and cover](2026-08-05-stochastic-terrain-and-cover.md) | Does the agent learn to use terrain for cover when the opponent shoots back? | **No — it learns range management.** Deleting every ruin moves exposure 0.116 → 0.120; return fire alone moves it 4.7x. At weapon range 24, where distance stops working, the policy collapses to 6.8% win. Reward *and* opponent curricula both unnecessary. Objective-placement defect found and fixed: 25% of episodes had overlapping objectives |
| **[2026-08-04 — correction: what was actually broken](2026-08-04-correction-what-was-actually-broken.md)** | **Read this first.** Is the policy learning at all? | **No.** The movement head never left initialisation; a 12-line heuristic beats 945 epochs of training 80% to 17%. Four further defects found; most conclusions below retracted |
| [2026-08-04 — reward-phase curriculum](2026-08-04-reward-phase-curriculum.md) | Why does the 25v25 curriculum never leave phase 0, and can it reach `win_at_the_end`? | ⚠️ Substantially retracted — training reward never left phase 0, so no cross-run conclusion holds |
| [2026-08-04 — mechanism defects](2026-08-04-mechanism-defects.md) | Detailed evidence for each defect found | D1/D2/D4 survive (arithmetic); D3/D5's numbers measured a near-random policy |
| [2026-08-04 — objective drift](2026-08-04-objective-drift.md) | What does the agent actually do during an episode? | Drift is real; ⚠️ its explanation is superseded — a uniform policy produces the same trace |
| [2026-08-04 — reward calculation changes](2026-08-04-reward-calculation-changes.md) | What changed in the reward, and what does the evidence support? | Reward became per-model; two calculators added, three dropped, one arithmetic bug fixed. Win 0.17 → 0.93–0.97 vs a 0.67 movement bar — but **confounded** with the PPO fixes that shipped alongside |

## Conventions

**Distinguish measurement from inference.** Every quantitative claim states how it was
obtained. Claims that are inferred rather than measured say so.

**Report negative results.** A refuted hypothesis costs a run to establish and costs
another run every time it is retried by someone who does not know it failed.

**State confounds.** Most runs here changed more than one variable, were stopped early,
and used a single seed. That is adequate for finding defects and inadequate for ranking
hyperparameters. Reports say which of the two they support.

**Prefer the live metric.** `success_rate` from a best checkpoint is not the same
quantity as the per-epoch `success_rate` a gate reads. See
[mechanism defects, D4](2026-08-04-mechanism-defects.md#d4--rungs-calibrated-against-the-wrong-distribution).

**Quote every number against a floor and a bar.** A `success_rate` alone says nothing —
17% read as progress until a scripted baseline scored 80% on the same board. Run
`just measure-baselines <env_config>` before interpreting any result; training logs
`eval/baseline_*` automatically. Note every number in these reports was measured against
`scripted_advance_to_objective`, an opponent that never fires — the `squad_march_shoot`
bar of 1.00 is partly an artefact of that, and falls to 0.60 against
`scripted_advance_and_shoot`.

**Verify the thing you are tuning is the thing being trained.** Seven runs tuned reward
phases whose rewards never reached the gradient.

## Reproducing

```bash
# Scripted baseline scores -- the floor and the bar. Run this before reading any result.
# Pass a seed_base to score on the same layouts as measure-checkpoint.
just measure-baselines <env_config> [n_episodes] [record] [seed_base]

# Score a checkpoint through the baselines' own code path, so the two are comparable
just measure-checkpoint <checkpoint> <env_config> [n_episodes] [record]

# Rolling-mean metrics for a run (point readings are n-sample binomials -- do not trust them)
just run-summary <run_id> [bucket_size]

# Per-phase criteria rates and the whole min_fraction curve for a checkpoint
just measure-phase-gates <checkpoint> <env_config> 40

# Behavioural analysis of a recorded match
just replay-summary <file>
just analyze <file>
just analyze-compare <files...>
```

Recordings live in `recordings/` (gitignored). Runs are in the `wargame_rl/wargame_rl`
Weights & Biases project; run IDs are given in each report.
