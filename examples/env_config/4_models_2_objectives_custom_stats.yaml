config_name: 4_m_2_o_60x44_custom_stats
number_of_wargame_models: 4
number_of_objectives: 2
objective_radius_size: 3
board_width: 60
board_height: 44
group_cohesion_enabled: true
group_max_distance: 5.0
group_violation_penalty: -0.1
max_groups: 2

# Attributes only (no x/y) — models are placed randomly but with
# explicit group assignments and wound pools.
models:
  - { group_id: 0, max_wounds: 50 }
  - { group_id: 0, max_wounds: 50 }
  - { group_id: 1, max_wounds: 120 }
  - { group_id: 1, max_wounds: 120 }

objectives:
  - { radius_size: 5 }
  - { radius_size: 2 }
