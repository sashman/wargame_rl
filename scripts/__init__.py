"""Run-inspection and measurement tooling.

Run these via `python -m scripts.<name>` (as the Justfile recipes do), not as
a file path: the project is not installed as a package, so `wargame_rl` is
importable only when the repo root is on `sys.path`.
"""
