# Testing

Applies to everything under `tests/`. General testing philosophy lives in the root `CLAUDE.md`
("Good Enough" Testing); this file covers the project-specific setup.

## Setup

- Pytest + shared fixtures in `conftest.py`; run via `just test`; coverage → `coverage.xml`
- Fixtures: `n_steps`, `env`, `experiences`, `policy_net` (a bare `TransformerNetwork`), `transformer_net`, `ppo_net`, `ppo_transformer_net`
- **Every network fixture builds `TEST_TRANSFORMER_CONFIG` — 2 layers of 32, heads left at the default 8 — not the production trunk.** `TransformerConfig()` defaults to 8 layers of 256 at 8 heads, ~12.7M parameters, and the suite was paying that on a 2-model 20x20 board, where the slowest tests were all trunk-bound and each checkpoint written was 153 MB. Measured: **155s to ~100s** wall clock under `-n auto`, and `test_z_e2e_training` **66.0s to 1.2s** run on its own. ⚠ `--durations` under `-n auto` is wall time *including worker starvation*, not a test's own cost — the e2e test still reports ~40s there while taking 1.2s alone, so compare wall clock or run serially. ⚠ **A test asserting on a NUMBER a trained network produces must build its own production-sized trunk**, because this is a different network; nothing does today, since these tests assert on shapes, dtypes, wiring and invariances, none of which trunk depth changes. ⚠ **`n_heads` is left at its default on purpose**: the head count changes no parameter shape, so it cannot be recovered from a checkpoint — `trunk_config_from_state_dict` infers depth and width and nothing else, which is also why `train()` has no `--n-heads`. `test_network_size` pins the production default at 8/8/256, pins that omitting `transformer_config` still builds it, and pins that a shrunk checkpoint loads back at its own shape, so shrinking the *fixtures* can never quietly shrink what ships — a default nobody asserts is one somebody eventually "optimises", and here that would orphan every checkpoint in `checkpoints/` and void every score in `CLAUDE.md`, surfacing only as a `load_state_dict` failure far from the change
- **`full_float32_precision` is autouse and pins `set_float32_matmul_precision("highest")` before every test.** That setting is process-wide and sticky, so one test enabling TF32 changes the arithmetic of everything after it in the same worker — which surfaced as a once-per-16-runs "flake" in `test_transformer_shooting_policy` that is actually a deterministic 25/25 failure under TF32. A test needing another mode sets it itself; the fixture only decides the starting point

## Rules

- No `@lru_cache` on fixtures — use `scope="module"`/`scope="session"` instead
- Annotate the `policy_net` fixture as `RL_Network`, not as the concrete network class
- Prefer integration tests; test through public APIs (add properties rather than accessing `_private` attrs)
- Type-annotate fixtures and test functions
- `wargame_rl/wargame/envs/interactive_demo.py` is NOT a test
- When env default config changes stepping (e.g. `skip_phases`), tests that rely on per-phase or all-phase behaviour must set config explicitly (e.g. `skip_phases=[]` in a shared `_make_env` or in the test)
- **A randomly-initialised network counts as randomness.** `TransformerNetwork.policy_from_env` seeds nothing, so a test asserting on its outputs needs `torch.manual_seed` — otherwise a failure arrives with weights nobody can reproduce
- **Before calling a test flaky, check what global state ran before it.** Process-wide torch settings survive across tests in a worker, so an intermittent *numerical* failure is more often a leak than noise. Rank hypotheses by what you can make fail on demand: thread oversubscription was the intuitive answer to the one flake seen here and gave 0 failures in 960 executions, while TF32 reproduced 25/25

## Test Files

**Env & clock** — `test_env` (reset/step) · `test_clock_integration` (phases, skip_phases, rounds) · `test_game_clock` (clock unit) · `test_turn_execution_fairness` · `test_fixed_placement` (config/placement) · `test_rollout_env_lifecycle` · `test_objective_control_distance_cache` · `test_start_state_augmentation` (the training-time squad teleport; its no-op guarantee is pinned against digests recorded from a worktree at the *parent commit*, because comparing two envs of the current build is vacuous — a stray draw shifts both streams and cancels, and that version passed while the augmentation drew on every reset)

**Rules & combat** — `test_unit_shooting` (a weapon names a **unit**: attacks are lost only when the whole unit dies — 3.6% of declared shots, against 36-40% when a shot at a dead *model* evaporated — the defender allocates to a wounded model first, fire never spills onto an undeclared unit, and `unit_count` sizes the action space from the highest `group_id` rather than from `max_groups`. `TestGroupIdsNeverAliasOntoOneColumn` sweeps `max_groups` in {2,3,5,6,8,10} against n < 200 for the regression that `group_span` used to **floor**: an army could split into more units than the one-hot has columns, and `_group_ids_to_one_hot` clips rather than raising, so two units shared a code while the shooting slice was sized at the true count. Asserted against the raw `i // span` assignment as well as against `n_groups_for`, so the two cannot drift apart and both be wrong) · `test_shooting_action` · `test_shooting_resolution` · `test_shooting_opponent` · `test_shooting_baseline` · `test_action_masking` · `test_los` · `test_wounds` · `test_killing_reward` · `test_terrain` · `test_terrain_observation` · `test_terrain_render` · `test_random_terrain` · `test_exposure_metric` (exposure, terrain proximity, `firepower_ratio` — including that a concentrated force outguns what it exposes, the case the metric's first version got backwards)

**Reward & mission** — `test_reward_phases` · `test_new_per_model_calculators` · `test_per_model_credit` · `test_player_ahead_on_vp_criteria` · `test_mission_vp` · `test_killing_reward` · `test_models_lost_reward` (the loss penalty must be global — per-model is identically zero on one-wound models) · `test_objective_hold_crowding` (`crowding_exponent`: the flat default stays bit-identical, and at a = 1 the pot is conserved — total pay across a point's occupants is its value however many stand there, which is the property `surplus_value` lacks) · `test_curriculum_configs` (every 25v25 config must keep `vp_gain` + a per-model calculator; also pins the batch-2 and batch-3 arm factorials) · `test_contest_deficit` (`closest_objective_v2`'s candidate gate. The load-bearing pair is that the **default is the historical one-model test** — an objective held 3-0 is not a candidate, because one body cannot take `opponent` to `contested` — while `contest_deficit: 3` makes the same objective a candidate. The parametrised case asserts the widening does **not** open up points we already hold, which is the 56.6% of exclusions that should stay excluded and the way this could quietly become a pay-to-reinforce term. Also that an explicit 1 and the default agree across the whole 8x8 count grid, so the historical gate cannot drift, and that an invalid deficit is rejected at construction rather than in `env.step()`. **Verified sensitive: 8 of 9 fail with the implementation stashed**, the ninth being the default-unchanged test, which must pass on both)

**Model & training** — `test_ppo` (⚠ its `Trainer` passes **`logger=False`, not `logger=None`** — Lightning reads None as *use the default*, which is a TensorBoardLogger writing `lightning_logs/` relative to the working directory, with `enable_checkpointing` then dropping a checkpoint under it. Left as None this test wrote **242 MB per run** into the repo: a 153 MB `epoch=0-step=2.ckpt` plus a ~100 MB `hparams.yaml`, the whole `WargameEnv` pickled as a hparam. The test now asserts `lightning_logs/` does not exist, because "fit did not raise" passed happily throughout) · `test_agent` (actions/episodes) · `test_state` (obs/batch tensors) · `test_transformer_shooting_policy` (shooting head, dead-token masking) · `test_batched_eval` (lockstep eval waves) · `test_training_diagnostics` · `test_train_resume` · `test_train_run_name` · `test_simulate` · `test_behaviour_clone_checkpoint` (the clone's key prefixes must land on a real module — `_apply_warm_start_weights` loads with `strict=False`, so a wrong prefix loads **nothing** and trains a random network while reporting a warm start; also pins `unit_match_counts`, which scores fidelity per *unit* because coherency is joint and per-model action match reported 98.3% while unit coherency sat at 0.580)  · `test_z_e2e_training` (⚠ **runs from `tmp_path`, and that is load-bearing, not tidiness**: every checkpoint path in the trainer is relative to the working directory — `./checkpoints/{run}` in three callbacks, plus Lightning's own `lightning_logs/` — so from the repo root this test leaks **~292 MB per invocation** and never cleans up. Forty-five runs had buried the real weights under **10.4 GB** of `ci_smoke` directories, with another 13.5 GB in `lightning_logs/` and `logs/` (`epoch=0-step=2.ckpt` plus a ~100 MB `hparams.yaml` each, the whole `WargameEnv` pickled as a Lightning hparam), and it filled the disk until this test failed with `ENOSPC`. That is worse than ordinary debris because `checkpoints/` is the **only copy of any trained weights** and `just clean` deletes it outright, so nobody can bulk-clear it. The test asserts the run landed in `tmp_path` *and* that nothing appeared under the repo's `checkpoints/`, so a future absolute path re-opens the leak loudly)

**Observation** — `test_objective_control_observation` (`observe_objective_control`: token width off/on, counts exclude dead models, recomputed each step, batching, and that the transformer resizes its objective embedding on its own) · `test_observation_budgets` (`objective_budget` / `terrain_budget`: all 45 shipped maps produce one tensor shape, a five- and a six-objective map batch together (28 tables carry five and 17 six, the latter where a marker equidistant from two equally large ruins designates both) — and weights built on the generated scenario `load_state_dict` onto a real map — the size mismatch this exists to remove. Padding is checked *behaviourally*: moving a padded objective cannot change the logits, with the same move on a real objective as the control, since a structural mask assertion would pass on a network that ignores objectives entirely)

**Baselines & opponents** — `test_baselines` (scripted reference policies) · `test_opponents` (opponent system) · `test_assignment_optimal` (the exact squad→objective assignment behind `assignment_optimal`, which exists to price a ceiling rather than to play well. Pins the subset DP against brute force, and pins the classic greedy trap where the cheapest first pick strands the second squad — if the optimal matching ever came back equal to greedy the ceiling measurement would be vacuous) · `test_contest_and_spread_baseline` (opponent-aware allocation + de-duplicated fire; records a *refuted* hypothesis — it loses to `squad_march_shoot`, because concentration is why that bar wins) · `test_scripted_baseline_opponent` (a baseline played on the opponent side. The whole correctness question is the side-swap, so the tests are led by a *behavioural* one — the player is deployed on two of the four objectives, and `squad_march_take` playing the opponent must therefore march at the other two, which an un-mirrored policy counting its own models would not. Verified sensitive: un-swapping the enemy list fails it. Two traps it had to survive first — the episode ends on step one under the default `all_at_objectives` phase, because the player *starts* on objectives, which made a run-it-out assertion vacuous; and the opponent acts after the phase advances, so a single step tests nothing. A static scan enumerates the side-specific env attributes the baseline package reads and fails when a new one is not mirrored, since a baseline reaching for an un-swapped attribute plays for the wrong army silently. `test_an_env_carrying_this_policy_survives_a_deep_copy` is the regression for the bug that shipped: a `__getattr__` proxy reaching through `self._env` recurses forever under `copy.deepcopy`, which reconstructs without calling `__init__` — Lightning deep-copies the env in `save_hyperparameters`, so **every training run on the policy died before epoch 0** while all twelve other tests stayed green, because none of them copied an env. It steps the copy rather than only building it, so it pins playability and not constructibility)

**Illustrations** — `test_record_gifs` (the README's GIFs keep the board's own colours. The colour assertion alone would be **vacuous** — quantising to a GIF palette preserved the flat fills under every setting tried, including a per-frame dithered one — so `test_a_video_round_trip_is_what_drifts_them` pins the thing that actually breaks: H.264 in `yuv420p` shifts every flat fill one to three points, which is how the previous hand-made GIF shipped with the board at (238, 229, 210) instead of (239, 231, 212). It fails loudly if a video round trip ever stops drifting, because at that point the colour test above proves nothing. Frames therefore go straight from the renderer to the GIF, never via the mp4 the recorder already has)

**Ratings & self-play** — `test_in_run_elo` (the two ratings a training run logs about itself. The load-bearing one is `test_the_update_does_not_depend_on_the_order_games_finished`: Elo updates do not commute and an epoch's games run in parallel across rollout envs, so a per-game sequential update would make the rating depend on which env finished first. `test_the_in_run_rating_is_not_a_fitted_one` and the monotone-in-the-margin test exist to stop either number being read as a Bradley-Terry rating — `eval/elo` carries no information `eval/vp_margin` does not) · `test_self_play_wiring` (also pins that `self_play/learner_elo` appears **only** with a pool, and that a banked game is credited to the opponent actually seated. Both are driven through a real `Trainer` rather than by calling the hooks, because the outcome is banked inside the parallel rollout collector — a hooks-only test passes on wiring that never records a game, and the env config is cut to two battle rounds so an episode actually ends inside one epoch's rollout)

**State I/O** — `test_snapshot` · `test_state_injection` · `test_event_stream` (also the delta codec's field coverage, pinned STRUCTURALLY: every dynamic `GameStateSnapshot` field has a `StateDelta` field and every dynamic `ModelSnapshot` field a `ModelDelta` field, plus one mutation per field round-tripped — the 2.7 melee lists had no delta field and every replay rebuilt from deltas carried the anchor's blows, #326; a 2.7 log still loads with `decision is None`) · `test_narrator`

**Rendering & debugging** — `test_renderer_v2` (Scene purity, control extraction, `FrameSource`, shooting, and the headless A/B against the untouched legacy renderer) · `test_debug_session` (hand-stepping a live match. The load-bearing case is `test_a_restored_env_replays_the_step_identically`: everything the debug mode is for rests on a rewind that changes nothing but the decision, so it pins reward, breakdown, per-model reward, positions, wounds and VP across a capture/step/restore/step cycle. Its fixture must actually *shoot* — shooting is skipped by default, so the weapons and `skip_phases` are load-bearing too, otherwise the RNG half goes untested. The loop tests drive `run_session` with a scripted renderer instead of a window, and their control helpers pause exactly as `DebugPresenter` does — a script that set `step_once` without pausing would exercise a combination no key can produce, and did, by silently resuming after a rewind. The inspector tests are behavioural, not pixel comparisons: the panel is *reserved* (`window_w == board + PANEL_W`, board not shrunk), clicking selects on either side, and two different selections must not draw the same column. `test_a_dead_model_earns_nothing_which_is_why_the_panel_says_so` arranges the casualty by zeroing wounds rather than shooting for one — 3v3 produced no death in 40 steps — and steps on until a *survivor* is paid, since on a step where the whole army earns zero "the dead model earned zero" proves nothing. The move-history test needs a movement-only config: with the default phases the second step is shooting, writes no records, and the test compares a step against itself. The order tests lead with `test_an_order_lands_where_the_ghost_says_not_where_the_click_was`, which authors a move, steps, and asserts the model is within 1e-9 of the drawn landing point — a ghost that lied would make every authored move a guess. Beside it: the landing point is deliberately *not* the click (the bins are coarse, and seeing how coarse is the point), a dead model is refused by name rather than by the mask, an illegal order is drawn but never applied, and the opponent wrapper is replaced rather than nested — stacking it would re-apply stale overrides for the rest of the session. The dice test steps once first so the step under test is the *shooting* one, and asserts over six seeds rather than one, since any single reroll may land on the same result by chance. The sight-shading tests are led by `test_the_shadow_is_the_engines_own_answer_and_not_the_renderers`, which walks every cell centre and asserts it is shaded exactly when `has_line_of_sight_between_points` says it is unseen — the shading is only worth believing where it contradicts intuition if it is a picture of the engine's predicate rather than of geometry the renderer projected. The rest pin what a picture cannot show: rectangles never overlap (translucent fill, so a doubled rect draws twice as dark), an empty board casts nothing, and a model in the way shades nothing — which is what the shading is a picture of) · `test_reproduce_recording` (record an episode, rebuild it from the log header's provenance, and assert the trace comes back *identical* — VP, per-step reward, both sides' positions and wounds. The fixture regenerates terrain per episode on purpose: a fixed layout would be reproduced by the config alone and the generator state would go untested. One case pins that an **unseeded** episode reproduces too, which is why provenance stores a generator state rather than a seed; one pins that provenance is header metadata and not a snapshot field, since a field on the recorded copy that a live snapshot lacks would break the replayed-equals-live invariant `test_v9_milestone_validation` asserts)

**Performance** — `test_network_size` (the production trunk is 8/8/256 and omitting `transformer_config` still builds it — the two halves of letting tests shrink the network safely; also that the small trunk really is ~50x smaller and still yields finite logits, since a trunk too small to run would make every fixture-based test vacuous) · `test_reward_golden` (bit-identical gate: per-step reward, per-model reward, breakdown, VP and positions against recorded trajectories in `tests/data/`; use `assert_array_equal`, never `assert_allclose` — these values feed published reports, and a tolerance hides exactly the float-reassociation regression a vectorisation introduces. Regenerate only deliberately: `uv run python -m tests.test_reward_golden --regenerate`) · `test_reward_memoisation` (the two hot-path rewrites against reference copies of the code they replaced — the golden trajectories only cover the states they happen to reach) · `test_observation_golden` (the network's *input*, which `test_reward_golden` does not pin: the six observation arrays bit-identical across three configs, the batch and single conversion paths agreeing, and — anchored to env truth rather than a recorded file — that the alive column is still where `_alive_feature_index` looks. **Both gates are verified sensitive**: a one-ULP feature change and an appended per-model column each fail them on all three configs. The alive check deliberately leaves one model *wounded but alive*, because at `max_wounds: 1` `wound_ratio` is bit-identical to `alive` and an off-by-one column read is otherwise undetectable — verified, it passes without that) · `test_precision` (TF32 gated on sm_80+ and restored after, and **off unless a caller asks** — the default is pinned separately because every other test here passes `enabled` explicitly and so would miss it flipping back, which cost 8.5 vp_margin for a day in 2026-08-09; `PPOModel.forward` returns float32 under bf16 autocast, and the quantisation that makes that guard necessary — a 0.007 nat log-prob change does not survive a bf16 round trip at these magnitudes)

**Map pool** — `test_map_pool` (the third terrain mode: the three modes are mutually exclusive, a seeded reset draws the same table twice, objective count really does vary 5↔6 across the pool, a drawn map's objectives are its own areas rather than re-derived from its ruins, every draw produces one tensor shape and they collate, and the whole pool is checked *at load* — mixed counts without a budget, a budget below the pool, an unknown name, a missing directory, a map off the board. Also pins that the shipped training config holds out every fifth table, since training on all 45 would silently consume the evaluation set)

**Rating & seats** — `test_rating_score` · `test_rating_elo` (the Bradley-Terry fit on synthetic designs: planted ratings and both advantage terms are recovered, an undefeated entrant stays finite, and a schedule varying zone and turn *together* is **refused** rather than handed a fabricated split. `test_the_layout_bootstrap_is_wider_than_resampling_rows` bootstraps the same games twice, grouped and ungrouped, because the reason `docs/elo.md` gives for the bootstrap — that the quasi-likelihood Hessian understates the error — does not survive contact: any [0,1] score has variance at most p(1-p), so the Bernoulli assumption is conservative, and what the Hessian actually misses is dependence between rows) · `test_rating_schedule` (a leg is a config transform; the four legs share terrain and objectives; a config whose zones are left at their defaults is **refused**, since `battle_factory` derives them and swapping two `None`s is a silent no-op that would have `h_zone` fitted from noise; a **`map_pool` config is refused on every leg**, including the two that swap nothing, because a drawn table carries its own deployment outlines and those stay bound to the seats — the rectangles the swap moves are only placement's sampling bounds) · `test_rating_arena` (a leg's margins must equal `evaluate_selector`'s on the same seeds, so the arena cannot drift into a second scoring loop) · `test_rating_ledger` (`test_all_four_leg_configs_fingerprint_identically` is the load-bearing one: `docs/elo.md`'s literal instruction would put the four legs of one pairing into four different ledgers) · `test_swap_invariance` (the mirrored observation equals the other seat's tensor for tensor — the guard that survives every shape tripwire being removed, since the static scan in `test_scripted_baseline_opponent` catches only new reads of *known* side-specific names. It needed three corrections before it meant anything, each of which left it passing and vacuous: seeding both envs alike gives two different boards because each army fills its own zone, the game tensor carries shared round/phase/score state, and objective distances are cached on the models. `test_the_comparison_would_notice_an_unmirrored_side` is the sensitivity control) · `test_model_opponent_policy` (a checkpoint on the opponent seat: it must draw **nothing** from the layout RNG, survive `deepcopy`, refuse unequal armies — `_alive_from_features` falls back to *all alive* out of range, so they degrade silently — and reject an unknown state-dict prefix rather than loading nothing) · `test_action_selector_resolution` (one resolver for a baseline name or a `.ckpt`; asserts in a **subprocess** that resolving a baseline imports no torch, which `debug.py` depends on) · `test_rating_seat_term` (the player-seat advantage as a fitted term. The load-bearing case is `test_a_rating_no_longer_depends_on_the_order_entrants_are_named_in` -- entrant A always takes the player seat and `pairings` lists each pair once in input order, so before this term the first-named entrant was advantaged by argument position alone. `test_identification_follows_the_pairing_graph` checks the cycle argument rather than asserting it: three entrants suffice, two do not, and one self-pairing rescues two. ⚠ The two-entrant refusal fires **with a prior in place**, because there the null direction has a rating component and the prior would silently decide the split rather than raise) · `test_opponent_coherency` (the opponent force's own tracker. ⚠ The obvious defect -- handing it the player's models -- passes every structural assertion, and the shipped dev config cannot catch it either: it gives every model its own `group_id`, so a unit is one model, coherency is 1.000 by definition on both seats, and the first two versions of these tests asserted nothing. The fixture rewrites the group ids; verified sensitive against the swapped-force version) · `test_pfsp` · `test_rating_pool` (the snapshot pool: the anchor survives every eviction, and thinning keeps the pool **spanning the run** rather than sliding over its recent past -- the two ways a pool goes wrong) · `test_self_play_wiring` (⚠ led by `test_a_training_module_builds_no_scheduler_when_self_play_is_off`: a version that constructed the scheduler and merely declined to use it would pass everything else here, and is how `augment_start` nearly shifted every layout. `test_a_snapshot_loads_back_as_an_opponent` is the regression for the bug a three-epoch smoke run found -- a snapshot must be the *Lightning* module's state dict, since `convert_state_dict` wants `ppo_model.policy_network.` and the inner module gives bare `policy_network.`; it raised only because that path loads strict, where `_apply_warm_start_weights` would have loaded nothing and reported a warm start. It steps the env rather than only constructing the policy) · `test_import_direction` (AST-walked: `envs/` imports neither `model/`, `rating/` nor torch — a real cycle, since `net.py` imports `envs.wargame` — and the rating mathematics imports nothing from `wargame_rl` at all)

**Board reads** — `test_board_layer_is_a_leaf` (AST-walked, and it pins *two* directions: `envs/board/` imports nothing but `domain/` and `types/`, and only `renders/v2/control.py` may import `board/`. Without it the rule is a comment — the first person who wants a board grid imports the renderer from inside the leaf and the cycle arrives with no error message) · `test_matchup` (the unit table as a *reduction* of `expected_damage_matrix`, never a second implementation. The load-bearing case is the reduction axes: a 5-model unit into a 5-model unit must read `5 x ED` and not `25 x ED`. `TestTheWoundClip` covers the conversion nothing shipped exercises — expected damage is **damage points**, and a model costs `D x ceil(W / D)` of them, since excess damage is lost *and* a model may absorb several hits. ⚠ Every config is `damage: 1 / max_wounds: 1`, where both effects are invisible, so a sweep over every Damage at `max_wounds: 1` pins that the conversion stays a no-op wherever every measured number lives)

**Evaluation** — `test_measure_maps` (final evaluation swaps `terrain` onto the golden scenario; the guarded failure is silent — a surviving `random_terrain` regenerates a layout and discards the map, so the run scores the training distribution while printing a map's name)

**Coherency** — `test_coherency` (the predicate, as pure geometry: a chain at exactly 2" passes, a six-model chain at 2" breaches the 9" spread, and — the case the rule's third clause exists for — **two pairs 5" apart satisfy both stated conditions and are still not a unit**, because the chain graph has two components. Also that blame lands on the straggler rather than the body it left, that dead models leave their unit so attrition cannot cascade, and that distances are base to base — the same pair is coherent with 32mm bases and incoherent as point models) · `test_coherent_deployment` (§ Setting up: every unit deploys in coherency across 20 seeds and both deployment zones, with the **control that default placement never does** — a vacuous pass here would be invisible otherwise — plus the byte-identical no-op when off, and the two config guards: a spread cap below the chain distance, and a `max_groups` that puts every model in its own unit where the rule holds vacuously)

**Coherency observation** — `test_coherency_observation` (the two `observe_coherency` columns and the corpse fix: the spread column separates a 5-model chain's ends from its middle where the nearest-neighbour column is identical for both; the component column reports 0.6/0.4 across a split unit so a model can tell which side of the break it is on; the spread clips at the cap so one stranded model cannot dominate; dead models report *compliant* values rather than violations, since the row is masked anyway and a violation there would be a standing penalty on every corpse. Plus the `_alive_feature_index` trap re-checked with the new columns on, anchored to env truth with one model genuinely dead — and asserting the model really is dead first, since `policy_from_env` resets the env and would otherwise revive it and make the test vacuous)

**Coherency enforcement** — `test_coherency_enforcement` (the end-of-move revert, on hand-placed geometry rather than on whether a policy happened to break formation. Both modes on the *same* move — `revert_unit` cancels all three models, `revert_model` returns only the straggler, **on a geometry where sending the straggler home actually leaves the unit coherent**; the original test used one where it did not, so it pinned the defect rather than the rule, and `is_coherent` now asserts the contract the revert count cannot. The escalation case is its regression: where a local revert leaves the unit spanning 18.5 against a 9 cap, `revert_model` widens to the whole unit. A unit already broken before it moved goes back to its start and stops there, still incoherent, rather than looping — plus the case that makes the modes coincide: a straggler far enough to breach the **spread** cap puts every model in the unit in breach, because that condition is collective, and this is why the modes tie on the bar and differ by 50 vp on `split_evenly`. Also that a model with no `previous_location` has nothing to revert, which is the casualty-broken unit the spec reserves attrition for)

**Coherency reward** — `test_unit_coherency_reward` (the `unit_coherency` calculator, the only coherency mechanism that is not a constraint. The load-bearing case is `test_the_straggler_and_its_squadmates_are_paid_apart`, which asserts the two payments *differ*: a term paying every model in a broken unit the same would satisfy every other assertion here and create no gradient at all, which is the mistake flat `objective_hold` made. Also that a lone survivor is **paid** rather than fined, since the rule makes a one-model unit coherent by definition and fining it prices a casualty the model did not cause; that units are scored independently, so one broken squad cannot fail an intact one; and both halves of the config guard — a config naming the calculator without `observe_coherency` is rejected at construction, **with the control that the same config is accepted once the flag is on**, since a guard that rejected unconditionally would pass the rejection test and make the calculator unusable) · **Coherency attrition** — `test_coherency_attrition` (§ Regaining coherency: the straggler dies and not the body — removing the lone model costs one where cutting the body would cost three, which is why the rule drops from the smallest component first; removal repeats one at a time until the unit is whole; a *connected* unit that merely overruns the spread cap loses its furthest model instead, since connectivity cannot pick a victim there; the loop is self-limiting because a one-model unit is coherent by definition, so a scattered unit never eats itself entirely; and the choice is deterministic, which the seeded env and the golden gates both require)

**Rules spec** — `test_scale_and_rules_quantities` (the inches↔units `Scale`, and that resolving at the default 1.0 reproduces the exact numbers the code used before the scale existed — that assertion is the no-op guarantee, so a failure means every baseline was measured under a different env; also pins that `RulesQuantities` holds only fields something reads, since a settable-but-inert config field is how a lever silently does nothing) · `test_rules_constants` (pins the domain against `docs/rules/constants.yaml` wherever the env claims fidelity; everywhere else the divergence is recorded in `implementation-status.md` rather than asserted, so a failure here means the code moved away from the spec, not that the spec is aspirational) · `test_no_ip_references` (denylist over tracked **and untracked** files — untracked matters, because a scratch file is exactly how a product reference gets reintroduced)
**Occlusion & cover** — `test_units_and_cover` (**models do not block sight — only terrain does**, a deliberate divergence recorded in the rules gap map, so a solid screen of enemy bases leaves the target fully visible while a ruin still hides it. The load-bearing case goes through `compute_unit_shooting_masks` — the mask the game actually builds — rather than a helper: the unit exemption that softened occlusion was threaded through a seam only the exposure tracker called, so a model *was* blocked by the squadmate in front of it on both real shooting paths, and a test asserting on the helper stayed green throughout. Cover is now terrain's alone: a ruin clipping one edge of the corridor is `COVER`, it costs one point of Ranged Skill, an unmodified 6 still hits, and at `base_radius: 0` the three rays coincide so cover cannot occur at all. `test_a_fully_hidden_member_does_not_deny_its_unit_cover` is the #289 regression — the unit reduction counts any blockage, so a `HIDDEN` member satisfies "not fully visible" instead of stripping its squad's cover; verified to fail on the pre-fix `== COVER` reduction. Visibility is symmetric, which `firepower_ratio` depends on. `test_a_shot_into_cover_reports_the_expectation_it_was_rolled_under` is the *analytical* half, and it is an end-to-end case on purpose: `expected_damage` was written before cover and read the weapon's skill straight off the profile, so a unit test of the formula agreed with itself while every recorded shot into terrain was quoted against an open-ground expectation. The paired unit tests live in `test_shooting_resolution` — cover equals exactly one point of skill, RS 6 in cover still hits on a 6 rather than reporting 0.0, the no-cover default is **bit-identical** to the pre-cover formula so `test_observation_golden` and every checkpoint survive, and a 20k-trial Monte Carlo of `resolve_shooting` agrees with the closed form, which is the assertion that fails when the dice and the formula drift apart again)

**Baseline geometry** — `test_baseline_geometry` (the batched arrival geometry of #270 — `Polygon.distances_to_boundary`, boundary-inclusive `contains_points`, and `steps_toward_objective` — equals the scalar per-model loop **exactly**, `assert_array_equal` and never `allclose`, over seeded sweeps on area and marker objectives through a real env's handler. A deterministic ULP-sensitivity case proves the equality can fail at all: the probe point sits straight out from a vertical edge so the distance IS the nudged coordinate — a random-polygon probe was rejected because a one-ULP nudge on the dominated component vanishes into the sum of squares and the "sensitivity" test passes vacuously. Squads are multi-model with distinct positions on purpose: a batching bug that collapses per-model state returns one model's answer for everyone, which a one-model squad can never catch)

**Continuous space** — `test_continuous_space_and_bases` (the two quantisations are gone, pinned on *distance travelled* rather than on a dtype — an integer array assigned a float truncates silently, so a dtype check alone would pass with the board still a chessboard: every movement action has a distinct outcome, a speed bin travels the same distance in every direction, and a model actually ends up off the grid. Plus bases: non-overlapping at placement, a zone too small for the army failing loudly with the numbers, no base hanging off the table, and objective range measured from the base edge)

**Config pairs** — `test_map_config_pairs` (the claim the four-opponent table rests on: the five refereed `configs/evaluation/25v25_maps_*` configs differ from one another in exactly `opponent_policy`, so a row here and a row there differ by the opponent and nothing else, and the two golden map configs differ in exactly `config_name` + `opponent_policy` — which is why nothing trains on `25v25_maps_coherency`. Compared on the *parsed* config rather than the text, since comments carry most of the bytes and none of the meaning. Verified sensitive before being trusted: the same helper reports `{config_name, coherency}` for the `repair` variant and `{config_name, observe_unit_centroid}` for the centroid one. Also pins that every member is refereed, since turning the referee off flatters the scripts by ~16 vp and puts the row in a different table)

**Config validation** — `test_config_rejects_unknown_keys` (every config model forbids extras, enumerated so a newly added model fails rather than silently swallowing typos; also pins that the legacy `size`/`width`/`height` aliases are *consumed* by the before-validator rather than left in the dict, and that free-form `params` dicts stay open)

**Cross-cutting** — `test_checkpoint_module_alias` (a moved module silently un-loads every existing checkpoint — Lightning pickles `PPO_Transformer` into `hyper_parameters`, so checkpoints carry import *paths*; verified sensitive by deleting the alias) · `test_integration` (backward compat) · `test_v9_milestone_validation` · `test_imports` · `test_interactive_demo`

**Per-model facade** — `test_per_model_bridge` (the FIRST test of `envs/per_model/`: a scripted policy through both facades on one layout and dice must agree **bit for bit** — positions, wounds, VP, reward, per-model reward, breakdown and the combat dice position — at every point the phase facade's step would have returned, **up to the first `FacadeDivergence` the facade records**: the per-model step honours orderings the whole-army step cannot (targets selected after an earlier unit's casualties, attrition on both sides, a battle that continues after a wipe), and from the first boundary settled after one the two games are allowed to part — on 4 of 15 cases they never do. Compared per settled reward *window* on the new side against per step on the old; a stepped command phase settles its window before the first decision, so the reset's `info["settled"]` is collected too — without that the first boundary was missing and the test read "phase movement != shooting". The melee case holds until the first charge that stands, detected by the flag on our own boundary *or* by the engagement an opponent's charge inside our window leaves behind — the flag alone missed it, because the fight boundary clears it before our next step) · `test_per_model_shooting` (the orderings the whole-army step cannot honour, on hand-placed geometry the test asserts first, with dice that kill on every attack: a unit's targets are selected blind and an attack at a unit a squadmate just wiped is lost; the next unit selects against the board its predecessor left; cover is judged against the members the attacking unit faces) · `test_per_model_fuzz` (a seat drawing uniformly random LEGAL decisions never raises, terminates at the phase clock's own `max_turns`, and never resurrects a casualty or lowers a VP total — the only test that exercises decisions no script would take) · `test_per_model_env` (the contract through `step`: the lock narrows to the opened unit and forces the opener, an illegal decision is refused by name and the point stands, stationary closes a unit in one step, the advance roll is hidden until the unit declares and equals the phase facade's for the seed, a unit with no target costs no step, the closing step pays the window, `max_turns` is the phase facade's across skip variants, a step after the end raises, attrition culls every unit on the board at each end of turn — read from the opponent's own seat as its movement opens — and the battle continues after the opponent is wiped out) · `test_per_model_melee` (the two decisions the phase facade never offers: the charge target step names exactly the units within 12" and within the revealed roll, after a standing charge the agent's strike mask names only units in contact; the charge flag survives the fight STEP's close and not the fight PHASE's, and so does `fought_this_phase`, on both seats; and the consolidate step's three modes, driven through `ShortMovePhase` on a lone fighter: Objective stands on its own rule, Engaging selects within 3" only, and an Engaging move drags the fresh unit in at the unit's close) · `test_per_model_dice` (the seeded source deals the phase facade's advance and charge rolls per unit whichever order they are asked in; an injected `FixedDice` reaches the attack sequence; every draw is tagged with its purpose, the agent's units are asked on their declaration step and the scripted seat's at turn start) · `test_per_model_layering` (AST-walked: each domain sub-domain imports only the kernel and the sub-domains its table allows, the domain never imports the application, `per_model/` never imports `wargame.py`, and only its named clients import `per_model/` — an explicit allow-list of prefixes plus the two root drivers, the repo root walked too, ⚠ not a directory-name exemption, which a `model/per_model/` package would have passed vacuously; companion cases assert the set network and the root drivers really are clients, so the lists cannot go stale) · `test_phase_activation` · `test_fight_sequence` (`FightSequence` driven with the closed loop's own default choice replays `resolve_fight_step` blow for blow on the same dice; a striker touching two units strikes the one it names; every Strikes First unit fights before the opposing player selects) · `test_unit_referees` (a charge stands only against the unit it declared; clipping a second unit or overrunning the roll reverts it whole; a fall back must end clear and coherent; the consolidation mode is the first that applies). · `test_per_model_render` (the eyeball rung made checkable, headless: a frame is drawn before every decision, the decision overlay changes the pixels and a plain re-render does not, the phase cadence draws exactly one frame per settled window, the random seat plays a whole episode under the renderer, and the frames export to an MP4). `tests/per_model_seats.py` holds the small two-unit config these share (now with `n_models` / `group_ids` / `max_groups` for the size-independence cases — a gapped `{0, 2}` army needs `max_groups=3`, since the validator refuses `group_id >= max_groups`), re-exports the random-legal seat from `per_model/random_seat.py`, and carries `charging_driver` / `shooting_charging_driver`, the hand-written seats that reach a standing charge and a fight step.

**Set network (stage 2)** — `test_per_model_tokens` (the token observation pinned to the env it reads, never to a recorded file: the widths at 6 models / 2 units equal those at 10 / 5; own and enemy rows share one width; the reveal rule on the token; **every displacement and unit-pointer column decoded back equals the point's own mask**, on a gapped-id army so a positional column would be caught; column 0 is STAY for a shooter and absent for a striker; offsets antisymmetric; sight and cover equal `visibility_between` within reach and are `present = 0` beyond it — the geometry places the armies 8" apart so both sides of the gate are exercised; the damage relation equals `expected_damage` per pair, ranged and melee; objective counts equal `objective_counts_from_norms_offset`; inside-terrain equals `Footprint.contains`; a corpse's row is present and masked; a stale scenario is refused; the opponent seat's tokens are the mirror) · `test_set_network` (Principle 2's receipts on the network alone: one instance plays two scenario sizes with no reload, `state_dict` shapes are identical across sizes, padding into a larger batch changes nothing, a dead model's features reach nobody — ⚠ the point predates the corpse there, so the test masks the selector itself; the env would never name it — the selector offers exactly the env's mask, every head masks what the env forbids, an all-dead row stays finite; and the size pin: 4/128/8 by default, the 2/32/4 fixture ~20x smaller) · `test_set_agent` (a network-driven episode terminates legally on the plain and the melee config, greedy and sampled — the agent raises on any decoded decision the point refuses, so termination is the proof; seeded sampling reproduces; **every finite column of the active head decodes to a decision the point accepts**, one point per (kind, phase) the shooting-and-charging driver reaches, with the fight step's column 0 absent and the target step's column 0 the decline; the scenario cache is rebuilt for a new episode, on random terrain so the rows actually differ; the closing step carries a value and no policy factor).

**PPO over decision steps (stage 3)** — `test_per_model_reward_timing` (who a step pays and what a close pays, each against a direct calculator call or a hand computation: every registry key has a payment class and the module refuses to import otherwise, a calculator outside the registry — or the legacy `closest_objective` — is refused by name, the env's own calculators are refused, and `_CloseView` is a `BattleView` that survives `deepcopy`; an `act` pays the actor `weight × calc / n_alive` and nobody else, checked against a deep copy of the retimer's own calculator so the potential's anchor matches; a skip `open` and a charge-target decline pay every consumed member, a naming `target` pays nothing; a shooting unit's kills land on its LAST member's step to the attackers the results name — ⚠ harvested from `_advance`, a diff around `apply` sees none; the close pays `vp_gain` the whole turn's net delta against `env.player_vp` read at the two closes, and a state global `scale × coverage`; on bridge-identical scripted play the EPISODE totals of `vp_gain` and the terminal bonus equal the phase facade's to 1e-12, and `model_kills` does on an episode with no player losses — the phase facade divides by the alive count AT SETTLE; a terminating decision step pays its terms and the close bundle, pinned on the retimer directly because the shipped criteria settle at phase boundaries and land on `close_turn`; a `close_turn` reports the opponent-turn strike-back kills; the close gives attrition no kill credit, like the window; attaching a retimer leaves the env's settled windows bit-identical; and the `closest_objective_v2` memo recomputes for two contexts sharing a turn — the regression for the recycled-id defect) · `test_per_model_ppo` (`compute_gae` against a hand-computed five-step sequence with `gamma` only across the closes and a mid-turn cut bootstrapped at 1.0; a lockstep rollout over a 6/2 and a 10/5 scenario in one batch runs to budget with every step legal and the second rollout CONTINUES the first's episodes — no reset between them; a terminating env is reset inline and reported; `evaluate_transitions` reproduces every sampled log-prob to 1e-5 with zero log-prob and entropy on closing rows; one update moves the weights and reports finite stats; dropout is refused) · `test_per_model_evaluate` (two evaluations of the same seeds are identical and leave the agent as they found it; the readouts agree with the env at the end) · `test_train_per_model` (the driver called as a function on a temp YAML from `small_config`: two updates, the baseline row first, eval on every update, `last.pt` and `pm-*.pt`, `env_config.yaml` verbatim, `provenance.json`; the checkpoint loads and plays an episode; a curriculum config and a cadence off the rollout grid are refused by name; the eval wave is two of three episodes and the row carries `mean_episode_decisions`, never `mean_episode_steps`).

**Evaluation and tooling under the per-model step (stage 4, #287)** — `test_evaluation_kernel` (the shared `EvalResult` — `BaselineResult` IS it — and `read_end_of_episode` on a hand-placed board: a contested objective scores for nobody) · `test_per_model_evaluate` (a wave of two scores exactly what one env at a time does; envs of two sizes share a wave; the readouts agree with the env; without retimers the per-model-only columns are `None`, not 0; a scripted chooser is seated BEFORE reset and read per call; mismatched lengths refused) · `test_per_model_coherency` (the intent at a movement close, summed unit by unit, equals `coherency_counts` over the force; trackers reset per episode; the opponent's is opt-in) · `test_per_model_recording` (phase cadence: exactly `1..max_turns` events, two from one `close_turn` when command is stepped, the window's `player_actions` are the acts, the shooting window carries its volley and the next does not, an exporter changes nothing settled; decision cadence: one event per `step()` carrying the decision, codec round trip and `snapshot_at` against the live list at every step — a dead model's `closest_objective_*` blanked on both sides, since a delta cannot express a field returning to None — anchors on the decision counter, `episode_step <= decision_budget` on random episodes with and without melee; provenance round-trips facade and cadence, a phase-facade log decodes to `EpisodeProvenance`, an unknown tag is refused rather than dropped; `analyze_match` refuses decision cadence and accepts phase; `provenance_of` refuses a per-model log; `record_episode` writes a decodable log with its driver) · `test_action_selector_resolution` (extended: a `.pt` refused by the phase resolver and a `.ckpt` by the per-model one, both by name; the per-model resolver seats a baseline, `random`, `set_network` and a `.pt` — labelled by its run suffix — and every first decision is legal; `evaluate_spec` on a baseline equals the three-call path and on a `.pt` returns a result with no decode; `record_spec` and `record_per_model`; and a subprocess pin that importing `scoring` and the per-model resolver loads no torch — the factory that registers the `model` opponent is imported lazily for exactly that) · `test_per_model_bridge` (two more pins: a script scores identically through both facades in every `EvalResult` field, coherency and intent included, and records identically at phase cadence up to the action vectors, where the per-model side may read STAY for a shot at a unit an earlier volley wiped and must agree fully on movement windows) · `test_per_model_layering` (also: `selectors.py`, `scoring.py` and `scripts/measure_throughput_per_model.py` are the named clients, `scripts/` is walked, `per_model/` never imports `baseline/`, and the evaluation kernel imports no facade).

## New Features

Cover: config validation · env integration · obs/info · tensor shapes · network forward · backward compat

Run `just validate` before pushing.
