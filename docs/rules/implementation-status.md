# Implementation status

What the environment implements against the [rules reference](README.md), and where it
knowingly departs.

Status values:

- **implemented** — the environment follows the rule.
- **partial** — some of the rule is in place; the missing part is named.
- **divergent** — the environment does something *different* on purpose. Not a bug.
- **absent** — nothing in the environment corresponds to the rule.

Every **divergent** row is a deliberate simplification, and saying so is the point: this
table is the roadmap for the next implementation phase.

## Board and timing

| Rule | Status | Owner / note |
|---|---|---|
| [Distances in inches](README.md#conventions) | **partial** | `inches_per_unit` (default `1.0`) and `envs/domain/scale.py` now define the mapping, and `envs/domain/rules_quantities.py` resolves rules distances into board units once at construction. Coordinates are continuous, so a distance finer than one unit is representable and a move of speed *s* covers exactly *s* in every direction. |
| [Board 44" × 60"](15-missions-and-scoring.md#setting-up-a-battle) | **divergent** | `board_width` / `board_height` in `envs/types/config/env.py`, default `50 × 50`. |
| [Five battle rounds](07-battle-round.md) | **divergent** | `number_of_battle_rounds` defaults to **100**. Episodes are sized for training, not for the tabletop. |
| [Round → turn → five phases](07-battle-round.md#player-turns) | implemented | `envs/types/game_timing.py` (`BattlePhase`), `envs/domain/game_clock.py`. |
| [Phase order](07-battle-round.md#player-turns) | implemented | `BATTLE_PHASE_ORDER`. |
| [Setup step order](15-missions-and-scoring.md#setting-up-a-battle) | partial | `SetupPhase` enumerates the steps; `game_clock.skip_setup` runs straight past them. |
| [Start/end triggers, rules sequencing](01-core-concepts.md#resolving-simultaneous-rules) | absent | Only one hook exists — `on_before_advance`, fired on every phase boundary by `envs/domain/turn_execution.py`. |
| [Battle continues after a wipe](15-missions-and-scoring.md#ending-the-battle) | implemented | `terminate_on_player_elimination` defaults to `False`. |

## Units and models

| Rule | Status | Owner / note |
|---|---|---|
| [Unit contains models](01-core-concepts.md#armies-units-and-models) | **divergent** | There is no unit entity. `WargameModel` (`envs/domain/entities.py`) is an individual; `group_id` is the closest thing to a unit and carries no rules of its own. |
| [Move (M)](02-unit-profiles.md#model-characteristics) | **partial** | `ModelConfig.move` (inches, resolved through the scale) gives each model its own M; `max_move_speed` is the scenario-wide default when it is unset, which every shipped config leaves it at. The speed bins keep their *count* and span each model's own M, so the action space is unchanged and a uniformly-fast army is byte-identical to one that never set the field. Two gaps remain: M is **not in the observation**, so a network cannot yet tell a fast model from a slow one and differing speeds would make the same action index mean different distances to it unseen; and a squad-marching scripted policy marches at its slowest member rather than giving each model its own budget, which the rules allow (§ Different Move characteristics) but which is what keeps its formation rigid. |
| [Base sizes](02-unit-profiles.md#bases) | partial | `base_radius` in `envs/types/config/env.py`, authored in inches and resolved through the scale onto `WargameModel.base_radius`. One radius for every model rather than per profile. **Defaults to the rules' 32mm infantry base** (radius 0.63in) as of 2026-08-12; it was **0.0** before, the dimensionless point every result prior to that was measured under. Where non-zero it separates models at placement, insets the board clamp, measures objective range from the base edge, makes engagement base to base, blocks movement, and sets the width of the corridor traced for sight — which is what lets terrain grant cover. It does **not** occlude sight (see Line of sight). Setting it back to 0.0 switches three mechanics off at once, silently. |
| [Toughness, Save, Wounds](02-unit-profiles.md#model-characteristics) | implemented | `ModelConfig` → `WargameModel.stats` (`toughness`, `save`, `max_wounds`, `current_wounds`) via `envs/domain/battle_factory.py`. |
| [Invulnerable save (InSv)](02-unit-profiles.md#model-characteristics) | absent | No field on `ModelConfig`; `resolve_shooting` checks one save only. |
| [Resolve (Rv)](02-unit-profiles.md#model-characteristics) | absent | — |
| [Control Value (CV)](02-unit-profiles.md#model-characteristics) | **divergent** | Control is a headcount of alive models in range; CV is implicitly 1 for everyone. `env_components/distance_cache.py`. The observation's per-objective counts are normalised by the **larger** of the two establishments rather than each side's own, so the two columns stay comparable the way the raw count comparison they describe is. |
| [Weapon profile](02-unit-profiles.md#weapon-characteristics) | partial | `WeaponProfile` carries `range`, `attacks`, `ballistic_skill`, `strength`, `ap`, `damage`. No `melee_skill`, no multiple profiles. |
| [Modifier order and clamps](02-unit-profiles.md#modifiers) | absent | Nothing modifies a characteristic at runtime, so nothing needs clamping. Pydantic bounds on `ModelConfig`/`WeaponProfile` are validation, not the rules' clamps. |
| [Random characteristics](02-unit-profiles.md#random-characteristics) | absent | Every characteristic is a fixed integer. |
| [Healing and reviving](02-unit-profiles.md#wounds) | absent | `take_damage` only ever decreases. |
| [Keywords](02-unit-profiles.md#keywords) | absent | No keyword system, so every keyword-gated rule below is unreachable. |

## Moving

| Rule | Status | Owner / note |
|---|---|---|
| [Move as a distance budget](03-moving.md#making-a-move) | **divergent** | Polar encoding — one (angle × speed) displacement per model per step. See `docs/movement.md`. |
| [Cannot cross the board edge](03-moving.md#making-a-move) | implemented | Positions are clipped to the board. |
| [Cannot move through enemy models](03-moving.md#making-a-move) | implemented | `domain/movement.py:resolve_move` treats enemy bases as **blockers**: the move stops at base contact. Friendly bases are `passable` — crossable but not endable-on — **which is what the rules say** (03-moving.md § Making a move: "it may pass through friendly models"), and which also avoids a squad gridlocking on its own front rank. Verified empirically: a model at (20,20) asked to move +6" at an enemy centred 3" away ends 1.74" along, exactly `3 - 0.63 - 0.63`. Gated on `base_radius > 0`, which defaults to the rules' 32mm infantry base, so this is on in every config. ⚠ This row read *absent — movement ignores occupancy entirely* until 2026-08-22; it predated bases. ⚠ The first correction of it claimed the rules do not permit passing through friendly models. They do, in the same section — that was my error, not the spec's. |
| [Coherency (2" / 9")](03-moving.md#coherency) | **partial** | The predicate is exact — `domain/coherency.py` evaluates both conditions *and* the single-connected-group clause, base to base, over alive models only, at the `coherency` config block's distances (defaulting to the rules' 2"/9"). `just measure-coherency` reports it for any policy. What is enforced is **deployment only**, via `coherency.enforce_at_deployment` (§ Setting up), and it is **off by default** — turning it on is a scenario change that moves every baseline. The **end-of-move revert** (§ Making a move) is implemented behind `coherency.enforce_move`, in three modes: `revert_unit` (the spec — one model out of place cancels the unit's move), `revert_model` (a divergence returning the models in breach) and `repair` (the largest divergence — it pulls stray models onto the unit body and reverts only when the unit cannot be gathered, so unlike either revert it can *restore* coherency rather than only refuse to break it; on nine held-out tables it takes a trained agent −34.8 → −4.5 vp_margin, but it moves every policy on the board, so it is a scenario change). A third mode, `clamp`, *shortened* the move instead of cancelling it; it was removed on 2026-08-16 once all three measured the same price and enforcement was established as a play-time referee rather than a training lever, which made the choice of mode a detail. **`revert_unit`, the spec's own mode, is free relative to the divergence** — on nine held-out tables with four checkpoints (two per warm-start lineage) the referee costs ~7 vp either way: off 87.2, `revert_model` 80.4, `revert_unit` **79.4**, 1 vp apart with the sign flipping across checkpoints. So use the spec mode at play; the divergence buys nothing. **The price of the rule is measured and was the same for all three**: on the real-tables scenario with a checkpoint that never saw the constraint, `vp_margin` +104.2 → +76.7 / +75.8 / +78.3 respectively, so the cost is not the revert's abruptness. **⚠ Enforce at play, never during training.** Ten runs on the real tables: measured with the referee *off*, so the numbers describe the policy rather than the wrapper, a policy trained under enforcement intends **0.569** units coherent against **0.756–0.886** for `objective_hold.require_coherent` alone, and loses on unseen ground too — **70.3** vp_margin on nine held-out tables against gate-only's **81.5**. Every reverted action produces the identical outcome, so they share an advantage and the policy gradient inside that set is exactly zero; only the entropy bonus acts there. Enforcement guarantees a legal board and teaches nothing. It is not the learned policy's either — the *scripted* bar pays the same toll (+92.2 → +67.5 under `clamp`), which makes ~25 vp the price of coherency on that mission rather than evidence of a policy trapped by it. Score any enforced arm against a bar measured **on the enforced config**. Selection **iterates to a fixed point** in both modes — reverting a model changes whether its unit is coherent, so `revert_model` re-checks and escalates to the whole unit when a local revert leaves the unit broken; a single pass enforced nothing, breaking units at the same rate as no enforcement at all. The guarantee is *this move did not break the unit*, not *the unit is coherent*: a unit already split by casualties cannot be repaired by a revert, which is what attrition is for. **§ Regaining coherency's attrition** is implemented behind `coherency.attrition`, at the last phase boundary of a player's turn (there is no End of Turn phase on this clock, and adding one would change episode length). Removal is deterministic — smallest chain component first, then the model furthest from the rest — and the casualties are **not** credited to the opponent, per the rule that they trigger nothing firing on destruction. Remaining divergences: the check happens at a phase boundary rather than a real End of Turn step, and a failed *deployment* raises rather than removing the unit from the board. `group_max_distance` + `group_cohesion` remain as the old one-distance approximation, and price only the nearest neighbour. **The cheapest way to play this rule legally is not enforcement at all but a legal decode**: `model/common/decoding.py` picks the most probable *legal* combination of per-model moves (top-3 each, 243 combinations per five-model unit), which is worth **+26.8 vp_margin and coherency 0.651 → 0.847** on three seeds over nine held-out tables, for no weight change and no divergence from the rule. |
| [Set up in coherency](03-moving.md#setting-up) | **partial** | `coherency.enforce_at_deployment` places every unit in coherency by construction — each model lands within the chain distance of an already-placed squadmate and within the spread cap of all of them. Default placement does not: measured at **0 of 20 episodes** coherent on the golden shooting config, which is why even a policy that never moves scored 0.006. With it on, the same policy scores 0.992. On failure this raises rather than removing the unit from the board, as the rules would. |
| [Engagement range 2" / 5"](03-moving.md#engagement) | **divergent** | `engagement_range` in `envs/types/config/env.py`, authored in inches and resolved through the scale. Defaults to **1**, not the rules' 2, because every baseline and trained result in the repo was measured at 1 — adopting the rules value changes which shots are legal and is a scenario change to be measured, not a correction. No vertical component. Used only to gate shooting. |
| [Remain stationary](09-movement-phase.md#remain-stationary) | implemented | The `"stay"` action slice, `STAY_ACTION = 0`. |
| [Normal move](09-movement-phase.md#normal-move) | implemented | The `"movement"` action slice. |
| [Advance move](09-movement-phase.md#advance-move) | **partial** | `n_advance_speed_bins` (default **0**, opt-in per scenario) registers an `advance` action slice: `n_movement_angles x n_advance_speed_bins`, valid in the movement phase. Distance is an **absolute rung above the model's Move**: `M + (bin + 1) x (6 / bins)`, so at `M = 6` with three bins the ladder is 8"/10"/12". One D6 is rolled per UNIT at the command→movement boundary and gates which rungs are **legal** (`ActionHandler.advance_legality`, masked on both seats), rather than deciding what an action means. ⚠ **Re-encoded 2026-08-23**, replacing `fraction x (M + roll)`, which had two defects: it admitted **dominated** actions (a 4" advance costs the turn's shooting and buys nothing a 4" normal move would not — half the slice in expectation) and it was the only slice in the game whose action indices changed meaning turn to turn, so a policy had to read `advance_roll` to know what its own action did. The reason recorded for admitting dominated bins — that a unit which cannot stop short cannot advance and halt to keep coherency — was checked against `env.step` and does **not** hold: only one model need choose an advance for the unit to advance, and its squadmates keep the whole normal slice. ⚠ At three bins a roll of 1 leaves **no** legal rung; the rules would permit a 7" advance and the ladder cannot express it, which is a resolution limit and not a bug — a 1" gain never repays a turn of fire. ⚠ The re-encoding is a **no-op for any non-advancing policy** (`squad_march_take` scores −2.8 / `held` 2.57 / `alive` 0.396 / `coherent` 0.845 on all 45 tables either side of it) but **voids the advance arm's checkpoints behaviourally** — the tensor width is unchanged so they still load, and their action indices now mean different distances. Advancing sets `advanced_this_turn`, which both shooting masks already read, so it forfeits the turn's shooting — correct here because no weapon has the ability that would permit firing after an advance. ⚠ The slice is appended **after** shooting so no existing action index moves; widening `n_speed_bins` instead would renumber everything (`decode_action` is angle-major, speed-minor) and warm starts load with `strict=False`. ⚠ The dice come from a **dedicated RNG stream**, drawn only when bins > 0, so no existing config's terrain, deployment or combat rolls shift. The roll and the spent-shooting flag are **both in the observation** when bins > 0 (`advance_roll`, normalised by the die so it is not 6x the scale of its neighbours, and `advanced_this_turn` for the value head — the action mask already forbids the shot). ⚠ **An advance scenario CANNOT be paired against a non-advance control.** Turning bins on changes the ACTION space (101 → 149 on `4v4_two_phases`), so the output head is a different shape and the first sampled action can differ; a zero-initialised conditioning path fixes an added *input* but cannot fix an added *action*. Compare unpaired, with more seeds, against a control retrained at the same epoch budget. Advance is **declared at unit level, in the command phase** (2026-08-23): a `move_type` slice of two actions (`normal`, `advance`), resolved from the unit's lowest-indexed alive model, binds the whole unit and spends its shooting immediately. STAY declares `normal`, so a policy that does not act in the command phase is unchanged. ⚠ This replaces an **OR over five per-model movement actions** — any one model picking a long bin used to spend all five models' shooting, which fires on 85.5% of five-model unit-turns at initialisation. ⚠ **`n_advance_speed_bins > 0` now requires the command phase**, and a config that skips it is rejected at construction. The advance roll moved to the **start of the side's turn** so the declaration is not made blind. Separating the declaration from the DISTANCE is what makes it safe: every member of a declared unit keeps the whole movement slice and can stop short to hold coherency, where a leader-only rule would cap squadmates at `M` and break the 2" chain. ⚠ **Still divergent:** each model still chooses its own distance independently. The rules have the unit make one move; here the unit makes one move *type* and five distances, so a 12"-versus-6" split inside a 2" chain can still break coherency. The move type is no longer divergent; the distance is. And an advancing unit should also be unable to declare a charge; the charge phase is a stub, so nothing enforces that yet — whatever implements it must read `advanced_this_turn`. |
| [Fall-back move](09-movement-phase.md#fall-back-move) | **partial** | Behind `melee.enabled` (default off, an exact no-op). ⚠ The GEOMETRY was already there and always has been: `back_off_to_unengaged` forces every endpoint out of engagement, which *is* the fall-back constraint. What was missing is the **cost** — before this an engaged model took an ordinary move, walked out of contact for free, and shot in the same turn. `ActionHandler._mark_fall_backs` now marks a unit that began its movement phase engaged and moved, and `fell_back_this_turn` forfeits the turn's shooting on **both** seats, as `advanced_this_turn` does. Unit-level: a model that stood still is still in a unit that withdrew. Gaps below. |
| — declared as a move type | absent | `DEFERRED: fallback.declared_move_type` — v1 INFERS the fall back (began engaged, then moved) rather than having the unit declare one. A declaration lives in the command phase, which most configs skip, and inferring it costs no action and no agent step. |
| — reckless break | absent | `DEFERRED: fallback.reckless_break` — ordered withdrawal only. Reckless break needs a backfire roll per model and a suppression roll, and neither suppression nor backfire exists. |
| — cannot declare a charge after falling back | absent | `DEFERRED: fallback.blocks_charge` — the shooting forfeit is implemented; the charge half needs the charge phase, which is still a stub. Whatever implements it must read `fell_back_this_turn`. |
| [Normal move requires an unengaged unit](09-movement-phase.md#normal-move) | **divergent** | The rules make a Normal move *eligible only* for an unengaged unit, so an engaged unit's only option is to fall back. Here an engaged unit still takes an ordinary move — it simply pays the fall-back cost for it. The observable difference is the maximum distance, which the rules cap at **M** for a fall back and which an advance rung could exceed. |
| [Set up / deployment zones](03-moving.md#setting-up) | partial | `envs/domain/placement.py` places models randomly inside a zone; no alternating deployment. **Wholly within IS enforced** — `_fits_in_zone` requires `Polygon.distance_to_boundary` to be at least the base radius. ⚠ Until 2026-08-22 it probed four cardinal points of the base, justified by "the zones are convex or nearly so". Both halves were wrong: 34 of the 45 real zones are triangles, staircases and arcs, and **convexity was never the property that mattered** — a cardinal probe misses whenever the nearest boundary point is not in a cardinal direction, which one edge at any angle to the axes is enough to cause, at convex and reflex vertices alike. Measured over the real zones it accepted **269 of 195,601 positions (0.14%) whose base crossed the boundary, on 30 of 45 tables**; the exact test accepts 0, and is 2–3x faster. |
| [Set up outside the zone (training only)](03-moving.md#setting-up) | **divergent, deliberate** | `start_on_objective_probability` teleports one group onto an objective at reset — outside its deployment zone, which no legal turn can do. A start-state augmentation for an optimisation failure, not a rule. Off by default and applied only on `reset(options={"augment_start": True})`, which the PPO rollout passes and no evaluation path does. Placement keeps the squad *unengaged* per the spec, but the opponent's free first turn can close to contact before the agent's first observation. |

## Attacks

| Rule | Status | Owner / note |
|---|---|---|
| [Select weapons](04-making-attacks.md#1-select-weapons) | **divergent** | `wargame.py:_resolve_shooting_action` fires `weapons[0]` only. A model with two weapons uses one. |
| [Select targets — visible, in range, unengaged](04-making-attacks.md#2-select-targets) | **partial** | `env_components/shooting_masks.py:compute_unit_shooting_masks`. A weapon names an enemy **unit**, and visibility and range are checked **independently** across it — some model visible, some model in range, not necessarily the same one. ⚠ **This row read `implemented` and was WRONG on the unengaged clause**, which gated only the *shooter*: nothing reduced the target axis, so a unit locked in melee could be shot freely by anyone not themselves engaged. Now implemented behind `melee.enabled` (`exclude_engaged_targets`), on **both** seats, and a bit-identical no-op while it is off. ⚠ It went unnoticed because it cannot currently fire — `back_off_to_unengaged` runs on every mover on both seats, so engagement is **0.0000%** of model-pairs — and because `.claude/hooks/docs_check.py` globbed `docs/*.md`, which does not recurse, so **this file had never been read by the drift check**. |
| [Identical attacks / pooling](04-making-attacks.md#identical-attacks) | absent | Each shot is resolved on its own. |
| [Excess attacks against a wiped unit are lost](05-attack-sequence.md#4-inflict-damage) | implemented | The **only** discard. Measured 3.6% of declared shots; it was 36-40% while a weapon named a model and a shot at a corpse evaporated. |
| [Hit rolls, critical hit on 6, unmodified 1 fails](05-attack-sequence.md#1-hit-rolls) | implemented | `envs/domain/shooting.py:resolve_shooting`. |
| [Wound ladder](05-attack-sequence.md#2-wound-rolls) | implemented | `envs/domain/shooting.py:wound_roll_threshold`. Asserted by `tests/test_rules_constants.py`. |
| [Highest Toughness in a mixed unit](05-attack-sequence.md#mixed-toughness) | absent | Targets are individual models. |
| [Allocation groups and order](05-attack-sequence.md#3-save-rolls) | partial | The defender allocates each attack to a model in the target unit, preferring one that has already lost Wounds (`domain/shooting.py:_allocate_target`). The *groups* themselves are absent — they split a unit by CHARACTER and by distinct (W, Sv, InSv), and this project has one profile per army and no characters, so every unit is a single group. |
| [Save vs AP](05-attack-sequence.md#4-inflict-damage) | implemented | `save + ap`, with an unmodified 1 always failing. |
| [Excess damage is lost](05-attack-sequence.md#4-inflict-damage) | implemented | `WargameModel.take_damage` clamps at 0. |
| [Destruction resolves after the attacking unit finishes](05-attack-sequence.md#suffering-damage-and-being-destroyed) | implemented (degenerately) | Attacking units resolve one at a time in group order, so a destroyed model is removed before the *next* unit fires — the rules' own sequencing. Within one unit's sequence, deferring removal is unobservable at `max_wounds: 1`: a destroyed model stops being allocatable the moment it dies either way. It would become observable with multi-wound models. |
| [Piercing damage](06-visibility-and-damage.md#piercing-damage) | absent | — |
| [Backfire rolls](06-visibility-and-damage.md#backfire-rolls) | absent | — |

## Shooting phase

| Rule | Status | Owner / note |
|---|---|---|
| [Normal shooting](10-shooting-phase.md#normal-shooting) | implemented | The `"shooting"` action slice; see `docs/shooting.md`. |
| [Advance blocks normal shooting](10-shooting-phase.md#normal-shooting) | **implemented** | `compute_unit_shooting_masks` takes `player_advanced` and both seats pass it — the player's from `observation_builder`, the opponent's from `wargame.py::_opponent_action_mask`. `ActionHandler._mark_advancing_units` sets the flag for every model of any unit in which one model chose an advance, so the forfeit is unit-wide as the rules require. ⚠ This row read "never set, so the gate is dormant" until 2026-08-23; it was made live when the advance slice shipped and the row was not updated. Measured cost to a scripted policy: 12.7% of shooting slots forfeited at an advance rate of 11.2% of unit-turns. |
| [Run-and-gun / sidearm / indirect shooting](10-shooting-phase.md) | absent | Only one shooting type exists. |
| [Engaged units cannot shoot](10-shooting-phase.md) | implemented | `compute_shooting_masks` rejects an attacker within `engagement_range` of any opponent, measured base to base. |
| [Engaged large models can be shot at](10-shooting-phase.md#shooting-at-engaged-large-models) | absent | No `MONSTER`/`VEHICLE` distinction. |

## Charge and fight

| Rule | Status | Owner / note |
|---|---|---|
| [Charge move](11-charge-phase.md#charge-move) | **partial** | Behind `melee.enabled`. The charge phase is a real agent step and the **movement slice is legal in it** — no new movement actions. ⚠ Reach is not the constraint it looks like: `back_off_to_unengaged` parks every mover **1.000008740"** from an engagement range of 1.0, i.e. **8.7 micro-inches** outside contact, so the distance a charge must cover is one speed bin. A dedicated absolute-rung ladder would be ~80 actions bought to cross a hundred-thousandth of an inch, and would change the output head's shape — which is what makes an action-space arm unpairable against its control. The charge is **the one move whose endpoint may be engaged**: `apply` drops the engagement rings in that phase only, and still passes the occupied bases, so an endpoint may be engaged but never inside another model. 2D6 per unit from a dedicated `_charge_rng`. Gaps below. |
| — declare blind, then roll, then choose targets | **divergent** | `DEFERRED: charge.blind_declaration` — the 2D6 is rolled at the start of the side's turn and is visible before the charge is made, exactly as `advance_roll` is. Legality is gated on the roll, so a declaration taken before it would have no legal distance to take. |
| — charge target declaration | absent | `DEFERRED: charge.target_declaration` — the target is not yet chosen by the agent, so the after-moving conditions ("engaged with ALL targets, with NO non-target") are not yet enforced. |
| — eligibility: within 12", unengaged, did not advance or fall back | absent | `DEFERRED: charge.eligibility` — any unit may currently charge. `advanced_this_turn` and `fell_back_this_turn` both exist and are what it must read. |
| — all-or-nothing revert on a failed charge | absent | `DEFERRED: charge.failed_charge_reverts` — `03-moving.md` returns every model to its start if any after-moving condition fails. |
| — 2D6 caps the charge distance | absent | `DEFERRED: charge.roll_caps_distance` — `charge_roll` is rolled and observable but does not yet mask the speed bins, so a charge currently reaches as far as a normal move. |
| [Fight phase — fight step](12-fight-phase.md#fight-step) | **partial** | `domain/fight.py:resolve_fight`, behind `melee.enabled` (default off, an exact no-op). Engaged models strike the unit they are in contact with; the defender allocates via the shared `_allocate_target`, and the attack sequence is `domain/shooting.py:resolve_attack`, extracted so melee and shooting cannot resolve the same dice differently. Resolved on the boundary LEAVING the fight phase, before `_regain_coherency`, so a unit shredded in melee can then lose stragglers to attrition — the rule's order. The phase carries **no agent action** and stays in `skip_phases`: its mask offers exactly one legal action per model (STAY), so stepping it would cost an agent step per round for a choice with one option. ⚠ `melee.enabled` therefore requires the **charge** phase only — an earlier validator demanded the fight phase too, which rejected the very config this design wants. Gaps below. |
| — alternating activation | absent | `DEFERRED: fight.alternating_activation` — v1 order is fixed: the active player's units, then the opposing player's, each in group order. The rules alternate between players and return to the Strikes First sub-step whenever a new Strikes First unit becomes eligible. |
| — passing | absent | `DEFERRED: fight.passing` — a unit whose targets all die simply does not fight. The rules would let it wait for an enemy pile-in to bring something within 5"; with no pile-in there is nothing to wait for. |
| — select a melee weapon | absent | `DEFERRED: fight.select_weapon` — a model fights with `melee_weapons[0]`. A choice with no second case while every model carries at most one. |
| — fighting after death | absent | `DEFERRED: fight.fighting_after_death` — a destroyed model is removed at once and never swings. A no-op only while every model has one wound. |
| [Strikes First](16-ability-reference.md#strikes-first) | absent | `DEFERRED: fight.strikes_first` — nothing grants it yet, because nothing charges. Whatever implements the charge must grant it to every model of a charging unit until the end of that turn. |
| [Pile in](12-fight-phase.md#pile-in-step) | absent | `DEFERRED: fight.pile_in` — no 3" close-up, so a unit engaged at the edge of engagement range fights from there and a unit that loses its nearest model cannot close. |
| [Overrun fight](12-fight-phase.md#overrun-fight) | absent | `DEFERRED: fight.overrun` — a unit that destroys its target cannot reach a new one this phase. |
| [Consolidate](12-fight-phase.md#consolidate-step) | absent | `DEFERRED: consolidate.step` — no consolidation move at all. ⚠ When it lands, note the modes are **ordered and compulsory**: Objective mode fires only for a unit that is unengaged AND not within 3" of an enemy, which after a successful charge is almost never. |
| [Melee weapon profile](02-unit-profiles.md#weapon-characteristics) | **partial** | `MeleeWeaponProfile` (`types/config/entities.py`): attacks, melee skill, strength, AP, damage. One profile per model, and no `[EXTRA ATTACKS]` or `[SIDEARM]`. ⚠ Cover does **not** apply in melee, per `12-fight-phase.md`, so a melee expected-damage figure is not comparable to a shooting one measured with cover in play. |

## Terrain and visibility

| Rule | Status | Owner / note |
|---|---|---|
| [Line of sight](06-visibility-and-damage.md#visibility) | **divergent** | **Only terrain blocks; models never do.** The rules let enemy and third-party models block, with the observer's own unit and the target's exempt. Neither applies here: no model on this table has an opaque silhouette — a 32mm infantry figure is a thin shape on a round base rather than a cylinder, so a line that clips a base can be drawn in practice — and with nothing occluding, the *ignore your own unit* exemption has nothing to exempt. Removed 2026-08-13, taking `Occluders` / `group_keys` / `sight_between` / `segments_clear_of_discs` with it; `git log -- wargame_rl/wargame/envs/domain/sight.py` restores it. **Every baseline predates the change**: on `golden/25v25_shooting_opponent.yaml`, 100 identical layouts, `squad_march_shoot` moved +38.0 → +17.0 vp_margin. `envs/domain/los.py` samples points along the segment at `los_sample_step` (default 0.25"). Three rays per pair give the three-state answer (hidden / cover / visible); the rays are a parallel corridor of the pair's width rather than tangents to the target, so sight stays exactly symmetric — the metrics depend on that. No height, so nothing is *fully* visible by the rules' vertical test. |
| [Terrain categories](13-terrain.md#terrain-categories) | **divergent** | One category. `Footprint` (`envs/domain/terrain.py`) is a closed outline — concave allowed — that blocks line of sight. |
| [Terrain and movement](13-terrain.md#terrain-and-movement) | absent | Movement ignores terrain completely — models pass through footprints freely. |
| [See out of and into a feature](13-terrain.md#obscuring) | **divergent** | `domain/sight.py` exempts any footprint containing either endpoint (`polygons_contain_points`, edge-inclusive). Two divergences, and the second is the load-bearing one. **The exemption belongs to obscuring, and is applied to every feature** — the spec grants see-out/see-into in the *Obscuring* section, while *Solid* has no such clause ("line of sight cannot be drawn across any enclosed gap"). With one terrain category there is nothing to withhold it from. **And it exempts the whole piece, not the model's own position**: a model standing inside a 20"-wide ruin sees straight through all 20" of it, verified — outside→outside across the same piece is correctly blocked, inside→anywhere is not. Per-piece, so a *second* ruin on the line still blocks, which bounds it. **Consequential on the real tables specifically**: every one of the 270 objective markers in `configs/evaluation/maps/` sits inside a ruin and on 24 of 45 tables the two centre markers share the largest central piece, so a unit holding the middle objective is standing *inside* that building while it fires through it. This is what "shooting through terrain" looks like in a recording. Fixing it changes LOS on every eval map and voids the baselines measured there. No height, so the 3" threshold has no analogue. **Measured before #193**, which restored the closing edge dropped from padded polygon outlines: the containment test that fires this exemption was over-reporting, so the exemption fired more often than the design alone calls for. On 8 real layouts the fix took grid points reading "inside a piece" 666 → 516 and visible LOS pairs 219,379 → **169,789 (−22.6%)**. The mechanism is unchanged and its test still passes (a single 4-vertex outline needs no padding), but the share of the recording attributable to the *design* rather than to that bug has not been re-measured. |
| [Obscuring](13-terrain.md#obscuring) | **divergent** | Achieved by the same footprint blocking, keyed off the feature rather than an enclosing terrain area. |
| [Cover (−1 RS)](13-terrain.md#cover) | implemented | A target only partly visible along the corridor between the two models is in cover, and `resolve_shooting` worsens the Ranged Skill by `COVER_RANGED_SKILL_PENALTY` via `ranged_skill`. The unmodified 6 still hits, so cover is never an absolute shield. The analytical path takes it too as of 2026-08-16 — `expected_damage` / `hit_probability` share `ranged_skill`, and `PairedShootingResult.in_cover` carries the fact into recordings (schema 2.4), which is what makes a quoted expectation the one the dice were rolled under. **The observation's `expected_damage_matrix` deliberately does not**: it is memoised per stat pair, and cover is per position pair. Since models stopped occluding it is granted by **terrain** alone, which is what the chapter describes. Requires `base_radius > 0`: with dimensionless models the three rays coincide and cover cannot occur. |
| [Hidden and detection range](13-terrain.md#hidden) | absent | — |
| [Elevated fire](16-ability-reference.md#elevated-fire) | absent | The board has no height. |

## Objectives and scoring

| Rule | Status | Owner / note |
|---|---|---|
| [Objective markers, within 3"](14-objectives.md#what-an-objective-is) | **divergent** | `objective_radius_size` in board units, default 1. Range is measured from the model's base edge, per the rules; the radius itself still defaults below the rules' 3". |
| [Terrain objectives](14-objectives.md#what-an-objective-is) | **implemented** | `objectives_on_terrain` makes the objective *be* a terrain footprint — its outline is its area, `radius_size` is 0 and control is standing on the ruin (`entities.py:set_area`, `placement.py:objectives_from_terrain`). The point-and-radius form remains the default, and `objective_terrain_clearance` still pushes *those* away from terrain; the two are alternatives, not a sequence. |
| [Level of control](14-objectives.md#level-of-control) | implemented | `env_components/distance_cache.py:objective_ownership_from_norms_offset` — strictly greater count controls, ties are uncontrolled. **All three readers now share one definition** (`objective_counts_from_norms_offset`). Until 2026-08-22 the *observation* had its own — `area.contains_points` on the model **centre**, against scoring's `norms_offset <= obj_radii` measured from the **base edge** — so the `player_count` the agent was shown disagreed with the count the mission scored on **7.6% of (objective, step) slots** (206 of 2,700, 215 models, held-out nine). Every objective-keyed reward term reads that feature. |
| [Control re-evaluated at the end of every phase](14-objectives.md#level-of-control) | **divergent** | Evaluated only when VP are scored, on leaving the command phase (`wargame.py:_on_before_advance`). |
| [Suppression zeroes Control Value](01-core-concepts.md#suppression) | absent | No suppression. |
| [Secured objectives](14-objectives.md#secured-objectives) | absent | — |
| [VP caps](15-missions-and-scoring.md#caps) | **divergent** | `DefaultVPCalculator` uses `vp_per_objective=5`, `cap_per_turn=15`, `min_round=2`. The per-round cap matches; there is no total cap. |
| [Most VP wins, ties draw](15-missions-and-scoring.md#determining-the-victor) | partial | VP are tracked for both sides; success is defined by the configured criteria (`player_vp_min`, `player_ahead_on_vp`) rather than a plain comparison. |

## Abilities

| Rule | Status | Owner / note |
|---|---|---|
| [Every weapon ability](16-ability-reference.md#weapon-abilities) | absent | `WeaponProfile` has no ability field. |
| [Every unit ability](16-ability-reference.md#unit-abilities) | absent | `ModelConfig` has no ability field. |

Adding either means a new field on the config, a place for it in the observation tensor,
and a branch in `resolve_shooting` — see `wargame_rl/wargame/model/CLAUDE.md` for the
tensor pipeline, and the checklist in `CLAUDE.md` under *Adding New Entity Types*.
