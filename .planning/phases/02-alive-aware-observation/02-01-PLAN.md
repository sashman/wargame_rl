---
phase: 02-alive-aware-observation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - wargame_rl/wargame/envs/types/model_observation.py
  - wargame_rl/wargame/envs/domain/entities.py
  - wargame_rl/wargame/envs/env_components/observation_builder.py
  - wargame_rl/wargame/model/common/observation.py
  - tests/test_wounds.py
autonomous: true
requirements: [WOUND-04, OBS-01, OBS-03]

must_haves:
  truths:
    - "WargameModelObservation includes alive (0.0/1.0), current_wounds, max_wounds for player and opponent slots"
    - "WargameModelObservation.size increases by 3 vs pre-phase-2 width"
    - "build_observation passes alive_mask_for(player_models) into get_model_action_masks"
    - "Dead player models have action_mask row True only at STAY_ACTION (index 0)"
    - "Per-model tensor row includes three trailing floats: alive; normalized current_wounds; normalized max_wounds per CONTEXT D-05"
    - "observation_to_tensor model matrix width equals feature_dim and matches sum of per-model logical sizes"
    - "WargameModel.to_space includes alive channel consistent with structured obs"
    - "Full pytest suite passes after updating any brittle size assertions"
  artifacts:
    - path: "wargame_rl/wargame/envs/types/model_observation.py"
      provides: "alive, current_wounds, max_wounds fields; updated size"
      contains: "alive:"
    - path: "wargame_rl/wargame/envs/env_components/observation_builder.py"
      provides: "_models_to_obs populates wound/alive; alive_mask on masks"
      contains: "alive_mask"
    - path: "wargame_rl/wargame/model/common/observation.py"
      provides: "feature_dim +3; _models_to_features appends wound tensor channels"
      contains: "feature_dim"
    - path: "wargame_rl/wargame/envs/domain/entities.py"
      provides: "to_space extended for alive/wounds"
      contains: "alive"
  key_links:
    - from: "wargame_rl/wargame/envs/env_components/observation_builder.py"
      to: "wargame_rl/wargame/envs/domain/entities.py"
      via: "_models_to_obs reads model.stats and model.is_alive"
      pattern: "is_alive"
    - from: "wargame_rl/wargame/model/common/observation.py"
      to: "wargame_rl/wargame/envs/types/model_observation.py"
      via: "tensor row width must match WargameModelObservation.size increment"
      pattern: "feature_dim"
---

<objective>
Expose wound status and explicit alive flags in structured observations, Gym `to_space`, and the float tensor pipeline; align observation `action_mask` with alive-guarded env behavior. Satisfy WOUND-04, OBS-01, OBS-03 and roadmap Phase 2 success criteria without changing model slot counts.

Purpose: The policy must see who is alive and how hurt they are while tensor shapes stay fixed when models die mid-episode.
Output: Updated types, builder, `to_space`, `observation.py`, tests; networks pick up new widths via dynamic `observation.size` (breaking checkpoints per CONTEXT D-09).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-alive-aware-observation/02-CONTEXT.md
@.planning/phases/02-alive-aware-observation/02-RESEARCH.md
@docs/ddd-envs.md

<interfaces>
From `wargame_rl/wargame/envs/types/model_observation.py` (extend):
```python
@dataclass
class WargameModelObservation:
    location: np.ndarray
    distances_to_objectives: np.ndarray
    group_id: int
    max_groups: int
    alive: float  # 1.0 or 0.0
    current_wounds: int
    max_wounds: int
```

From `wargame_rl/wargame/envs/env_components/actions.py`:
```python
STAY_ACTION = 0
def get_model_action_masks(self, phase, n_models, alive_mask=None) -> np.ndarray: ...
```

From `wargame_rl/wargame/model/common/observation.py` (extend `_models_to_features`):
- Append `np.hstack` columns: shape `(n_models, 3)` for alive, wound ratio, max_wounds/100 clipped.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend WargameModelObservation and WargameModel.to_space</name>
  <files>
    wargame_rl/wargame/envs/types/model_observation.py,
    wargame_rl/wargame/envs/domain/entities.py
  </files>
  <read_first>
    wargame_rl/wargame/envs/types/model_observation.py,
    wargame_rl/wargame/envs/domain/entities.py,
    .planning/phases/02-alive-aware-observation/02-CONTEXT.md
  </read_first>
  <action>
  **model_observation.py**
  - Add fields: `alive: float`, `current_wounds: int`, `max_wounds: int` (after `max_groups`).
  - Update `size` property: add **3** to the return value (three scalar channels aligned with tensor tail).

  **entities.py — `WargameModel.to_space`**
  - Extend the returned `spaces.Dict` so each model’s observation space includes wound/alive consistent with structured obs:
    - `alive`: `spaces.Box(low=0, high=1, shape=(1,), dtype=np.float32)` (or int8 if project standard prefers; prefer float32 to match tensors).
    - Reuse or extend existing `stats` dict: ensure `current_wounds` and `max_wounds` remain `Box(0, 100, (1,), dtype=np.int32)` (or match current dtype).
  - Do **not** remove existing keys; add `alive` alongside `stats` if that is cleaner than nesting inside `stats`.
  </action>
  <verify>
    <automated>uv run ruff check wargame_rl/wargame/envs/types/model_observation.py wargame_rl/wargame/envs/domain/entities.py</automated>
  </verify>
  <acceptance_criteria>
    - `WargameModelObservation` defines `alive`, `current_wounds`, `max_wounds`
    - `size` is old formula + 3
    - `to_space` includes an explicit alive box (or equivalent documented in SUMMARY)
  </acceptance_criteria>
  <done>Dataclass and Gym space carry alive + wound channels</done>
</task>

<task type="auto">
  <name>Task 2: Populate obs fields and wire alive action mask</name>
  <files>wargame_rl/wargame/envs/env_components/observation_builder.py</files>
  <read_first>
    wargame_rl/wargame/envs/env_components/observation_builder.py,
    wargame_rl/wargame/envs/domain/entities.py,
    wargame_rl/wargame/envs/env_components/actions.py
  </read_first>
  <action>
  - Import `alive_mask_for` from `wargame_rl.wargame.envs.domain.entities`.
  - In `_models_to_obs`, for each `WargameModel` set:
    - `alive = 1.0 if m.is_alive else 0.0`
    - `current_wounds = int(m.stats["current_wounds"])`
    - `max_wounds = int(m.stats["max_wounds"])`
  - In `build_observation`, when `action_registry is not None`:
    - `player_alive = alive_mask_for(view.player_models)`
    - `action_mask = action_registry.get_model_action_masks(phase, len(view.player_models), alive_mask=player_alive)`
  </action>
  <verify>
    <automated>uv run pytest tests/test_action_masking.py tests/test_env.py -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `_models_to_obs` passes new constructor args
    - `get_model_action_masks` receives `alive_mask=player_alive`
  </acceptance_criteria>
  <done>Structured obs and mask aligned with env alive rules</done>
</task>

<task type="auto">
  <name>Task 3: Tensor pipeline — feature_dim +3 and model features</name>
  <files>wargame_rl/wargame/model/common/observation.py</files>
  <read_first>
    wargame_rl/wargame/model/common/observation.py,
    wargame_rl/wargame/envs/types/model_observation.py
  </read_first>
  <action>
  - Increment `feature_dim` in `_observation_to_numpy` by **3**.
  - In `_models_to_features`, after existing `hstack` blocks, compute per row:
    - `alive_col = np.array([[m.alive] for m in models], dtype=np.float32)`
    - `cw = np.array([[m.current_wounds] for m in models], dtype=np.float32)`
    - `mw = np.maximum(np.array([[m.max_wounds] for m in models], dtype=np.float32), 1.0)`
    - `ratio = np.clip(cw / mw, 0.0, 1.0)`
    - `mw_norm = np.clip(mw / 100.0, 0.0, 1.0)`
    - `hstack(..., alive_col, ratio, mw_norm)`
  - Update docstrings on `observation_to_tensor` / `observations_to_tensor_batch` to describe the three trailing channels.
  - Confirm `WargameModelObservation` list in obs matches attribute access (`m.alive` etc.) — `models` in `_models_to_features` are `WargameModelObservation` instances, not domain models.
  </action>
  <verify>
    <automated>uv run pytest tests/test_state.py tests/test_action_masking.py -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `feature_dim` includes +3
    - `_models_to_features` output width matches `feature_dim`
  </acceptance_criteria>
  <done>Tensors expose alive + wound features</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Tests — obs fields, dead mask, tensor width</name>
  <files>tests/test_wounds.py</files>
  <read_first>
    tests/test_wounds.py,
    wargame_rl/wargame/envs/wargame.py,
    wargame_rl/wargame/model/common/observation.py,
    tests/conftest.py
  </read_first>
  <behavior>
    - After eliminating a player model, next `reset`+`step` observation has `wargame_models[i].alive == 0.0` and wound fields match `stats`
    - For dead model index, `observation.action_mask[i]` is False except at `STAY_ACTION`
    - `observation_to_tensor(obs, device="cpu")` third tensor has shape `(n_models, feature_dim)` with `feature_dim` equal to `2 + n_obj*2 + max_groups + 1 + 3`
    - Same width before and after elimination (shape stability)
  </behavior>
  <action>
  Add focused tests (prefer extending `tests/test_wounds.py` or new `tests/test_observation_alive.py` if cleaner):
  - Build small `WargameEnv` from fixtures, `reset`, damage model, `step`, assert observation fields and mask row.
  - Assert tensor third element shape[1] matches expected formula using env config.
  </action>
  <verify>
    <automated>uv run pytest tests/test_wounds.py -v --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - New tests cover D-10 from CONTEXT
    - `uv run pytest tests/ -q` passes
  </acceptance_criteria>
  <done>Regression coverage for WOUND-04 / OBS-01 / OBS-03</done>
</task>

<task type="auto">
  <name>Task 5: Sweep brittle size assertions and format</name>
  <files>tests/</files>
  <read_first>
    grep results for size_wargame_models, feature_dim, WargameModelObservation, observation.size
  </read_first>
  <action>
  - Run `uv run pytest tests/ -q`; fix any tests with hard-coded model feature widths or observation totals.
  - Run `just format && just lint` (or `uv run ruff format` / `ruff check`).
  </action>
  <verify>
    <automated>just validate</automated>
  </verify>
  <acceptance_criteria>
    - Full suite green
    - No ruff/mypy regressions on touched files
  </acceptance_criteria>
  <done>Repo clean after obs width change</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/ -q`
- `uv run ruff check wargame_rl/wargame/envs/types/model_observation.py wargame_rl/wargame/envs/domain/entities.py wargame_rl/wargame/envs/env_components/observation_builder.py wargame_rl/wargame/model/common/observation.py`
</verification>

<success_criteria>
- Structured observation and tensors include alive + wound signal for all slots
- Observation action mask matches dead → stay-only
- Shapes stable across elimination
- WOUND-04, OBS-01, OBS-03 satisfied; roadmap Phase 2 success criteria met
- Checkpoints from before this change are incompatible (document in SUMMARY)
</success_criteria>

<output>
After completion, create `.planning/phases/02-alive-aware-observation/02-01-SUMMARY.md`
</output>
