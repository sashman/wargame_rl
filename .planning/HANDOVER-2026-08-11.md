# Handover — 2026-08-11

State of the "replicate the 25v25 golden run on continuous geometry" goal.

## Where things stand

**Goal:** the agent should beat the scripted shooting bar on the phase-03
scenario (continuous board, model bases, polygon terrain, objectives that *are*
terrain footprints).

**Status: not met. Best measured configuration is 9.9 vp short.**

| | agent | bar (`squad_march_shoot`) | deficit |
|---|---|---|---|
| clustered objectives | +50.6 | +67.7 | -17.1 |
| **spread objectives (current)** | **+49.0** | **+58.9** | **-9.9** |

All figures n=100, seeds 700000-700099, `--no-tf32`. The deficit narrowed only
because the **bar fell**; the agent did not improve in absolute terms.

## Nothing is running

No training processes, GPU idle. All watchers stopped.

## Git

- Branch **`experiment/objective-income`**, 9 commits ahead of `origin/main`, clean
  tree, pushed. **No PR opened for it yet.**
- **Pull `main` before branching.** Local `main` is behind `origin/main`, and #160
  was squash-merged, so `git log main..HEAD` lists it as if it were unmerged.
- Merged to `main` today: **#157** (phase-03 geometry), **#159** (45 eval maps),
  **#160** (terrain in snapshots, schema 2.1).
- Other open PRs are unrelated: #165, #137 (marked DO NOT MERGE, superseded by
  #157 — worth closing), #115, #102.

## The best configuration

`configs/experiments/25v25_spread_hold.yaml`

- `objective_hold` weight **2.5**, `objective_coverage` **0.1** (the one reward
  change that replicated), `closest_objective_v2.progress_scale` **6.0**
- `objectives_spread_on_terrain: true`
- terrain `count: 15, min_size: 3, max_size: 11`, rectangles

Its header carries the n=100 baseline table. **The bar is +58.9.**

## What is established (do not re-derive)

1. **`objective_hold` 1.25 -> 2.5 with `objective_coverage` 0.3 -> 0.1 is worth
   +9.8 vp**, both seeds, replicated across two scenarios (+5.2 on the first).
   Moving income from a *global* term to a *per-model* one sharpens the gradient.
2. **Terrain must be fitted to the 45 real layouts** in `configs/evaluation/maps/`,
   never tuned against `just measure-terrain` — that metric rewards piece count
   and produced 0.194 hidden against the real game's 0.088. See
   `reports/2026-08-10-real-terrain-and-the-abandonment-gap.md`.
3. **The agent does not use cover; it manages range.** `blocked_share` 0.738
   against the bar's 0.735 — identical. What differs is how often it is in range
   at all (0.394 v 0.679). Measured on realistic terrain, so the 2026-08-05
   finding now stands on the right environment.
4. **Objective selection must be mirror-symmetric by construction.** Maximising
   spread unconstrained put 2 objectives on one side in 38/200 layouts. Pinned by
   a test.

## What is refuted (do not retry)

- `overstack_penalty_per_extra` -> 0.0 — **null** (+49.4 v control +50.6).
- **Spreading objectives fixes abandonment** — it does not. Measured at 3.6 sigma
  as a natural experiment, it did not transfer to training.
- **Raising `progress_scale`** — monotonically *worse*: 6.0 -> 12.0 -> 17.0 gives
  46.7 -> 35.4 -> 32.1 at epoch ~270.
- `fallback_to_nearest: false` — dropped before running: unassigned models get
  `overstack_penalty` and **zero** progress reward, and group assignment gives
  each objective to one group, so 2 of 5 squads would earn only a penalty.
- **`crowding_exponent` is not the suspect.** At `a=1.0` the pot is conserved, so
  the ninth model on a point already earns a ninth of a lone model's pay.
- **Pinning the assigned target** — refuted before building; the switch rate that
  motivated it is an artefact (above).
- **`model_kills` is not the competing income.** The range-management behaviour
  made it the obvious suspect; it is **4.5% of income**, 1.00/episode. Refuted at
  zero GPU cost by reading the ledger.

## The invariant, and the diagnosis

**Abandonment is 0.380 / 0.370 / 0.357** across five weight configurations and
two scenarios, against the bar's **0.147**. The agent is pushed off an objective
**1.7%** of the time against the bar's **24.7%** — it is not losing fights over
objectives, it is not turning up. It parks ~9 surplus models on the ones it holds
(bar: 3.9) while winning the firefight ~2:1 and keeping a fifth more models alive.

Per-model income says why (probe, n=15 episodes):

| activity | reward/step |
|---|---|
| alone on an objective | **1.30** |
| on a crowded one (8 others) | 0.62 |
| loitering outside | 0.50 |
| **in transit** | **0.26** |

The crowding gradient *works* (1.30 alone -> 0.36 at twelve occupants), and
leaving is profitable over a 20-round episode: ~1.26 of loss over a ~3.3 round
walk, then +0.66/step, breaking even ~1.9 rounds after arrival.

**So the reward is not mis-weighted — the landscape has a trough between two
peaks and the policy sits on the smaller one.** Crossing needs a *sustained*
multi-round walk that single-step exploration is punished for starting.

## The next step — SUPERSEDED, see below

~~Pin a model's target objective once assigned.~~ **Measured and refuted before
it was built** (2026-08-11, later the same day). The 0.273/step switch rate it
rested on is an artefact: 0.292 in transit, **0.063 once each model's first step
is excluded**, because 280 of 335 transit switches are each model's single
initial assignment. Worth ~2.5% of the trough, and 96% of switches involve the
fallback that pinning would not touch. Do not build it.

## What is running instead

**Arms `25v25_global_cut{3,4}`**, 2 seeds each, 600 epochs, `--no-tf32`, wandb
group `global-cut`. They remove the global `objective_coverage` term and move
its income into per-model `objective_hold` (2.5 -> 3.0 and 4.0).

The per-calculator ledger on the `spread_hold` checkpoint, 12 episodes:

| calculator | /episode | share | kind |
|---|---|---|---|
| `objective_hold` | 10.34 | 0.463 | per-model |
| **`vp_gain`** | **7.50** | **0.336** | **global** |
| **`objective_coverage`** | **2.23** | **0.100** | **global** |
| `model_kills` | 1.00 | 0.045 | per-model |
| `closest_objective_v2` | −1.00 | 0.045 | per-model, net negative |

The globals are **44% of income and broadcast whole to every model**, which is
the trough exactly: a loiterer earns 0.46/step, ~0.49 of it globals it gets for
doing nothing, against 0.26 for walking. Five arms tuned what objectives pay;
none cut the floor. This is also the direction of the only change that has ever
replicated here (+9.8 vp), pushed further.

**Falsifier:** if occupancy drops, total objective income fell somewhere and the
arm is dead — the rule that killed `overstack_penalty` and `surplus_value`.

Score against `spread_hold` +49.0 and the bar +58.9 (n=100, seeds 700000+,
epoch 600).

## The teleport result, which reframes the problem

A whole squad placed **free** on the objective the agent normally abandons is
worth **+21.1 vp_margin** — more than the entire 9.9 gap to the bar. The policy
gives it back: **53% walk off in the first step, at 100% survival**, before a
casualty. Retention decays 0.47 -> 0.26 by round 3 -> 0.09 at the end, while
`held` at episode end barely moves (1.75 -> 1.82) — VP accrues per round, so the
gain is bought in the few rounds it stays.

Separating departures from deaths is what makes this a verdict: the objective
averages 4.91 opponents, so the first reading (retention over *all* teleported
models) confounded being shot off with walking off.

**Caveat worth keeping:** the teleported state is off-distribution for the
policy, so its behaviour there is weaker evidence about preference than it looks.

## The reasoning error worth remembering

I justified raising `progress_scale` hard because progress is potential-based and
"telescopes to `scale * (initial - final distance)`, so oscillation cannot farm
it". That holds only if the potential is a fixed function of state — which is
the right general lesson: *a shaping term keyed on an assigned target is only
potential-based while the assignment holds.*

**But the farming explanation of that arm's failure is withdrawn.** At the true
0.063/step churn the potential is close enough to fixed that telescoping
approximately holds, so amplifying a farm cannot be what happened. Two candidates
remain, unseparated: at scale 17 progress is worth ~0.44/step against a per-term
budget of ~10 an episode, so it crowds out `objective_hold` and `vp_gain`; and it
is symmetric, so every retreat or lateral manoeuvre is penalised at that rate.

**The error worth keeping is the second one**: I quoted a rate that bundled an
unavoidable initialisation into a churn measurement, then designed against it.
*Bucket a rate by whether the event could have been otherwise before building on
it.*

## Reports written today

- `reports/2026-08-10-real-terrain-and-the-abandonment-gap.md` — the first
  training run on real-scale terrain, plus the cover resolution.
- `reports/2026-08-11-spreading-objectives-lowers-the-bar-not-the-abandonment.md`
  — the spread result, the two probes, and the `progress_scale` refutation.

## Scratchpad tools (not committed; rebuild if useful)

Under the session scratchpad, all run as
`PYTHONPATH=$PWD uv run python <script> ...` from the repo root:

- `probe_transit_income.py <ckpt> <config> [n]` — per-model income by activity
  (on_crowded / on_contested / in_transit / loitering) plus an occupancy curve.
  Uses `env.phase_manager.last_per_model_reward`.
- `measure_cover_use.py <ckpt|policy> <config> [n]` — range_margin / threatened /
  blocked_share. `blocked_share` is the only real cover metric here, because
  objectives *are* terrain so "hugging ruins" conflates holding with covering.
- `measure_signal_clarity.py <config> [n] [seeds] [ckpt]` — paired per-layout
  policy comparison split by objective separation.
- `measure_geometry_effect.py`, `measure_objective_capacity.py`,
  `measure_income.py`.

## Gotchas that cost time today

- **`pgrep -f "train.py..."` matches the `just` wrapper shells**, so watchers
  self-match and never fire, and kill commands hit wrappers not trainers. Use
  `ps -eo comm=,args= | awk '$1 ~ /^python3?$/ && /train\.py/'`.
- **SIGINT deadlocks the trainers** in a loguru reentrancy storm — they hold GPU
  memory at 0% utilisation. Use SIGKILL.
- **`eval/vp_margin` and `eval/baseline_*_vp_margin` are not comparable** — 30
  episodes at seeds 500000+ against 20 at 10000+. Score with
  `just measure-checkpoint` against the recorded n=100 table instead.
- **`reward/components/closest_objective_v2/target_obj_idx` is a diagnostic**
  (an objective index, -1.0 when unset), not reward. It dominates any naive
  component sum.
- Do not query the Wandb API while runs train; read `wandb/run-*/files/` instead.
